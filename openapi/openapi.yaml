openapi: 3.1.0
info:
  title: Saturation API
  version: 1.0.0
  description: 'The Saturation API lets you read and write production finance data. You work with budgets, transactions, purchase
    orders, payment requests, payments, documents, contacts, and the Library. You can search across them and subscribe to
    webhooks when data changes. Authenticate every request with a personal token in the Authorization header: Authorization:
    Bearer <token> Create a token in Saturation under Settings > Developers > API. The token acts as you in the workspace
    where it was created. Conventions:

    - A successful response is the resource itself. A list is a data array with a nextCursor. Only errors are wrapped, as
    { success: false, code, message, requestId }, with a typed code you can branch on.

    - Money is an integer amount of minor units with a currency, for example { "amount": 152900, "currency": "USD" }. Dates
    are ISO-8601 strings.

    - Lists are paginated. Pass limit (up to 100, default 50) and the cursor from the previous response. Pass withCount=true
    for a total.

    - 404 not_found is returned when a resource is missing or you cannot read it, so existence never leaks.'
  contact:
    name: Saturation API
    url: https://docs.saturation.io
  license:
    name: Proprietary
    url: https://saturation.io/terms
servers:
- url: https://next-api.saturation.io/v1
  description: Production
- url: http://localhost:4300/v1
  description: Local development
security:
- bearerAuth: []
tags:
- name: Meta
  description: Identity and health. GET /me resolves the current token's identity and works before you know a workspace id.
    GET /healthz is an unauthenticated health check.
- name: Workspaces
  description: The workspace your token can act in.
- name: Projects
  description: Create, read, update, and delete projects. A project is addressable by its slug or its id.
- name: Spaces
  description: Folders that group projects in a workspace.
- name: Contacts
  description: Your workspace contacts (vendors, crew, payees) and their documents.
- name: Tokens
  description: List, inspect, and revoke your API tokens. You create tokens in Saturation under Settings > Developers > API;
    these routes manage them after that.
- name: Library (workspace)
  description: 'The workspace Library, where you set up reusable budget building blocks: rate packs, incentive packs, fringes,
    globals, currencies, tags, and units. You enable packs here, then add items to a project.'
- name: Library (project)
  description: 'The Library items added to one project: rate packs, incentives, fringes, globals, and currencies. Each item
    remembers its source (`sourceId` for workspace copies, `sourceIncentiveVersionId` for Saturation incentive versions),
    and you can edit it without changing the original.'
- name: Budget
  description: Read a project budget and its computed totals (lines, phases, accounts, rollups, and variance) and edit budget
    lines and phases. A computed read can return 504 budget_compute_timeout or 413 budget_too_large.
- name: Transactions
  description: The unified financial record. Each transaction has a source, type, and status, with itemized lines, stats,
    and document lookups.
- name: Purchase Orders
  description: Purchase orders with a read-only status, current Activity, a financial Summary, linked records, suggested matches,
    and a Timeline.
- name: Payment Requests
  description: Requests to pay a person or company. A request stays separate from the payment that follows.
- name: Payments
  description: Payments from request through settlement, with linked records and a Timeline.
- name: Documents
  description: Upload a document once, then assign it to a transaction, budget line, purchase order, or contact. You can also
    read a document's content and manage its links.
- name: Search
  description: Search across transactions, documents, contacts, and budget lines, at workspace or project scope. You only
    see results you have permission to read, and results are paginated.
- name: Comments
  description: Comments on entities in a workspace.
- name: Webhooks
  description: Subscribe an endpoint to data-change events. Manage subscriptions, send a test delivery, and review delivery
    history. Each delivery is signed (X-Saturation-Signature) so you can verify it.
- name: Views
  description: Saved filters you can reuse. A view stores a filter you can apply by id, and you can add more filters on top
    of it.
paths:
  /projects/{projectId}/budget:
    get:
      operationId: budget_getDocument
      summary: Get the budget document (tree + phases + totals + editable phase data)
      description: Returns the budget document as a flat array of lines (rebuild the tree from `parentId`), visible phase
        columns, computed totals, and editable estimate phase data, all in one response. Use `path` or `accountCode` to narrow
        a large budget before the server line ceiling is applied. `accountId`, tag filters, date windows, hidden phases, and
        `expand` are rejected on this document read.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: path
        in: query
        required: false
        description: Materialized account path (for example `1100/1110`) naming exactly one root line and its descendants.
        schema:
          type: string
      - name: accountCode
        in: query
        required: false
        description: Leaf account code naming exactly one root line and its descendants. Duplicate codes return `409 account_code_ambiguous`.
        schema:
          type: string
      - name: phase
        in: query
        required: false
        description: Visible phase id, alias, name, or type. Ambiguous selectors return `400 validation`; hidden phases are
          excluded.
        schema:
          type: string
      - name: accountId
        in: query
        required: false
        deprecated: true
        description: Rejected on `GET /budget`; use `accountCode`.
        schema:
          type: string
      - name: tags
        in: query
        required: false
        deprecated: true
        description: Rejected on `GET /budget`; tag-filtered documents are deferred.
        schema:
          type: string
      - name: tagMode
        in: query
        required: false
        deprecated: true
        description: Rejected on `GET /budget`; tag-filtered documents are deferred.
        schema:
          $ref: '#/components/schemas/TagMode'
      - name: dateFrom
        in: query
        required: false
        deprecated: true
        description: Rejected on `GET /budget`; date-windowed documents are deferred.
        schema:
          type: string
      - name: dateTo
        in: query
        required: false
        deprecated: true
        description: Rejected on `GET /budget`; date-windowed documents are deferred.
        schema:
          type: string
      - name: includeHiddenPhases
        in: query
        required: false
        deprecated: true
        description: Rejected on `GET /budget`; hidden phases are intentionally excluded.
        schema:
          type: boolean
      - name: expand
        in: query
        required: false
        deprecated: true
        description: Rejected on budget document reads; the document is already fully shaped.
        schema:
          type: string
      - name: If-None-Match
        in: header
        required: false
        description: Conditional GET. Pass a prior `ETag`; an unchanged budget (for the same caller's permission projection)
          returns `304 Not Modified`. The permission gate is re-run before any `304`, so a foreign-tenant or stale ETag never
          short-circuits authorization.
        schema:
          type: string
      responses:
        '200':
          description: The budget document.
          headers:
            ETag:
              description: Opaque validator per (budget, permission-projection). Pass back as `If-None-Match`.
              schema:
                type: string
            Cache-Control:
              description: '`max-age=T`, the bounded-staleness window (the cache TTL).'
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetDocument'
        '304':
          description: Not modified, the ETag matched (permission re-checked first).
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '413':
          $ref: '#/components/responses/Error413'
        '429':
          $ref: '#/components/responses/Error429'
        '504':
          $ref: '#/components/responses/Error504'
  /projects/{projectId}/budget/totals:
    get:
      operationId: budget_getTotals
      summary: Get grand totals (optionally for a filtered slice)
      description: Returns the engine's grand totals as of `computedAt`. With supported filters (`phase`, `accountId`/`path`
        subtree) the response is the engine-coalesced slice total, computed server-side and never summed by the client. `path`
        matches only the materialized account path; `accountId` is a code classifier and can be ambiguous. `expand` is rejected.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: phase
        in: query
        required: false
        description: Narrow to one phase, by phase id or `type` (e.g. `estimate`).
        schema:
          type: string
        example: estimate
      - name: accountId
        in: query
        required: false
        description: Account code classifier, subtree-scopes the slice (a code may sit on many rows).
        schema:
          type: string
        example: '1100'
      - name: path
        in: query
        required: false
        description: Coded account path to subtree-scope the slice (e.g. `1100/1110`).
        schema:
          type: string
        example: 1100/1110
      - name: If-None-Match
        in: header
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Totals for the (optionally filtered) slice.
          headers:
            ETag:
              schema:
                type: string
            Cache-Control:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetTotals'
        '304':
          description: Not modified, the ETag matched (permission re-checked first).
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '413':
          $ref: '#/components/responses/Error413'
        '429':
          $ref: '#/components/responses/Error429'
        '504':
          $ref: '#/components/responses/Error504'
  /projects/{projectId}/budget/lines:
    get:
      operationId: budget_listLines
      summary: List budget lines (paginated, filterable)
      description: 'Browse/edit individual stored budget lines, paginated. Filters AND-compose. `accountId` is a classifier
        and returns a set (an account code can sit on many rows); `path` returns exactly one row (or `409 account_path_ambiguous`
        if a coded path matches more than one live row). For the complete budget document use `GET /budget`. Expand keys:
        `phaseTotals, phaseData, contact, account, source`; `phaseData` returns raw editable estimate phase data keyed by
        phase id. (The legacy `lines.notes` expand is not ported, the note is a plain-text `line.notes` field always present
        inline; see `BudgetLine.notes`.)'
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: accountId
        in: query
        required: false
        description: Account code classifier, returns a SET (a code may sit on many rows). Use `path`/`id` for an exact single-row
          read.
        schema:
          type: string
        example: '1100'
      - name: path
        in: query
        required: false
        description: Coded account path for an exact single-row read (e.g. `1100/1110`). A coded path matching more than one
          live row returns `409 account_path_ambiguous`.
        schema:
          type: string
        example: 1100/1110
      - name: tags
        in: query
        required: false
        description: Comma-separated tag ids/names; composed per `tagMode`.
        schema:
          type: string
        example: vfx
      - name: tagMode
        in: query
        required: false
        description: How `tags` composes (`any` = OR default, `all` = AND, `none` = exclude).
        schema:
          $ref: '#/components/schemas/TagMode'
      - name: kind
        in: query
        required: false
        description: Filter by the `kind` discriminator (comma-separated for multiple).
        schema:
          type: string
        example: line,account
      - $ref: '#/components/parameters/budgetLineExpand'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      responses:
        '200':
          description: A page of budget lines.
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/BudgetLine'
                  nextCursor:
                    $ref: '#/components/schemas/NextCursor'
                  count:
                    type: integer
                    description: Total matching lines; present only when `withCount=true`.
                  truncated:
                    type: boolean
                    description: True when a permission/expand under-fetch bounded the page short of `limit`.
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: budget_createLine
      summary: Create a budget line with optional phase data
      description: Creates a stored budget line under `parentId`, optionally with initial editable phase data keyed by phase
        id. Server mints `id`, the internal address reference, and timestamps, they cannot be client-supplied. Naming a server-owned
        field returns `422 field_read_only`. Emits a durable `budgetLine.created` audit event.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetLineCreate'
      responses:
        '201':
          description: The created budget line.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/budget/lines/bulk:
    post:
      operationId: budget_createLinesBulk
      summary: Create budget lines in one all-or-nothing bulk
      description: 'Creates multiple stored budget lines, each optionally carrying initial editable phase data keyed by phase
        id. The bulk is transactional: either every line and phase-data entry is written, or none are. A required `Idempotency-Key`
        provides exactly-once same-body retries.'
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetLineBulkCreate'
      responses:
        '201':
          description: The created budget lines in request order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLineBulkCreateResponse'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/budget/lines/phase-data/bulk:
    post:
      operationId: budget_upsertLinePhaseDataBulk
      summary: Upsert editable line phase data in one all-or-nothing bulk
      description: 'Upserts raw editable estimate phase data for existing lines and phases. The bulk is transactional: either
        every phase-data entry is written, or none are. A required `Idempotency-Key` provides exactly-once same-body retries.'
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetLinePhaseDataBulkUpsert'
      responses:
        '200':
          description: The normalized editable phase data after the upsert.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLinePhaseDataBulkUpsertResponse'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/budget/lines/{lineId}:
    get:
      operationId: budget_getLine
      summary: Get a budget line by id
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: lineId
        in: path
        required: true
        description: "Budget line identifier (`lin_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/budgetLineExpand'
      responses:
        '200':
          description: The budget line.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: budget_updateLine
      summary: Update a budget line
      description: Patches the provider-writable allow-list, optionally upserting editable phase data keyed by phase id. Naming
        a server-owned field (`address`, `budgetId`, `path`, `depth`, timestamps) returns `422 field_read_only` listing offenders,
        never a silent strip. Emits a durable `budgetLine.updated` audit event.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: lineId
        in: path
        required: true
        description: "Budget line identifier (`lin_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetLineUpdate'
      responses:
        '200':
          description: The updated budget line.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLine'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: budget_deleteLine
      summary: Soft-delete a budget line
      description: Soft-deletes the line (`soft-delete timestamp = now`). Re-creating an edited-then-deleted line resurrects
        the diverged copy as-is (edits preserved); pass `?reset=true` to re-snapshot instead. Emits a durable `budgetLine.deleted`
        audit event.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: lineId
        in: path
        required: true
        description: "Budget line identifier (`lin_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: reset
        in: query
        required: false
        description: When resurrecting on re-create, `true` re-snapshots from source instead of preserving edits.
        schema:
          type: boolean
          default: false
      responses:
        '204':
          description: Soft-deleted. No body.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/budget/lines/{lineId}/phase-data/{phaseId}:
    put:
      operationId: budget_upsertLinePhaseData
      summary: Upsert one editable line phase-data entry
      description: Upserts raw editable estimate phase data for one line/phase intersection. Omitted fields are left unchanged;
        explicit `null` clears nullable fields.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: lineId
        in: path
        required: true
        description: "Budget line identifier (`lin_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: phaseId
        in: path
        required: true
        description: "Budget phase identifier (`phs_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetLinePhaseDataUpsert'
      responses:
        '200':
          description: The normalized editable phase data after the upsert.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetLinePhaseDataUpsertResponse'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/budget/phases:
    get:
      operationId: budget_listPhases
      summary: List budget phases
      description: The phase columns of the budget (soft-deleted phases excluded).
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      responses:
        '200':
          description: The budget phases.
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/BudgetPhase'
                  nextCursor:
                    $ref: '#/components/schemas/NextCursor'
                  count:
                    type: integer
                    description: Total matching phases; present only when `withCount=true`.
                  truncated:
                    type: boolean
                    description: True when a permission/expand under-fetch bounded the page short of `limit`.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: budget_createPhase
      summary: Create a budget phase
      description: Adds a phase column. Emits a durable audit event.
      tags:
      - Budget
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      - $ref: '#/components/parameters/projectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - type
              - name
              additionalProperties: false
              properties:
                type:
                  $ref: '#/components/schemas/PhaseType'
                name:
                  type: string
                alias:
                  type: string
                color:
                  type: string
                isHidden:
                  type: boolean
                derivedFromPhaseId:
                  type: string
                  description: 'Create a SCENARIO: an estimate phase derived from an existing estimate phase in the same budget,
                    with that phase''s values copied server-side. Both the base and the new phase must be `type: estimate`.'
                copyValues:
                  type: boolean
                  description: Copy the base phase's values into the new phase. Only meaningful with `derivedFromPhaseId`;
                    defaults to `true`. Pass `false` for an empty derived phase that still records its lineage.
                rollupFormula:
                  type: string
                  maxLength: 2000
                  pattern: ^(?:@\{[\w-]+\}|@[\w-]+|[\d\s+\-*/().,%])*$
                  description: 'Rollup formula (only legal with `type: rollup`): phase references as `@{<phaseId>}` combined
                    with + and -, e.g. `@{phaseA} - @{phaseB}`. Without a formula a rollup column totals 0.'
            example:
              type: estimate
              name: Scenario B
              derivedFromPhaseId: ph_1a2b
      responses:
        '201':
          description: The created phase.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetPhase'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/budget/phases/{phaseId}:
    get:
      operationId: budget_getPhase
      summary: Get a budget phase by id
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: phaseId
        in: path
        required: true
        description: "Budget phase identifier (`phs_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The phase.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetPhase'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: budget_updatePhase
      summary: Update a budget phase
      description: Patches the writable phase fields. Naming a server-owned field returns `422 field_read_only`. Emits a durable
        audit event.
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: phaseId
        in: path
        required: true
        description: "Budget phase identifier (`phs_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              minProperties: 1
              properties:
                name:
                  type: string
                alias:
                  type: string
                color:
                  type:
                  - string
                  - 'null'
                isHidden:
                  type: boolean
                rollupFormula:
                  type:
                  - string
                  - 'null'
                  maxLength: 2000
                  pattern: ^(?:@\{[\w-]+\}|@[\w-]+|[\d\s+\-*/().,%])*$
                  description: 'Rollup formula (only legal on a `type: rollup` phase): `@{<phaseId>}` references combined
                    with + and -. Null clears it. Setting it on a non-rollup phase returns `400 validation`.'
      responses:
        '200':
          description: The updated phase.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetPhase'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: budget_deletePhase
      summary: 'Soft-delete a budget phase (CAUTION: deleting an actual- or rollup-type phase removes the actual or totals
        column - reorganizing estimate columns never requires it)'
      description: 'Soft-deletes the phase column (`soft-delete timestamp = now`). CAUTION: deleting an actual- or rollup-type
        phase removes that column from the budget. Reorganizing or renaming estimate columns never requires deleting actual
        or rollup phases. Emits a durable audit event.'
      tags:
      - Budget
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: phaseId
        in: path
        required: true
        description: "Budget phase identifier (`phs_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Soft-deleted. No body.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /documents:
    post:
      operationId: documents_drop
      tags:
      - Documents
      summary: Drop a document (optionally assign atomically)
      description: Upload a document once. Optionally link it to one or more typed targets in the same atomic call via `metadata.links`.
        If any target fails validation, permission, or the cross-workspace check, the request fails and creates no document
        row or orphan blob. Multiple targets must use distinct kinds. The link permission gate is `update` on each target
        subject. This is a billable create. It accepts an optional `Idempotency-Key` header for safe retries.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        description: Optional client-generated key for safe retries of this billable drop. Replaying the same key with a different
          body returns `409 idempotency_conflict`.
        schema:
          type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DocumentCreateRequest'
            encoding:
              metadata:
                contentType: application/json
      responses:
        '201':
          description: The created document, including any links made atomically with the drop.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    get:
      operationId: documents_list
      tags:
      - Documents
      summary: List / filter documents
      description: List documents in the workspace, paginated (cap 100). Filter by folder, assignment target, unassigned-only,
        project, status, classification, coarse type, or a name/description search.
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/documentExpand'
      - $ref: '#/components/parameters/withCount'
      - $ref: '#/components/parameters/view'
      - name: folder
        in: query
        required: false
        description: "Restrict to documents in this folder (`fld_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: linkedTo
        in: query
        required: false
        description: Restrict to documents assigned to a specific target, expressed as `kind:id` (e.g. `transaction:txn_8f2a1c9e`).
        schema:
          type: string
          pattern: ^(transaction|purchaseOrder|contact|project):.+$
        examples:
          byTransaction:
            value: transaction:txn_8f2a1c9e
      - name: unassigned
        in: query
        required: false
        description: When `true`, restrict to documents with no entity assignment (no reference set).
        schema:
          type: boolean
      - name: project
        in: query
        required: false
        description: "Restrict to documents scoped to this project (`prj_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: status
        in: query
        required: false
        description: Restrict to documents in this processing status.
        schema:
          $ref: '#/components/schemas/DocumentStatus'
      - name: classification
        in: query
        required: false
        description: Restrict to documents with this fine-grained classification.
        schema:
          type: string
      - name: coarseType
        in: query
        required: false
        description: Restrict to documents of this coarse content category.
        schema:
          $ref: '#/components/schemas/DocumentCoarseType'
      - name: q
        in: query
        required: false
        description: Search over document name / description.
        schema:
          type: string
      responses:
        '200':
          description: A paginated page of documents.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentCollection'
              examples:
                page:
                  value:
                    data:
                    - id: doc_4b1f0a7c
                      name: vendor-invoice-0425.pdf
                      mimeType: application/pdf
                      size: 184320
                      status: READY
                      projectId: prj_71aa
                      links:
                      - kind: transaction
                        id: txn_8f2a1c9e
                      createdAt: '2026-05-28T14:03:11Z'
                      updatedAt: '2026-05-28T14:03:40Z'
                    nextCursor: eyJpZCI6ImRvY180YjFmMGE3YyJ9
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /documents/{documentId}:
    get:
      operationId: documents_get
      tags:
      - Documents
      summary: Get document metadata
      parameters:
      - name: documentId
        in: path
        required: true
        description: "Document id (`doc_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/documentExpand'
      responses:
        '200':
          description: The document.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: documents_update
      tags:
      - Documents
      summary: Rename / move / re-describe a document
      description: Update client-writable fields only (`name`, `description`, `folderId`). Links are managed through `/documents/{documentId}/links/{kind}`.
        Naming a server-owned field returns `422 field_read_only`.
      parameters:
      - name: documentId
        in: path
        required: true
        description: "Document id (`doc_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentUpdateRequest'
            examples:
              rename:
                value:
                  name: vendor-invoice-april.pdf
      responses:
        '200':
          description: The updated document.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: documents_delete
      tags:
      - Documents
      summary: Delete a document
      description: Soft-delete the document. Idempotent, safe to retry.
      parameters:
      - name: documentId
        in: path
        required: true
        description: "Document id (`doc_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted. No body.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /documents/{documentId}/content:
    get:
      operationId: documents_getContent
      tags:
      - Documents
      summary: Download document bytes
      description: Stream the stored document blob. The response `Content-Type` is the document's `mimeType`.
      parameters:
      - name: documentId
        in: path
        required: true
        description: "Document id (`doc_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The raw document bytes.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /documents/{documentId}/links/{kind}:
    parameters:
    - name: documentId
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/Id'
    - name: kind
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/DocumentTargetKind'
    put:
      operationId: documents_putLink
      tags:
      - Documents
      summary: Link a document to a typed target
      description: Links the document to one target of this kind. The same target is an idempotent no-op. A different target
        conflicts unless replace is true.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentLinkRequest'
      responses:
        '200':
          description: The document with its updated links.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: documents_deleteLink
      tags:
      - Documents
      summary: Remove a document link
      description: Removes the current link for this kind. The operation is idempotent when no link of that kind exists.
      responses:
        '200':
          description: The document with its updated links.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /documents/{documentId}/extraction:
    get:
      operationId: documents_getExtraction
      tags:
      - Documents
      summary: Get a document's structured extraction
      description: The structured fields the platform extracted from the document, the replacement for the legacy per-attachment
        parsed-document read. Returns the classification, a flat map of extracted typed fields (e.g. invoice number, vendor,
        total, dates), and any line items, each with a confidence score so a consumer can threshold low-confidence values.
        Available only once `status = READY`; while the document is still `PENDING`/`PROCESSING` this returns `404 not_found`
        (extraction not ready yet, re-fetch the document and retry when its `status` is `READY`), and a `FAILED` document
        has no extraction. Read-only, extraction is set by the platform, never by the client.
      parameters:
      - name: documentId
        in: path
        required: true
        description: "Document id (`doc_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The document's structured extraction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentExtraction'
              examples:
                invoice:
                  value:
                    documentId: doc_4b1f0a7c
                    status: READY
                    classification: invoice
                    coarseType: financial
                    extractedAt: '2026-05-28T14:03:40Z'
                    fields:
                      invoiceNumber:
                        value: '4471'
                        confidence: 0.98
                      vendorName:
                        value: B&H Photo
                        confidence: 0.95
                      total:
                        value: '1529.00'
                        confidence: 0.91
                      invoiceDate:
                        value: '2026-05-20'
                        confidence: 0.88
                    lineItems:
                    - description: Camera body rental - 3 days
                      amount:
                        amount: 75000
                        currency: USD
                      confidence: 0.86
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/rate-packs:
    get:
      operationId: library_listRatePacks
      summary: List rate packs visible to the workspace
      description: "Rate packs visible to the workspace: `PUBLIC` \u222A workspace-owned, live. Each carries `enabled`/`isOwned`/`latestVersion`.\
        \ Enabled/installed deprecated packs are included and flagged `deprecated: true`."
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/withCount'
      - $ref: '#/components/parameters/ratePackExpand'
      - name: q
        in: query
        required: false
        description: Free-text search over pack name / publisher / tags.
        schema:
          type: string
      - name: category
        in: query
        required: false
        schema:
          type: string
      - name: visibility
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/PackVisibility'
      - name: deprecated
        in: query
        required: false
        description: Filter by deprecation state.
        schema:
          type: boolean
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of rate packs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePackCollection'
              example:
                data:
                - id: rpk_2a9f
                  slug: iatse-local-871
                  name: IATSE Local 871
                  category: union
                  tags:
                  - union
                  - scripted
                  visibility: PUBLIC
                  isOwned: false
                  deprecated: false
                  enabled: true
                  latestVersion:
                    id: rpv_1
                    effectiveDate: '2026-01-01'
                    itemCount: 142
                nextCursor: eyJpZCI6InJwa18yYTlmIn0
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createRatePack
      summary: Author a PRIVATE workspace-owned rate pack
      description: Creates a PRIVATE pack and auto-enables it for the workspace. Returns the created pack.
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RatePackCreate'
            example:
              name: My Crew Rates
              category: custom
              tags:
              - internal
      responses:
        '201':
          description: The created rate pack.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePack'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/rate-packs/{packId}:
    get:
      operationId: library_getRatePack
      summary: Get a rate pack
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/ratePackExpand'
      responses:
        '200':
          description: The rate pack.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePack'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateRatePack
      summary: Update an owned rate pack
      description: Patches an owned pack. A non-owned pack returns `404 not_found` (no existence leak).
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RatePackUpdate'
            example:
              name: My Crew Rates 2026
      responses:
        '200':
          description: The updated rate pack.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePack'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteRatePack
      summary: Soft-delete an owned rate pack
      description: Soft-deletes the pack (`soft-delete timestamp`), preserving lineage. Idempotent.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/rate-packs/{packId}/enablement:
    post:
      operationId: library_enableRatePack
      summary: Enable a rate pack for the workspace
      description: Idempotent upsert of the workspace-enable link (deterministic id, resurrects a soft-deleted link). Enabling
        a deprecated pack returns `400 validation`; a PRIVATE pack owned by another workspace returns `404 not_found`.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The enable link.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePackEnableLink'
              example:
                id: wrp_5
                ratePackId: rpk_2a9f
                enabledAt: '2026-05-28T10:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_disableRatePack
      summary: Disable a rate pack for the workspace
      description: Soft-deletes the enable link. Idempotent.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Disabled.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/rate-packs/{packId}/items:
    get:
      operationId: library_listRatePackItems
      summary: Search/browse a pack's current-version items
      description: Latest-version, non-deleted items. Access requires the pack to be enabled, owned, or PUBLIC. Paginated
        (cap 100).
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - name: q
        in: query
        required: false
        description: Free-text search over item title / aliases.
        schema:
          type: string
      - name: group
        in: query
        required: false
        schema:
          type: string
      - name: agreement
        in: query
        required: false
        schema:
          type: string
      - name: local
        in: query
        required: false
        schema:
          type: string
      - name: effectiveDate
        in: query
        required: false
        schema:
          type: string
          format: date
      responses:
        '200':
          description: A page of pack items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePackItemCollection'
              example:
                data:
                - id: rpi_9c
                  title: Costume Designer
                  rate:
                    amount: 6500
                    currency: USD
                  unit: Day
                  currency: USD
                  group: Wardrobe
                  local: '871'
                  labels: []
                nextCursor: null
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: library_createRatePackItem
      summary: Add an item to an owned pack
      description: Adds an item (auto-creates v1 if the pack has no version). Server recomputes itemCount/searchText.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RatePackItemCreate'
            example:
              title: Key Grip
              rate:
                amount: 5800
                currency: USD
              unit: Day
              group: Grip
      responses:
        '201':
          description: The created item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePackItem'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '409':
          $ref: '#/components/responses/Error409'
  /library/rate-packs/{packId}/items/{itemId}:
    patch:
      operationId: library_updateRatePackItem
      summary: Update a pack item
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - name: itemId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RatePackItemUpdate'
            example:
              rate:
                amount: 6000
                currency: USD
      responses:
        '200':
          description: The updated item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatePackItem'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteRatePackItem
      summary: Soft-delete a pack item
      description: Soft-deletes the item; server recomputes itemCount. Idempotent.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - name: itemId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/incentive-packs:
    get:
      operationId: library_listIncentivePacks
      summary: List incentive packs visible to the workspace
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - $ref: '#/components/parameters/incentivePackExpand'
      - name: category
        in: query
        required: false
        schema:
          type: string
      - name: jurisdiction
        in: query
        required: false
        schema:
          type: string
      - name: deprecated
        in: query
        required: false
        schema:
          type: boolean
      - name: enabled
        in: query
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: A page of incentive packs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncentivePackCollection'
              example:
                data:
                - id: ipk_ga
                  slug: georgia-film-credit
                  name: Georgia Film Tax Credit
                  jurisdiction: US-GA
                  visibility: PUBLIC
                  isOwned: false
                  deprecated: false
                  enabled: true
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
  /library/incentive-packs/{packId}:
    get:
      operationId: library_getIncentivePack
      summary: Get an incentive pack
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/incentivePackExpand'
      responses:
        '200':
          description: The incentive pack.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncentivePack'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/incentive-packs/{packId}/programs:
    get:
      operationId: library_listIncentivePrograms
      summary: List an incentive pack's programs (rules)
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - name: jurisdiction
        in: query
        required: false
        schema:
          type: string
      - name: incentiveType
        in: query
        required: false
        schema:
          type: string
      - name: incentiveStructure
        in: query
        required: false
        schema:
          type: string
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - draft
          - published
          - archived
      responses:
        '200':
          description: A page of incentive programs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncentiveProgramCollection'
              example:
                data:
                - id: ipr_base
                  name: Base Transferable Credit
                  incentiveType: tax-credit
                  incentiveStructure: transferable
                  status: published
                  currentVersionId: ipv_2026
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/incentive-packs/{packId}/enablement:
    post:
      operationId: library_enableIncentivePack
      summary: Enable an incentive pack for the workspace
      description: Idempotent upsert or resurrection of the workspace enablement. Enabling a deprecated pack returns `400
        validation`; a private pack owned by another workspace returns `404 not_found`. Incentive programs are added through
        `POST /projects/{projectId}/library/incentives`.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The enable link.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncentivePackEnableLink'
              example:
                id: wip_3
                incentivePackId: ipk_ga
                enabledAt: '2026-05-28T10:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_disableIncentivePack
      summary: Disable an incentive pack for the workspace
      description: Soft-deletes the enable link. Idempotent.
      tags:
      - Library (workspace)
      parameters:
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Disabled.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/fringes:
    get:
      operationId: library_listFringes
      summary: List workspace fringe templates
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of fringe templates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FringeCollection'
              example:
                data:
                - id: frg_pr
                  name: Payroll Tax
                  rate: 0.0765
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createFringe
      summary: Create a workspace fringe template
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FringeWrite'
            example:
              name: Payroll Tax
              rate: 0.0765
      responses:
        '201':
          description: The created template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fringe'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/fringes/{fringeId}:
    get:
      operationId: library_getFringe
      summary: Get a workspace fringe template
      tags:
      - Library (workspace)
      parameters:
      - name: fringeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The fringe template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fringe'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateFringe
      summary: Update a workspace fringe template
      tags:
      - Library (workspace)
      parameters:
      - name: fringeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FringeWrite'
            example:
              rate: 0.08
      responses:
        '200':
          description: The updated template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fringe'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteFringe
      summary: Soft-delete a workspace fringe template
      description: Soft-deletes the template. Project copies keep their `sourceId`; `expand=source` then resolves to a tombstone
        (never null).
      tags:
      - Library (workspace)
      parameters:
      - name: fringeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/globals:
    get:
      operationId: library_listGlobals
      summary: List workspace global templates
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of global templates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalCollection'
              example:
                data:
                - id: gbl_days
                  name: Shoot Days
                  value: 35
                  unit: Day
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createGlobal
      summary: Create a workspace global template
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalWrite'
            example:
              name: Shoot Days
              value: 35
              unit: Day
      responses:
        '201':
          description: The created template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Global'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/globals/{globalId}:
    get:
      operationId: library_getGlobal
      summary: Get a workspace global template
      tags:
      - Library (workspace)
      parameters:
      - name: globalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The global template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Global'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateGlobal
      summary: Update a workspace global template
      tags:
      - Library (workspace)
      parameters:
      - name: globalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalWrite'
            example:
              value: 40
      responses:
        '200':
          description: The updated template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Global'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteGlobal
      summary: Soft-delete a workspace global template
      tags:
      - Library (workspace)
      parameters:
      - name: globalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/currencies:
    get:
      operationId: library_listCurrencys
      summary: List workspace currency templates
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of currency templates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrencyCollection'
              example:
                data:
                - id: cur_gbp
                  code: GBP
                  name: British Pound
                  rate: 1.27
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createCurrency
      summary: Create a workspace currency template
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CurrencyWrite'
            example:
              code: GBP
              name: British Pound
              rate: 1.27
      responses:
        '201':
          description: The created template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Currency'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/currencies/{currencyId}:
    get:
      operationId: library_getCurrency
      summary: Get a workspace currency template
      tags:
      - Library (workspace)
      parameters:
      - name: currencyId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The currency template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Currency'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateCurrency
      summary: Update a workspace currency template
      tags:
      - Library (workspace)
      parameters:
      - name: currencyId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CurrencyWrite'
            example:
              rate: 1.31
      responses:
        '200':
          description: The updated template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Currency'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteCurrency
      summary: Soft-delete a workspace currency template
      tags:
      - Library (workspace)
      parameters:
      - name: currencyId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/fringe-groups:
    get:
      operationId: library_listFringeGroups
      summary: List workspace fringe-tag templates
      description: Fringe groups.
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of fringe-tag templates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FringeGroupCollection'
              example:
                data:
                - id: frt_union
                  name: Union
                  color: blue
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createFringeGroup
      summary: Create a workspace fringe-tag template
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FringeGroupWrite'
            example:
              name: Union
              color: blue
      responses:
        '201':
          description: The created template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FringeGroup'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/fringe-groups/{fringeGroupId}:
    get:
      operationId: library_getFringeGroup
      summary: Get a workspace fringe-tag template
      tags:
      - Library (workspace)
      parameters:
      - name: fringeGroupId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The fringe-tag template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FringeGroup'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateFringeGroup
      summary: Update a workspace fringe-tag template
      tags:
      - Library (workspace)
      parameters:
      - name: fringeGroupId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FringeGroupWrite'
            example:
              color: green
      responses:
        '200':
          description: The updated template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FringeGroup'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteFringeGroup
      summary: Soft-delete a workspace fringe-tag template
      tags:
      - Library (workspace)
      parameters:
      - name: fringeGroupId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/tags:
    get:
      operationId: library_listTags
      summary: List the workspace tag pool
      tags:
      - Library (workspace)
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: eligibilityKey
        in: query
        required: false
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of tags.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagCollection'
              example:
                data:
                - id: tag_atl
                  name: Atlanta
                  color: orange
                  createdAt: '2026-01-02T08:00:00Z'
                  updatedAt: '2026-01-02T08:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createTag
      summary: Create a workspace tag
      description: Idempotent on a supplied `id`. `color` must be a Tailwind key or null (else `422`).
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagCreate'
            example:
              name: Atlanta
              color: orange
      responses:
        '201':
          description: The created tag.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/tags/{tagId}:
    get:
      operationId: library_getTag
      summary: Get a workspace tag
      tags:
      - Library (workspace)
      parameters:
      - name: tagId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The tag.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateTag
      summary: Update a workspace tag
      tags:
      - Library (workspace)
      parameters:
      - name: tagId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagUpdate'
            example:
              color: green
      responses:
        '200':
          description: The updated tag.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteTag
      summary: Soft-delete a workspace tag
      description: Soft-deletes the tag; historical assignments stay resolvable. Idempotent.
      tags:
      - Library (workspace)
      parameters:
      - name: tagId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /library/units:
    get:
      operationId: library_listUnits
      summary: List units
      description: The 17 read-only built-in units merged with the workspace's live custom units.
      tags:
      - Library (workspace)
      parameters:
      - name: q
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: All units.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitCollection'
              example:
                data:
                - id: Day
                  label: Day
                  isCustom: false
                - id: cun_box
                  label: Box Rental
                  isCustom: true
                  createdAt: '2026-03-01T08:00:00Z'
                  updatedAt: '2026-03-01T08:00:00Z'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: library_createUnit
      summary: Create a unit
      description: Built-in units cannot be changed. Custom units can be created, updated, and deleted.
      tags:
      - Library (workspace)
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnitCreate'
            example:
              label: Box Rental
      responses:
        '201':
          description: The created unit.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unit'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
  /library/units/{unitId}:
    get:
      operationId: library_getUnit
      summary: Get a unit
      description: Built-in units are readable and immutable. Custom units use the same shape and can be updated or deleted.
      tags:
      - Library (workspace)
      parameters:
      - name: unitId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: The unit.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unit'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
    patch:
      operationId: library_updateUnit
      summary: Update a unit
      tags:
      - Library (workspace)
      parameters:
      - name: unitId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnitUpdate'
            example:
              label: Box Rental (weekly)
      responses:
        '200':
          description: The updated unit.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unit'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteUnit
      summary: Delete a unit
      tags:
      - Library (workspace)
      parameters:
      - name: unitId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/rate-packs:
    get:
      operationId: library_listProjectRatePacks
      summary: List rate packs installed in the project
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/projectRatePackExpand'
      - name: installed
        in: query
        required: false
        description: Filter to installed (true) packs.
        schema:
          type: boolean
      responses:
        '200':
          description: A page of installed rate packs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectRatePackCollection'
              example:
                data:
                - id: prp_1
                  ratePackId: rpk_2a9f
                  installedAt: '2026-05-01T08:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/rate-packs/{packId}:
    put:
      operationId: library_addRatePack
      summary: Add a rate pack into the project
      description: "Two-step ENABLE\u2192ADD: requires a live workspace-enable link, else `400 validation` (`ERR_RATE_PACK_NOT_ENABLED_FOR_WORKSPACE`).\
        \ Rejects deprecated (`400`); missing project/pack (`404`). Idempotent upsert/resurrect (deterministic id)."
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The rate pack added to the project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectRatePack'
              example:
                id: prp_1
                ratePackId: rpk_2a9f
                installedAt: '2026-05-28T10:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_removeRatePack
      summary: Remove a rate pack from the project
      description: Soft-deletes the project rate pack link. Idempotent.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: packId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Removed.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/incentives:
    get:
      operationId: library_listProjectIncentives
      summary: List the project's incentives
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/projectIncentiveExpand'
      - name: isApplied
        in: query
        required: false
        schema:
          type: boolean
      - name: jurisdiction
        in: query
        required: false
        schema:
          type: string
      - name: incentiveType
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: A page of project incentives.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIncentiveCollection'
              example:
                data:
                - id: inc_ga1
                  name: Georgia Base Credit
                  sourceType: saturation
                  sourceIncentiveVersionId: ipv_2026
                  isApplied: true
                  amount:
                    amount: 1250000
                    currency: USD
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: library_addProjectIncentive
      summary: Add an incentive program into the project
      description: Adds a program (its current published version) as a project incentive. Keyed on `(projectId, sourceIncentiveVersionId)`;
        idempotent. Rejected (`400 validation`) when the version/program/pack is not `published`, the pack is `deprecated`,
        or the pack is neither workspace-enabled nor workspace-owned.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectIncentiveAdd'
            example:
              programId: ipr_base
      responses:
        '200':
          description: The project incentive (existing, when idempotent re-add).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIncentive'
        '201':
          description: The newly created project incentive.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIncentive'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/incentives/{incentiveId}:
    get:
      operationId: library_getProjectIncentive
      summary: Get a project incentive
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: incentiveId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/projectIncentiveExpand'
      responses:
        '200':
          description: The project incentive.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIncentive'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateProjectIncentive
      summary: Update a project incentive
      description: The source-tracking fields (`sourceId`, `sourceIncentiveVersionId`, `sourceType`) are read-only.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: incentiveId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectIncentiveUpdate'
            example:
              isApplied: false
      responses:
        '200':
          description: The updated project incentive.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectIncentive'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteProjectIncentive
      summary: Remove a project incentive
      description: Soft-deletes the project incentive. Idempotent. Creating the same project incentive again resurrects it.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: incentiveId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Removed.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/fringes:
    get:
      operationId: library_listProjectFringes
      summary: List project fringe copies
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/libraryExpand'
      - name: sourceId
        in: query
        required: false
        description: Filter to copies of a specific workspace source.
        schema:
          $ref: '#/components/schemas/Id'
      - name: diverged
        in: query
        required: false
        description: Filter to copies whose values differ from their source.
        schema:
          type: boolean
      responses:
        '200':
          description: A page of project fringe copies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringeCollection'
              example:
                data:
                - id: frg_copy1
                  name: Payroll Tax
                  rate: 0.08
                  sourceType: workspace
                  sourceId: frg_pr
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: library_addProjectFringe
      summary: Copy a workspace fringe into the project
      description: "Copies a workspace template in (idempotent on `(projectId, sourceId)`). A live copy \u2192 `200` no-op;\
        \ an edited-then-soft-deleted copy \u2192 resurrect **as-is** (edits preserved); `?reset=true` \u2192 explicit re-snapshot\
        \ from source."
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: reset
        in: query
        required: false
        description: When true, re-snapshot all fields from source (overwrites in-project edits).
        schema:
          type: boolean
          default: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - sourceId
              properties:
                sourceId:
                  $ref: '#/components/schemas/Id'
            example:
              sourceId: frg_pr
      responses:
        '200':
          description: The project copy (existing live or resurrected).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringe'
        '201':
          description: The newly created project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringe'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/fringes/{fringeId}:
    get:
      operationId: library_getProjectFringe
      summary: Get a project fringe copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: fringeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/libraryExpand'
      responses:
        '200':
          description: The project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringe'
              example:
                id: frg_copy1
                name: Payroll Tax
                rate: 0.08
                sourceType: workspace
                sourceId: frg_pr
                source:
                  id: frg_pr
                  name: Payroll Tax
                  rate: 0.0765
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateProjectFringe
      summary: Update a project fringe copy
      description: Diverges the copy from its source; `sourceId`/`sourceType` are read-only.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: fringeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FringeWrite'
            example:
              rate: 0.085
      responses:
        '200':
          description: The updated project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringe'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteProjectFringe
      summary: Remove a project fringe copy
      description: Soft-deletes the copy. Re-`add` resurrects it as-is unless `?reset=true`. Idempotent.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: fringeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Removed.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/globals:
    get:
      operationId: library_listProjectGlobals
      summary: List project global copies
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/libraryExpand'
      - name: sourceId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Id'
      - name: diverged
        in: query
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: A page of project global copies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectGlobalCollection'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: library_addProjectGlobal
      summary: Copy a workspace global into the project
      description: "Idempotent on `(projectId, sourceId)`. Live copy \u2192 `200`; soft-deleted edited copy \u2192 resurrect\
        \ as-is; `?reset=true` \u2192 re-snapshot from source."
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: reset
        in: query
        required: false
        schema:
          type: boolean
          default: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - sourceId
              properties:
                sourceId:
                  $ref: '#/components/schemas/Id'
            example:
              sourceId: gbl_days
      responses:
        '200':
          description: The project copy (existing live or resurrected).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectGlobal'
        '201':
          description: The newly created project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectGlobal'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/globals/{globalId}:
    get:
      operationId: library_getProjectGlobal
      summary: Get a project global copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: globalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/libraryExpand'
      responses:
        '200':
          description: The project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectGlobal'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateProjectGlobal
      summary: Update a project global copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: globalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalWrite'
            example:
              value: 38
      responses:
        '200':
          description: The updated project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectGlobal'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteProjectGlobal
      summary: Remove a project global copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: globalId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Removed.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/currencies:
    get:
      operationId: library_listProjectCurrencies
      summary: List project currency copies
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/libraryExpand'
      - name: sourceId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Id'
      - name: diverged
        in: query
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: A page of project currency copies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectCurrencyCollection'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: library_addProjectCurrency
      summary: Copy a workspace currency into the project
      description: "Idempotent on `(projectId, sourceId)`. Live copy \u2192 `200`; soft-deleted edited copy \u2192 resurrect\
        \ as-is; `?reset=true` \u2192 re-snapshot from source."
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: reset
        in: query
        required: false
        schema:
          type: boolean
          default: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - sourceId
              properties:
                sourceId:
                  $ref: '#/components/schemas/Id'
            example:
              sourceId: cur_gbp
      responses:
        '200':
          description: The project copy (existing live or resurrected).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectCurrency'
        '201':
          description: The newly created project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectCurrency'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/currencies/{currencyId}:
    get:
      operationId: library_getProjectCurrency
      summary: Get a project currency copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: currencyId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/libraryExpand'
      responses:
        '200':
          description: The project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectCurrency'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateProjectCurrency
      summary: Update a project currency copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: currencyId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CurrencyWrite'
            example:
              rate: 1.33
      responses:
        '200':
          description: The updated project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectCurrency'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteProjectCurrency
      summary: Remove a project currency copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: currencyId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Removed.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/fringe-groups:
    get:
      operationId: library_listProjectFringeGroups
      summary: List project fringe-tag copies
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/libraryExpand'
      - name: sourceId
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Id'
      - name: diverged
        in: query
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: A page of project fringe-tag copies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringeGroupCollection'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: library_addProjectFringeGroup
      summary: Copy a workspace fringe-tag into the project
      description: Idempotent on `(projectId, sourceId)`. Resurrect as-is; `?reset=true` re-snapshots.
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: reset
        in: query
        required: false
        schema:
          type: boolean
          default: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - sourceId
              properties:
                sourceId:
                  $ref: '#/components/schemas/Id'
            example:
              sourceId: frt_union
      responses:
        '200':
          description: The project copy (existing live or resurrected).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringeGroup'
        '201':
          description: The newly created project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringeGroup'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/fringe-groups/{fringeGroupId}:
    get:
      operationId: library_getProjectFringeGroup
      summary: Get a project fringe-tag copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: fringeGroupId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/libraryExpand'
      responses:
        '200':
          description: The project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringeGroup'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: library_updateProjectFringeGroup
      summary: Update a project fringe-tag copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: fringeGroupId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FringeGroupWrite'
            example:
              color: red
      responses:
        '200':
          description: The updated project copy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectFringeGroup'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: library_deleteProjectFringeGroup
      summary: Remove a project fringe-tag copy
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - name: fringeGroupId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Removed.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/library/tags:
    get:
      operationId: library_listProjectTags
      summary: List tags associated with the project
      description: Tags with an active `is linked to` edge to the project (definitions, not assignments).
      tags:
      - Library (project)
      parameters:
      - $ref: '#/components/parameters/projectId'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - name: q
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: A page of associated tags.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagCollection'
              example:
                data:
                - id: tag_atl
                  name: Atlanta
                  color: orange
                  createdAt: '2026-01-02T08:00:00Z'
                  updatedAt: '2026-01-02T08:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects:
    get:
      operationId: master-data_listProjects
      tags:
      - Projects
      summary: List projects
      description: 'List the workspace''s projects, permission-filtered to those the token can read. Paginated (cap-100).
        Soft-deleted projects are hidden unless `includeDeleted=true`. Legacy parity note: the project filter set is intentionally
        narrower than legacy. The high-value facets are kept (`status`, `spaceId`, `q`); the legacy `labels[]` project-label
        filter is NOT ported, Next has no project-label primitive (projects are grouped by `spaceId`, and per-entity tagging
        is the Library tag pool, not a project label). Deep, saved filters belong to a `view` (), not the query string.'
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/withCount'
      - name: status
        in: query
        required: false
        description: Filter by status. Separate multiple values with commas (for example, `active,archived`).
        schema:
          type: string
          example: active
      - name: spaceId
        in: query
        required: false
        description: Only projects filed under this space.
        schema:
          $ref: '#/components/schemas/Id'
      - name: q
        in: query
        required: false
        description: Free-text search over project name / slug.
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        description: Include soft-deleted projects (each carries a non-null `soft-delete timestamp`).
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of projects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectList'
              example:
                data:
                - id: prj_8f2a1c9e
                  slug: q3-feature-doc
                  name: Q3 Feature Doc
                  emoji: "\U0001F3AC"
                  image: null
                  status: active
                  summary: A short documentary on Q3 features.
                  spaceId: spc_1a2b3c
                  createdAt: '2026-05-01T12:00:00Z'
                  updatedAt: '2026-05-20T09:30:00Z'
                nextCursor: eyJpZCI6InByal84ZjJhMWM5ZSJ9
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: master-data_createProject
      tags:
      - Projects
      summary: Create a project
      description: Create a project in the workspace. Requires `create` on the `Project` subject (request-derived). Server-owned
        fields are rejected with `422 field_read_only`.
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectCreate'
            example:
              name: Q3 Feature Doc
              emoji: "\U0001F3AC"
              spaceId: spc_1a2b3c
      responses:
        '201':
          description: The created project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
              example:
                id: prj_8f2a1c9e
                slug: q3-feature-doc
                name: Q3 Feature Doc
                emoji: "\U0001F3AC"
                image: null
                status: active
                summary: null
                spaceId: spc_1a2b3c
                createdAt: '2026-05-29T10:00:00Z'
                updatedAt: '2026-05-29T10:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
  /projects/{project}:
    parameters:
    - name: project
      in: path
      required: true
      description: "Project canonical id (`prj_\u2026`) or slug."
      schema:
        type: string
    get:
      operationId: master-data_getProject
      tags:
      - Projects
      summary: Get a project
      description: Fetch one project by canonical id or slug. `404 not_found` also covers exists-but-unauthorized.
      responses:
        '200':
          description: The project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
              example:
                id: prj_8f2a1c9e
                slug: q3-feature-doc
                name: Q3 Feature Doc
                emoji: "\U0001F3AC"
                image: null
                status: active
                summary: A short documentary on Q3 features.
                spaceId: spc_1a2b3c
                createdAt: '2026-05-01T12:00:00Z'
                updatedAt: '2026-05-20T09:30:00Z'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: master-data_updateProject
      tags:
      - Projects
      summary: Update a project
      description: Partially update a project. Gated on `update` against the row-derived `Project` subject (`{kind:'Project',
        workspaceId, projectId}`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdate'
            example:
              name: Q3 Feature Doc (final)
              status: archived
      responses:
        '200':
          description: The updated project.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /spaces:
    get:
      operationId: master-data_listSpaces
      tags:
      - Spaces
      summary: List spaces
      description: List the workspace's spaces. Paginated.
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/withCount'
      - name: parentId
        in: query
        required: false
        description: Only spaces directly nested under this parent space.
        schema:
          $ref: '#/components/schemas/Id'
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of spaces.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpaceList'
              example:
                data:
                - id: spc_1a2b3c
                  slug: features
                  name: Features
                  image: null
                  parentId: null
                  archived: false
                  createdAt: '2026-04-01T12:00:00Z'
                  updatedAt: '2026-04-01T12:00:00Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: master-data_createSpace
      tags:
      - Spaces
      summary: Create a space
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpaceCreate'
            example:
              name: Features
      responses:
        '201':
          description: The created space.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Space'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
  /spaces/{spaceId}:
    parameters:
    - name: spaceId
      in: path
      required: true
      description: "Space canonical id (`spc_\u2026`)."
      schema:
        $ref: '#/components/schemas/Id'
    patch:
      operationId: master-data_updateSpace
      tags:
      - Spaces
      summary: Update a space
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpaceUpdate'
            example:
              name: Feature Docs
              archived: true
      responses:
        '200':
          description: The updated space.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Space'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: master-data_deleteSpace
      tags:
      - Spaces
      summary: Delete a space
      description: Soft-delete a space. Projects filed under it are not deleted; they become ungrouped (`spaceId` cleared).
        Idempotent.
      responses:
        '204':
          description: Deleted (no body).
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /contacts:
    get:
      operationId: master-data_listContacts
      tags:
      - Contacts
      summary: List contacts
      description: 'List the workspace''s contacts. Paginated. Supports `expand=documents,transactions` (permission-projected,
        an empty/partial expanded array means "not-readable-or-absent", indicated by the response''s `truncated` flag). Legacy
        parity note: the contact filter set is intentionally narrower than legacy. The high-value facets are kept (`q`, `type`,
        `tags`/`tagMode`, `track1099`, `hasTaxId`); legacy origin / linked-user / start-work facets are not ported, and the
        full tax / bank numbers are never filterable or returned (only the display-safe `taxIdLast4` / `hasTaxId` hints).
        Deep, saved filters belong to a `view` ().'
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/contactExpand'
      - $ref: '#/components/parameters/withCount'
      - $ref: '#/components/parameters/view'
      - name: q
        in: query
        required: false
        description: Free-text search over name / company / email.
        schema:
          type: string
      - name: type
        in: query
        required: false
        description: Filter by contact type. Comma-separate for an IN set.
        schema:
          type: string
          example: contractor
      - name: tags
        in: query
        required: false
        description: Comma-separated tag handles to filter by.
        schema:
          type: string
      - name: tagMode
        in: query
        required: false
        description: How `tags` combine.
        schema:
          type: string
          enum:
          - any
          - all
          - none
          default: any
      - name: track1099
        in: query
        required: false
        description: Only contacts tracked (or not) for 1099 reporting.
        schema:
          type: boolean
      - name: hasTaxId
        in: query
        required: false
        description: Only contacts that do (or do not) have a tax id on file.
        schema:
          type: boolean
      - name: includeDeleted
        in: query
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of contacts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactList'
              example:
                data:
                - id: con_5c4d3e2f
                  displayName: Aerial Imaging LLC
                  name: Jordan Lee
                  company: Aerial Imaging LLC
                  title: Drone Operator
                  type: company
                  email: jordan@aerialimaging.example
                  phone: null
                  address: null
                  website: null
                  instagram: null
                  imdb: null
                  defaultRate:
                    amount: 85000
                    currency: USD
                  track1099: true
                  taxIdLast4: '4821'
                  hasTaxId: true
                  notes: null
                  createdAt: '2026-03-10T08:00:00Z'
                  updatedAt: '2026-05-02T16:45:00Z'
                nextCursor: null
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: master-data_createContact
      tags:
      - Contacts
      summary: Create a contact
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactCreate'
            example:
              name: Jordan Lee
              company: Aerial Imaging LLC
              type: company
              email: jordan@aerialimaging.example
              track1099: true
              defaultRate:
                amount: 85000
                currency: USD
      responses:
        '201':
          description: The created contact.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
  /contacts/{contactId}:
    parameters:
    - name: contactId
      in: path
      required: true
      description: "Contact canonical id (`con_\u2026`)."
      schema:
        $ref: '#/components/schemas/Id'
    get:
      operationId: master-data_getContact
      tags:
      - Contacts
      summary: Get a contact
      parameters:
      - $ref: '#/components/parameters/contactExpand'
      responses:
        '200':
          description: The contact.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: master-data_updateContact
      tags:
      - Contacts
      summary: Update a contact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactUpdate'
            example:
              title: Senior Drone Operator
              track1099: false
      responses:
        '200':
          description: The updated contact.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: master-data_deleteContact
      tags:
      - Contacts
      summary: Delete a contact
      description: Soft-delete a contact. Idempotent.
      responses:
        '204':
          description: Deleted (no body).
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/comments:
    parameters:
    - name: projectId
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/Id'
    get:
      operationId: master-data_listComments
      tags:
      - Comments
      summary: List comments
      description: List comments in one project. Narrow to one entity with `targetKind` + `targetId`. Paginated.
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/withCount'
      - name: targetKind
        in: query
        required: false
        description: Anchor entity kind to filter by (paired with `targetId`).
        schema:
          $ref: '#/components/schemas/CommentTargetKind'
      - name: targetId
        in: query
        required: false
        description: Anchor entity id to filter by.
        schema:
          $ref: '#/components/schemas/Id'
      - name: threadId
        in: query
        required: false
        description: Only replies under this root comment.
        schema:
          $ref: '#/components/schemas/Id'
      - name: resolved
        in: query
        required: false
        description: Filter by resolution state.
        schema:
          type: boolean
      responses:
        '200':
          description: A page of comments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentList'
              example:
                data:
                - id: cmt_7a8b9c
                  projectId: prj_8f2a1c9e
                  threadId: null
                  target:
                    kind: budgetLine
                    id: bud_1234abcd
                  authorId: usr_a1b2c3
                  authorName: Sam Rivera
                  content: Can we confirm the drone day rate here?
                  resolved: false
                  createdAt: '2026-05-21T14:00:00Z'
                  updatedAt: '2026-05-21T14:00:00Z'
                nextCursor: null
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
        '404':
          $ref: '#/components/responses/Error404'
    post:
      operationId: master-data_createComment
      tags:
      - Comments
      summary: Create a comment
      description: Post a comment anchored to a typed target entity. The author is the token's principal; the comment's project
        is derived from the target. Gated on `update` against the target entity's permission subject (you can comment where
        you can edit the thing).
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommentCreate'
            example:
              content: Can we confirm the drone day rate here?
              target:
                kind: budgetLine
                id: bud_1234abcd
      responses:
        '201':
          description: The created comment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /projects/{projectId}/comments/{commentId}:
    parameters:
    - name: projectId
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/Id'
    - name: commentId
      in: path
      required: true
      description: "Comment canonical id (`cmt_\u2026`)."
      schema:
        $ref: '#/components/schemas/Id'
    patch:
      operationId: master-data_updateComment
      tags:
      - Comments
      summary: Update a comment
      description: Edit the body (author only) or toggle resolution. Re-anchoring the target is not allowed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommentUpdate'
            example:
              resolved: true
      responses:
        '200':
          description: The updated comment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: master-data_deleteComment
      tags:
      - Comments
      summary: Delete a comment
      description: Soft-delete a comment. Replies under it are deleted with it. Idempotent.
      responses:
        '204':
          description: Deleted (no body).
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /me:
    get:
      operationId: meta-auth_getMe
      tags:
      - Meta
      summary: The token's identity and workspace
      description: Returns the token's principal identity and the one workspace it acts in, with **no path params**. Roles
        resolve live per request. Never leaks teamId / teamRole.
      responses:
        '200':
          description: The token's identity and permission-filtered workspace reach.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Me'
              example:
                id: usr_4f1a8c2e
                type: user
                email: producer@example.com
                name: Dana Producer
                workspaces:
                - workspaceId: ws_2b9d7a1f
                  workspaceRole: admin
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '429':
          $ref: '#/components/responses/Error429'
  /payment-requests:
    get:
      operationId: payment-requests_list
      summary: List payment requests
      description: Returns requests to pay. Use expand to include linked records.
      tags:
      - Payment Requests
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - name: status
        in: query
        schema:
          $ref: '#/components/schemas/PaymentRequestStatus'
      - name: projectId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: purchaseOrderId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: contactId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: expand
        in: query
        schema:
          type: string
        description: Comma-separated PaymentRequestExpand values.
      responses:
        '200':
          description: Payment requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentRequestList'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
  /payment-requests/{paymentRequestId}:
    get:
      operationId: payment-requests_get
      summary: Get a payment request
      description: Returns one request and any linked records you ask to include.
      tags:
      - Payment Requests
      parameters:
      - name: paymentRequestId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - name: expand
        in: query
        schema:
          type: string
        description: Comma-separated PaymentRequestExpand values.
      responses:
        '200':
          description: The payment request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentRequest'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
  /payments:
    get:
      operationId: payments_list
      summary: List payments
      description: Returns each payment from request through settlement.
      tags:
      - Payments
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - name: status
        in: query
        schema:
          $ref: '#/components/schemas/PaymentStatus'
      - name: projectId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: purchaseOrderId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: paymentRequestId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: contactId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: expand
        in: query
        schema:
          type: string
        description: Comma-separated PaymentExpand values.
      responses:
        '200':
          description: Payments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentList'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
  /payments/{paymentId}:
    get:
      operationId: payments_get
      summary: Get a payment
      description: Returns one payment and any linked records you ask to include.
      tags:
      - Payments
      parameters:
      - name: paymentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - name: expand
        in: query
        schema:
          type: string
        description: Comma-separated PaymentExpand values.
      responses:
        '200':
          description: The payment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
  /payments/{paymentId}/timeline:
    get:
      operationId: payments_timeline
      summary: Get a payment timeline
      description: Returns payment history, newest first.
      tags:
      - Payments
      parameters:
      - name: paymentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      responses:
        '200':
          description: Payment history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentTimelinePage'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
  /purchase-orders:
    get:
      operationId: purchase-orders_list
      summary: List purchase orders
      description: Lists purchase orders you can read. Use `projectId` to limit the list to one project or unassigned orders.
        Deleted orders are hidden unless `includeDeleted=true`.
      tags:
      - Purchase Orders
      parameters:
      - name: projectId
        in: query
        required: false
        description: "Filter by project. A `prj_\u2026` id or project slug returns only that project's purchase orders; `none`\
          \ returns unassigned (workspace-level) purchase orders; omitting it returns every purchase order in the workspace.\
          \ A named project that does not exist (or is not readable) returns `404 not_found`."
        schema:
          type: string
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/purchaseOrderSort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/purchaseOrderExpand'
      - $ref: '#/components/parameters/withCount'
      - $ref: '#/components/parameters/view'
      - name: status
        in: query
        required: false
        description: Filter by current status.
        schema:
          $ref: '#/components/schemas/PurchaseOrderStatus'
      - name: contactId
        in: query
        required: false
        description: "Filter to POs whose vendor contact is this id (`con_\u2026`)."
        schema:
          type: string
      - name: budgetLineId
        in: query
        required: false
        description: "Filter to POs charged against this budget line (`bud_\u2026`)."
        schema:
          type: string
      - name: number
        in: query
        required: false
        description: Exact-match filter on the human-facing PO `number` (no text index on number).
        schema:
          type: string
      - name: q
        in: query
        required: false
        description: Free-text search. A text search index exists on `title` only, so `q` matches `title`; `number` is exact-match
          via the `number` filter.
        schema:
          type: string
      - name: includeDeleted
        in: query
        required: false
        description: Include soft-deleted purchase orders (`soft-delete timestamp` populated). Default `false`.
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: A page of purchase orders.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderList'
              examples:
                page:
                  value:
                    data:
                    - id: po_8f2a1c9e
                      workspaceId: ws_3b1
                      projectId: prj_demo
                      number: PO-0042
                      title: Camera package rental
                      status: pending
                      contactId: con_77a
                      budgetLineId: bud_5510
                      date: '2026-05-20'
                      total:
                        amount: 152900
                        currency: USD
                      createdAt: '2026-05-18T14:02:11.000Z'
                      updatedAt: '2026-05-20T09:31:00.000Z'
                    nextCursor: eyJpZCI6InBvXzhmMmExYzllIn0
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: purchase-orders_create
      summary: Create a purchase order
      description: Create a purchase order. The server forces `status='draft'` and sets `createdById`. Pass an optional `projectId`
        in the body to assign the PO to a project; omit it to create a workspace-level (unassigned) purchase order. Naming
        a server-owned field (`status`, `createdById`, `workspaceId`, any `flow*`/`activity*` column) returns `422 field_read_only`.
        Requires an `Idempotency-Key` header.
      tags:
      - Purchase Orders
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      - $ref: '#/components/parameters/purchaseOrderExpand'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderCreate'
            examples:
              create:
                value:
                  number: PO-0042
                  title: Camera package rental
                  contactId: con_77a
                  budgetLineId: bud_5510
                  date: '2026-05-20'
                  items:
                  - description: Alexa Mini LF body
                    qty: 5
                    unit: day
                    rate: 25000
      responses:
        '201':
          description: The created purchase order (status `draft`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
              examples:
                created:
                  value:
                    id: po_8f2a1c9e
                    workspaceId: ws_3b1
                    projectId: prj_demo
                    number: PO-0042
                    title: Camera package rental
                    status: draft
                    contactId: con_77a
                    budgetLineId: bud_5510
                    date: '2026-05-20'
                    createdById: usr_aa1
                    createdAt: '2026-05-18T14:02:11.000Z'
                    updatedAt: '2026-05-18T14:02:11.000Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}:
    get:
      operationId: purchase-orders_get
      summary: Get a purchase order
      description: Fetch one purchase order. An id that does not exist, or exists but is not readable by the principal, returns
        the same `404 not_found`, so existence never leaks.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/purchaseOrderExpand'
      responses:
        '200':
          description: The purchase order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: purchase-orders_update
      summary: Update a purchase order
      description: "Partially update a purchase order. Allowed only while `status \u2208 {draft, rejected}`; otherwise `409\
        \ po_invalid_status`. `status` is server-owned, naming it (or any other server-owned field) returns `422 field_read_only`\
        \ and mutates nothing."
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/purchaseOrderExpand'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderUpdate'
            examples:
              rename:
                value:
                  title: Camera package rental (updated)
                  notes: Net-45
      responses:
        '200':
          description: The updated purchase order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: purchase-orders_delete
      summary: Delete a purchase order
      description: Soft-delete a purchase order (sets `soft-delete timestamp`). Allowed only while `status = draft`; otherwise
        `409 po_invalid_status` (not a silent no-op). Idempotent.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted. No content.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/submit:
    post:
      operationId: purchase-orders_submit
      summary: Reserved purchase order approval submission
      description: Public v1 validates that the PO is submittable, assigned to a project, and has an active approval flow,
        then returns `409 po_invalid_status` until this endpoint is wired to the workspace approval-run service. Use the workspace
        PO submit route for now; this endpoint deliberately does not fake a `pending` status or emit `purchaseOrder.pending`
        without a real run.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/cancel-submission:
    post:
      operationId: purchase-orders_cancelSubmission
      summary: Cancel a pending submission
      description: Cancel the approval request and move a pending PO back to `draft`. Other statuses return `409 po_invalid_status`.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The purchase order back in `draft`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
              examples:
                cancelled:
                  value:
                    id: po_8f2a1c9e
                    workspaceId: ws_3b1
                    projectId: prj_demo
                    status: draft
                    updatedAt: '2026-05-18T14:10:00.000Z'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/void:
    post:
      operationId: purchase-orders_void
      summary: Void a purchase order
      description: Move an `approved` or `actualizing` PO to `void`. Other statuses return `409 po_invalid_status`. Emits
        `purchaseOrder.void`.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The voided purchase order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
              examples:
                voided:
                  value:
                    id: po_8f2a1c9e
                    workspaceId: ws_3b1
                    projectId: prj_demo
                    status: void
                    updatedAt: '2026-05-21T16:00:00.000Z'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/mark-paid:
    post:
      operationId: purchase-orders_markPaid
      summary: Mark a purchase order as paid
      description: Mark an `approved` or `actualizing` PO as `paid`. The PO must have at least one linked, non-deleted transaction.
        The action records the settlement time and emits `purchaseOrder.paid`.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The paid purchase order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
              examples:
                paid:
                  value:
                    id: po_8f2a1c9e
                    workspaceId: ws_3b1
                    projectId: prj_demo
                    status: paid
                    updatedAt: '2026-05-21T18:00:00.000Z'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/transactions/{transactionId}:
    parameters:
    - name: purchaseOrderId
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/Id'
    - name: transactionId
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/Id'
    put:
      operationId: purchase-orders_putTransaction
      tags:
      - Purchase Orders
      summary: Link a transaction to a purchase order
      responses:
        '200':
          description: The linked purchase order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
    delete:
      operationId: purchase-orders_deleteTransaction
      tags:
      - Purchase Orders
      summary: Unlink a transaction from a purchase order
      responses:
        '200':
          description: The purchase order after unlinking.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
  /purchase-orders/{purchaseOrderId}/timeline:
    get:
      operationId: purchase-orders_timeline
      summary: Get the purchase order timeline
      description: Returns purchase-order history, newest first.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      responses:
        '200':
          description: A page of chronological purchase-order events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderTimelinePage'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/items:
    get:
      operationId: purchase-orders_listItems
      summary: List purchase order items
      description: The line items of a purchase order, paginated.
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      responses:
        '200':
          description: A page of purchase-order items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderItemList'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: purchase-orders_createItem
      summary: Add a purchase order item
      description: "Add a line item. Allowed only while the parent PO `status \u2208 {draft, rejected}`; otherwise `409 po_invalid_status`.\
        \ `id` and `purchaseOrderId` are server-owned. This billable create requires an `Idempotency-Key` header for exactly-once\
        \ retries; replaying the same key with a different body returns `409 idempotency_conflict`."
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderItemWrite'
            examples:
              create:
                value:
                  description: Alexa Mini LF body
                  budgetLineId: bud_5510
                  qty: 5
                  unit: day
                  rate: 25000
      responses:
        '201':
          description: The created item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderItem'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /purchase-orders/{purchaseOrderId}/items/{itemId}:
    patch:
      operationId: purchase-orders_updateItem
      summary: Update a purchase order item
      description: "Update a line item. Allowed only while the parent PO `status \u2208 {draft, rejected}`; otherwise `409\
        \ po_invalid_status`. Naming a server-owned field returns `422 field_read_only`."
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: itemId
        in: path
        required: true
        description: "Purchase order item identifier (`poi_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderItemWrite'
            examples:
              update:
                value:
                  qty: 6
                  rate: 24000
      responses:
        '200':
          description: The updated item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderItem'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: purchase-orders_deleteItem
      summary: Delete a purchase order item
      description: "Delete a line item. Allowed only while the parent PO `status \u2208 {draft, rejected}`; otherwise `409\
        \ po_invalid_status`. Idempotent."
      tags:
      - Purchase Orders
      parameters:
      - name: purchaseOrderId
        in: path
        required: true
        description: "Purchase order identifier (`po_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - name: itemId
        in: path
        required: true
        description: "Purchase order item identifier (`poi_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted. No content.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /search:
    get:
      operationId: search_workspace
      summary: Search the workspace or one project
      tags:
      - Search
      description: Ranked, mixed-type search across the workspace or one project, permission-filtered so a token never sees
        results from projects it cannot read. Each hit is `{kind, id, label, snippet, score}`; fetch the full object by `{kind,
        id}`. `searchedTypes` echoes which kinds were actually queried so partial coverage is visible. Results are returned
        in a stable order so pagination is consistent.
      parameters:
      - name: q
        in: query
        required: true
        description: The search query. 1 to 500 characters.
        schema:
          type: string
          minLength: 1
          maxLength: 500
        examples:
          panavision:
            value: Panavision
      - name: types
        in: query
        required: false
        description: "Comma-separated subset of kinds to search; omit to search every supported kind. The response always\
          \ echoes `searchedTypes` (`requested \u2229 supported \u2229 authorized`), so a kind with no text index or a fully-denied\
          \ scope is dropped (not silently empty). An unrecognized kind fails validation (`fieldErrors.types`)."
        style: form
        explode: false
        schema:
          type: array
          items:
            $ref: '#/components/schemas/SearchKind'
        examples:
          twoKinds:
            value: transactions,documents
      - name: projectId
        in: query
        required: false
        description: Limit results to one project.
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      responses:
        '200':
          description: A ranked page of mixed-type hits plus the echoed `searchedTypes`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
              examples:
                panavision:
                  summary: Workspace search for "Panavision"
                  value:
                    data:
                    - kind: transactions
                      id: txn_8f2a1c9e
                      label: Panavision Camera Package
                      snippet: Invoice 4471, Panavision rental
                      score: 0.62
                    - kind: documents
                      id: doc_4b71e0aa
                      label: Panavision quote.pdf
                      score: 0.41
                    nextCursor: eyJzIjowLjQxLCJrIjoiZG9jdW1lbnRzIiwiaWQiOiJkb2NfNGI3MSJ9
                    searchedTypes:
                    - transactions
                    - documents
                    - budget-lines
                    - purchase-orders
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /transactions:
    get:
      operationId: transactions_list
      tags:
      - Transactions
      summary: List transactions
      description: Flat, AND-composed list of the unified ledger for a workspace. Every returned row passes a request-derived
        permission `read` on `{kind:'Transaction', workspaceId, projectId}`; a row the token cannot read is never returned
        (enumeration-safe). Paginated (cap 100). `q` runs the text `similarity(description)` search. The page also echoes
        the `statusReachability` matrix.
      parameters:
      - name: projectId
        in: query
        description: "Filter by project (`prj_\u2026` id or slug). Use `none` for unassigned (workspace-level) transactions;\
          \ omit to return every transaction in the workspace."
        schema:
          type: string
      - name: source
        in: query
        description: Filter by rail. Comma-separated = IN.
        schema:
          $ref: '#/components/schemas/TransactionSource'
      - name: type
        in: query
        description: Filter by movement type (extensible enum). Comma-separated = IN.
        schema:
          type: string
      - name: status
        in: query
        description: Filter by status. Separate multiple values with commas.
        schema:
          $ref: '#/components/schemas/TransactionStatus'
      - name: isReversal
        in: query
        description: Closed boolean over the derived reversal set (refund and dispute states, plaid reversals). `true` = money-back
          events only.
        schema:
          type: boolean
      - name: contactId
        in: query
        description: Filter by assigned contact.
        schema:
          $ref: '#/components/schemas/Id'
      - name: budgetLineId
        in: query
        description: Filter by coded budget line (transaction-level coding).
        schema:
          $ref: '#/components/schemas/Id'
      - name: purchaseOrderId
        in: query
        description: Filter by linked purchase order.
        schema:
          $ref: '#/components/schemas/Id'
      - name: dateFrom
        in: query
        description: Inclusive lower bound on `timestamp` (ISO-8601). `dateFrom > dateTo` -> 400 invalid_date_range.
        schema:
          type: string
          format: date-time
      - name: dateTo
        in: query
        description: Inclusive upper bound on `timestamp` (ISO-8601).
        schema:
          type: string
          format: date-time
      - name: amountMin
        in: query
        description: Inclusive lower bound on `amount` in minor units.
        schema:
          type: integer
          format: int64
      - name: amountMax
        in: query
        description: Inclusive upper bound on `amount` in minor units.
        schema:
          type: integer
          format: int64
      - name: isItemized
        in: query
        description: Filter itemized (`true`) vs single-coded (`false`) transactions.
        schema:
          type: boolean
      - name: merchant
        in: query
        description: Filter by rail-supplied merchant name (case-insensitive substring match). A restored high-value legacy
          facet, useful for grouping sourced spend by vendor.
        schema:
          type: string
      - name: hasDocuments
        in: query
        description: 'Restore the high-value legacy "has attachments" facet, retargeted to the documents model: `true` returns
          only transactions with at least one assigned document, `false` only those with none. (Legacy `hasAttachments`; the
          underlying assignment model is the new `/documents` + assign surface.)'
        schema:
          type: boolean
      - name: q
        in: query
        description: Full-text search over `description`.
        schema:
          type: string
      - $ref: '#/components/parameters/transactionExpand'
      - $ref: '#/components/parameters/transactionSort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/withCount'
      - $ref: '#/components/parameters/view'
      responses:
        '200':
          description: A page of transactions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionCollection'
              example:
                data:
                - id: txn_8f2a1c9e
                  source: plaid
                  sourceId: plx_991
                  type: CreditCard
                  status: posted
                  isReversal: false
                  amount:
                    amount: 152900
                    currency: USD
                  currency: USD
                  timestamp: '2026-05-20T14:03:00.000Z'
                  description: B&H Photo - camera rental
                  merchant: B&H Photo
                  contactId: con_4d5e
                  projectId: prj_films
                  budgetLineId: bud_camera
                  isItemized: false
                  purchaseOrderId: null
                  createdAt: '2026-05-20T14:05:00.000Z'
                  updatedAt: '2026-05-20T14:05:00.000Z'
                nextCursor: eyJ0cyI6MTY4
                statusReachability:
                  journal:
                  - posted
                  - void
                  - projected
                  plaid:
                  - pending
                  - posted
                  - void
                  - needs_review
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: transactions_create
      tags:
      - Transactions
      summary: Create a journal transaction
      description: Create a manual (journal) transaction. `source` is forced to `journal`; a sourced `source` in the body
        -> `422 source_not_postable` (sourced rows arrive via their rail). The optional `projectId` body field assigns the
        row to a project (omit for a workspace-level / unassigned transaction). permission `create` with the request-derived
        `{kind:'Transaction', workspaceId, projectId}` subject. The `status` is validated against the status x source reachability
        matrix. Fires `transaction.created`. This billable create requires an `Idempotency-Key` header; replaying a key with
        a different body -> `409 idempotency_conflict`.
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionJournalCreate'
            example:
              type: Invoice
              amount:
                amount: 50000
                currency: USD
              timestamp: '2026-05-22T00:00:00.000Z'
              description: Vendor invoice
              contactId: con_4d5e
              budgetLineId: bud_postprod
      responses:
        '201':
          description: The created journal transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
              example:
                id: txn_a1b2c3d4
                source: journal
                sourceId: null
                type: Invoice
                status: posted
                isReversal: false
                amount:
                  amount: 50000
                  currency: USD
                currency: USD
                timestamp: '2026-05-22T00:00:00.000Z'
                description: Vendor invoice
                contactId: con_4d5e
                projectId: prj_films
                budgetLineId: bud_postprod
                isItemized: false
                createdAt: '2026-05-22T09:00:00.000Z'
                updatedAt: '2026-05-22T09:00:00.000Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /transactions/stats:
    get:
      operationId: transactions_stats
      tags:
      - Transactions
      summary: Transaction stats (count + sum)
      description: Aggregate count and summed amount for the same filter vocabulary as the list, permission-scoped. Keeps
        the text `q` search branch. Filter to one `currency` for a meaningful summed `amount` across mixed-currency ledgers.
      parameters:
      - name: projectId
        in: query
        description: "Filter by project (`prj_\u2026` id or slug). Use `none` for unassigned (workspace-level) transactions;\
          \ omit to return every transaction in the workspace."
        schema:
          type: string
      - name: source
        in: query
        schema:
          $ref: '#/components/schemas/TransactionSource'
      - name: type
        in: query
        schema:
          type: string
      - name: status
        in: query
        schema:
          $ref: '#/components/schemas/TransactionStatus'
      - name: isReversal
        in: query
        schema:
          type: boolean
      - name: contactId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: budgetLineId
        in: query
        schema:
          $ref: '#/components/schemas/Id'
      - name: purchaseOrderId
        in: query
        description: Filter by linked purchase order.
        schema:
          $ref: '#/components/schemas/Id'
      - name: dateFrom
        in: query
        schema:
          type: string
          format: date-time
      - name: dateTo
        in: query
        schema:
          type: string
          format: date-time
      - name: amountMin
        in: query
        schema:
          type: integer
          format: int64
      - name: amountMax
        in: query
        schema:
          type: integer
          format: int64
      - name: isItemized
        in: query
        schema:
          type: boolean
      - name: merchant
        in: query
        description: Filter by rail-supplied merchant name (case-insensitive substring match).
        schema:
          type: string
      - name: hasDocuments
        in: query
        description: '`true` = only transactions with at least one assigned document; `false` = only those with none (the
          restored legacy "has attachments" facet, retargeted to the documents model).'
        schema:
          type: boolean
      - name: q
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Aggregate stats for the filter.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionStats'
              example:
                total: 142
                amount:
                  amount: 9381200
                  currency: USD
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /transactions/bulk:
    post:
      operationId: transactions_bulk_create
      tags:
      - Transactions
      summary: Bulk journal import
      description: Create many JOURNAL transactions in one request. Every row is forced to `source=journal`; a non-journal
        source on any row -> `422 source_not_postable`. Each row may carry an optional `projectId` to assign it to a project
        (omit for a workspace-level / unassigned row). Over the cap (500) -> `413 bulk_too_large`. Requires an `Idempotency-Key`
        header; replaying a key with a different body -> `409 idempotency_conflict`. This is the highest-stakes billable create
        (one credit charge per created row). Fires one `transaction.created` per created row.
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionBulkCreate'
            example:
              transactions:
              - type: Cash
                amount:
                  amount: 2500
                  currency: USD
                timestamp: '2026-05-18T00:00:00.000Z'
                description: Petty cash - craft services
              - type: Check
                amount:
                  amount: 120000
                  currency: USD
                timestamp: '2026-05-19T00:00:00.000Z'
                description: Location fee
                budgetLineId: bud_locations
      responses:
        '201':
          description: The created transactions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionCollection'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '413':
          $ref: '#/components/responses/Error413'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /transactions/{txId}:
    get:
      operationId: transactions_get
      tags:
      - Transactions
      summary: Get a transaction
      description: Fetch one transaction by id. Returns `404 not_found` for both a missing id and an exists-but-unauthorized
        id (no existence oracle). Row-derived permission `read`.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/transactionExpand'
      responses:
        '200':
          description: The transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
              example:
                id: txn_8f2a1c9e
                source: saturation_credit
                sourceId: scx_771
                type: CreditCard
                status: posted
                isReversal: true
                amount:
                  amount: -4500
                  currency: USD
                currency: USD
                timestamp: '2026-05-21T10:00:00.000Z'
                description: Refund - duplicate charge
                merchant: B&H Photo
                sourceLast4: '4242'
                projectId: prj_films
                budgetLineId: bud_camera
                isItemized: false
                createdAt: '2026-05-21T10:02:00.000Z'
                updatedAt: '2026-05-21T10:02:00.000Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: transactions_update
      tags:
      - Transactions
      summary: Update a transaction
      description: Partial update. Which fields are writable depends on the transaction's `source`. Assignment fields succeed
        on any source; setting `projectId` assigns the row to a project (or `null` unassigns it back to the workspace level).
        Core fields (`amount`, `currency`, `timestamp`, `type`, `status`, `merchant`, `sourceLast4`, `sourceName`) are writable
        only on `journal` - naming one on a sourced row -> `422 field_read_only`. Server-owned fields are never writable.
        A `status` change is validated against the reachability matrix -> `409 status_unreachable_for_source`. Row-derived
        permission `update`. Fires `transaction.updated`.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionPatch'
            example:
              budgetLineId: bud_camera
              notes: Re-coded to camera package
      responses:
        '200':
          description: The updated transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: transactions_delete
      tags:
      - Transactions
      summary: Delete a journal transaction
      description: Soft-delete (`soft-delete timestamp`/`deletedReason`) restricted to `source=journal`; a sourced row ->
        `409` (the rail owns the record). Re-POST or the resurrect path restores. Row-derived permission `delete`. Returns
        `204`.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Soft-deleted. No body.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '429':
          $ref: '#/components/responses/Error429'
  /transactions/{txId}/items:
    get:
      operationId: transactions_items_list
      tags:
      - Transactions
      summary: List transaction items
      description: The itemized lines of a transaction. Row-derived permission `read` on the parent.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      responses:
        '200':
          description: The items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionItemCollection'
              example:
                data:
                - id: txi_4a1b2c3d
                  lineNumber: '1'
                  description: Camera body - 3 days
                  budgetLineId: bud_camera
                  qty: 3
                  unit: Day
                  rate:
                    amount: 25000
                    currency: USD
                  amount:
                    amount: 75000
                    currency: USD
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: transactions_items_create
      tags:
      - Transactions
      summary: Add a transaction item
      description: Create an item. The server assigns `lineNumber`. The first item flips the parent `isItemized=true` and
        coding moves from `transaction.budgetLineId` to the items. Row-derived permission `update` on the parent. Fires `transaction.updated`.
        This billable create requires an `Idempotency-Key` header; replaying a key with a different body -> `409 idempotency_conflict`.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      - name: Idempotency-Key
        in: header
        required: true
        description: 'Required client-generated key (16-255 chars) for exactly-once creation. Retrying with the same key replays
          the original result (`Idempotency-Replayed: true`); the same key with a different body returns `409 idempotency_conflict`.
          Missing or too-short key returns `400 validation`.'
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionItemCreate'
            example:
              description: Lens kit - 3 days
              budgetLineId: bud_camera
              qty: 3
              unit: Day
              rate:
                amount: 8000
                currency: USD
              amount:
                amount: 24000
                currency: USD
      responses:
        '201':
          description: The created item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionItem'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /transactions/{txId}/items/{itemId}:
    patch:
      operationId: transactions_items_update
      tags:
      - Transactions
      summary: Update a transaction item
      description: Partial update of an item. `id`/`lineNumber`/tenant keys are server-owned -> `422 field_read_only` if named.
        Row-derived permission `update` on the parent. Fires `transaction.updated`.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      - name: itemId
        in: path
        required: true
        description: Transaction item id (`txi_...`).
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionItemPatch'
            example:
              qty: 4
              amount:
                amount: 32000
                currency: USD
      responses:
        '200':
          description: The updated item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionItem'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: transactions_items_delete
      tags:
      - Transactions
      summary: Delete a transaction item
      description: Soft-delete an item (for unsplit recovery). Deleting the last item recomputes the parent `isItemized` back
        to `false`. Row-derived permission `update` on the parent. Fires `transaction.updated`. Returns `204`.
      parameters:
      - name: txId
        in: path
        required: true
        description: Transaction id (`txn_...`).
        schema:
          $ref: '#/components/schemas/Id'
      - name: itemId
        in: path
        required: true
        description: Transaction item id (`txi_...`).
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Item soft-deleted. No body.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /webhooks:
    get:
      operationId: webhooks_list
      summary: List webhook subscriptions
      description: List the workspace's outbound webhook subscriptions the caller can manage, permission-filtered and paginated.
        Secrets are never included. Filter by subscribed `events` or by `active` state.
      tags:
      - Webhooks
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/withCount'
      - name: events
        in: query
        required: false
        description: Only subscriptions that include at least one of these event types. Repeat the param or pass a comma-separated
          list.
        schema:
          type: array
          items:
            $ref: '#/components/schemas/WebhookEvent'
        style: form
        explode: false
      - name: active
        in: query
        required: false
        description: Filter by whether the subscription is currently delivering (`isActive`).
        schema:
          type: boolean
      responses:
        '200':
          description: A page of webhook subscriptions (secrets omitted).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookCollection'
              example:
                data:
                - id: whk_5c1a9e02
                  url: https://hooks.example.com/saturation
                  events:
                  - transaction.created
                  - budget.changed
                  payloadStyle: thin
                  isActive: true
                  workspaceId: ws_1a2b3c4d
                  createdAt: '2026-05-28T12:00:00.000Z'
                  updatedAt: '2026-05-28T12:00:00.000Z'
                nextCursor: null
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    post:
      operationId: webhooks_create
      summary: Create a webhook subscription
      description: Register an outbound webhook. `manage`-gated. The `url` must be HTTPS and pass the SSRF guard at registration
        (private / loopback / link-local / cloud-metadata / IPv6-mapped / decimal-octal-hex / internal-DNS targets are rejected).
        A `secret` is generated when not supplied and returned in this response only, it never appears on any later read.
        Naming a server-owned field (`id`, `workspaceId`, owning identity, `isActive`) returns `422 field_read_only`.
      tags:
      - Webhooks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookCreate'
            example:
              url: https://hooks.example.com/saturation
              events:
              - transaction.created
              - transaction.updated
              - budget.changed
              payloadStyle: thin
      responses:
        '201':
          description: The created subscription, including the signing `secret` (returned exactly once, store it now).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookWithSecret'
              example:
                id: whk_5c1a9e02
                url: https://hooks.example.com/saturation
                events:
                - transaction.created
                - transaction.updated
                - budget.changed
                payloadStyle: thin
                isActive: true
                workspaceId: ws_1a2b3c4d
                secret: whsec_example
                createdAt: '2026-05-28T12:00:00.000Z'
                updatedAt: '2026-05-28T12:00:00.000Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /webhooks/{webhookId}:
    get:
      operationId: webhooks_get
      summary: Get a webhook subscription
      description: Fetch one subscription. The `secret` is never returned. A subscription that exists but the caller cannot
        manage returns the same `404 not_found` as a genuine miss (existence never leaks).
      tags:
      - Webhooks
      parameters:
      - name: webhookId
        in: path
        required: true
        description: "Webhook subscription identifier (`whk_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '200':
          description: The subscription (secret omitted).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Webhook'
              example:
                id: whk_5c1a9e02
                url: https://hooks.example.com/saturation
                events:
                - transaction.created
                - budget.changed
                payloadStyle: thin
                isActive: true
                workspaceId: ws_1a2b3c4d
                createdAt: '2026-05-28T12:00:00.000Z'
                updatedAt: '2026-05-28T12:00:00.000Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
    patch:
      operationId: webhooks_update
      summary: Update a webhook subscription
      description: Update `url`, `events`, `payloadStyle` or `isActive` (explicit allow-list). Tenant keys (`workspaceId`
        / `projectId`), the owning identity, and `secret` are server-owned, naming any returns `422 field_read_only` (fail-loud,
        never a silent strip). A `url` change re-runs the SSRF guard. The `secret` is never returned.
      tags:
      - Webhooks
      parameters:
      - name: webhookId
        in: path
        required: true
        description: "Webhook subscription identifier (`whk_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookUpdate'
            example:
              events:
              - transaction.created
              - purchaseOrder.approved
              isActive: false
      responses:
        '200':
          description: The updated subscription (secret omitted).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Webhook'
              example:
                id: whk_5c1a9e02
                url: https://hooks.example.com/saturation
                events:
                - transaction.created
                - purchaseOrder.approved
                payloadStyle: thin
                isActive: false
                workspaceId: ws_1a2b3c4d
                createdAt: '2026-05-28T12:00:00.000Z'
                updatedAt: '2026-05-28T13:30:00.000Z'
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
    delete:
      operationId: webhooks_delete
      summary: Delete a webhook subscription
      description: Permanently unsubscribe. Idempotent, deleting an already-deleted subscription returns `204`. A subscription
        that exists but the caller cannot manage returns `404 not_found`.
      tags:
      - Webhooks
      parameters:
      - name: webhookId
        in: path
        required: true
        description: "Webhook subscription identifier (`whk_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '204':
          description: Deleted. No content.
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
  /webhooks/{webhookId}/test-delivery:
    post:
      operationId: webhooks_sendTestDelivery
      summary: Send a test delivery
      description: Issue a synthetic `ping` event through the same signed, SSRF-guarded delivery path and record a `WebhookDelivery`
        row (readable via `GET.../deliveries`). Use it to confirm an endpoint is reachable and the signature verifies. No
        request body.
      tags:
      - Webhooks
      parameters:
      - name: webhookId
        in: path
        required: true
        description: "Webhook subscription identifier (`whk_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      responses:
        '202':
          description: The test delivery was queued and recorded; inspect its outcome via the deliveries log.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookDelivery'
              example:
                id: whd_9a02c1e5
                event: transaction.created
                status: pending
                entityKind: ping
                entityId: whk_5c1a9e02
                payloadStyle: thin
                requestUrl: https://hooks.example.com/saturation
                attempts: 1
                occurredAt: '2026-05-28T14:00:00.000Z'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '409':
          $ref: '#/components/responses/Error409'
        '422':
          $ref: '#/components/responses/Error422'
        '429':
          $ref: '#/components/responses/Error429'
  /webhooks/{webhookId}/deliveries:
    get:
      operationId: webhooks_listDeliveries
      summary: List recent deliveries
      description: Inspect recent delivery attempts for one subscription (debugging), paginated and newest-first by default.
        Filter by attempt `status`, by `event` type, or by `since`.
      tags:
      - Webhooks
      parameters:
      - name: webhookId
        in: path
        required: true
        description: "Webhook subscription identifier (`whk_\u2026`)."
        schema:
          $ref: '#/components/schemas/Id'
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/cursor'
      - $ref: '#/components/parameters/sort'
      - $ref: '#/components/parameters/order'
      - $ref: '#/components/parameters/withCount'
      - name: status
        in: query
        required: false
        description: Only deliveries with this attempt outcome.
        schema:
          $ref: '#/components/schemas/WebhookDeliveryStatus'
      - name: event
        in: query
        required: false
        description: Only deliveries for this event type.
        schema:
          $ref: '#/components/schemas/WebhookEvent'
      - name: since
        in: query
        required: false
        description: Only deliveries that occurred at or after this ISO-8601 timestamp. An end before the start (when paired
          with sort bounds) returns `400 invalid_date_range`.
        schema:
          type: string
          format: date-time
      responses:
        '200':
          description: A page of delivery attempts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookDeliveryCollection'
              example:
                data:
                - id: whd_9a02c1e5
                  event: transaction.created
                  status: success
                  entityKind: transaction
                  entityId: txn_8f2a1c9e
                  payloadStyle: thin
                  requestUrl: https://hooks.example.com/saturation
                  responseStatus: 200
                  attempts: 1
                  occurredAt: '2026-05-28T14:00:00.000Z'
                  deliveredAt: '2026-05-28T14:00:00.420Z'
                nextCursor: null
        '400':
          $ref: '#/components/responses/Error400'
        '401':
          $ref: '#/components/responses/Error401'
        '403':
          $ref: '#/components/responses/Error403'
        '404':
          $ref: '#/components/responses/Error404'
        '429':
          $ref: '#/components/responses/Error429'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: A bearer token for the API. Create one in Saturation under Settings > Developers > API, then send it in
        the Authorization header as `Bearer <token>`. The token acts as you in the one workspace it was created in.
  parameters:
    projectId:
      name: projectId
      in: path
      required: true
      description: "Project identifier, accepts the canonical `id` (`prj_\u2026`) or the project `slug`."
      schema:
        type: string
    limit:
      name: limit
      in: query
      required: false
      description: Maximum number of items to return on a page. Capped at 100.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    cursor:
      name: cursor
      in: query
      required: false
      description: Opaque cursor from a prior page's `nextCursor`. Encodes the mint-time filter + sort; replaying it against
        a changed filter or sort returns `400 cursor_invalid`. Stable under concurrent insert / soft-delete.
      schema:
        type: string
    sort:
      name: sort
      in: query
      required: false
      description: Field to sort by (replaces legacy `sortBy`). A non-unique sort field appends `,id` as the tiebreaker. Allowed
        fields are typed per resource.
      schema:
        type: string
    order:
      name: order
      in: query
      required: false
      description: Sort direction (replaces legacy `sortOrder`).
      schema:
        type: string
        enum:
        - asc
        - desc
        default: asc
    transactionSort:
      name: sort
      in: query
      required: false
      description: Field to sort the transactions list by. A non-unique field appends `,id` as the tiebreaker. Defaults to
        `timestamp` `desc`.
      schema:
        $ref: '#/components/schemas/TransactionSortField'
    purchaseOrderSort:
      name: sort
      in: query
      required: false
      description: Field to sort the purchase-order list by. A non-unique field appends `,id` as the tiebreaker.
      schema:
        $ref: '#/components/schemas/PurchaseOrderSort'
    view:
      name: view
      in: query
      required: false
      description: Replay a saved view (`?view={id}`), a named, persisted filter + shape bundle. The view's filter/sort/expand
        resolve first; any inline query params layer on top to refine. An unknown or unreadable view id returns `404 not_found`.
      schema:
        $ref: '#/components/schemas/Id'
    transactionExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on transactions (depth \u2264 2). Unknown or too-deep key returns\
        \ `400 expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/TransactionExpandKey'
      style: form
      explode: false
    purchaseOrderExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on a purchase order (depth \u2264 2). Unknown key returns `400 expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/PurchaseOrderExpand'
      style: form
      explode: false
    documentExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on documents (depth \u2264 2). Unknown key returns `400 expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/DocumentExpand'
      style: form
      explode: false
    contactExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on contacts (depth \u2264 2). Unknown key returns `400 expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/ContactExpand'
      style: form
      explode: false
    budgetLineExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on budget lines (depth \u2264 2). Unknown or too-deep key returns\
        \ `400 expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/BudgetLineExpand'
      style: form
      explode: false
    libraryExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on a project copy (depth \u2264 2). Unknown key returns `400 expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/LibraryExpand'
      style: form
      explode: false
    projectIncentiveExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on a project incentive (depth \u2264 2). Unknown key returns `400\
        \ expand_invalid`."
      schema:
        type: array
        items:
          $ref: '#/components/schemas/ProjectIncentiveExpand'
      style: form
      explode: false
    ratePackExpand:
      name: expand
      in: query
      required: false
      description: Comma list of related data to inline on a workspace-source rate pack. Unknown key returns `400 expand_invalid`.
      schema:
        type: array
        items:
          $ref: '#/components/schemas/RatePackExpand'
      style: form
      explode: false
    incentivePackExpand:
      name: expand
      in: query
      required: false
      description: Comma list of related data to inline on a workspace-source incentive pack. Unknown key returns `400 expand_invalid`.
      schema:
        type: array
        items:
          $ref: '#/components/schemas/IncentivePackExpand'
      style: form
      explode: false
    projectRatePackExpand:
      name: expand
      in: query
      required: false
      description: Comma list of related data to inline on a project-installed rate pack. Unknown key returns `400 expand_invalid`.
      schema:
        type: array
        items:
          $ref: '#/components/schemas/ProjectRatePackExpand'
      style: form
      explode: false
    viewDataExpand:
      name: expand
      in: query
      required: false
      description: "Comma list of related data to inline on resolved view rows (depth \u2264 2). The valid keys depend on\
        \ the view's `subjectType`, they are the same per-resource allow-list as the underlying resource (transactions, documents,\
        \ contacts or budget lines). An unknown or too-deep key for the resolved subject returns `400 expand_invalid`."
      schema:
        type: array
        items:
          type: string
      style: form
      explode: false
    withCount:
      name: withCount
      in: query
      required: false
      description: Opt in to a total `count` on the collection envelope. Off by default because counting is expensive. Valid
        on every list response, the canonical envelope always carries `count`.
      schema:
        type: boolean
        default: false
  responses:
    Error400:
      description: Bad request, `validation`, `cursor_invalid`, `expand_invalid`, `invalid_date_range`, `webhook_https_required`
        or `document_invalid_target_kind`.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error401:
      description: Unauthenticated, `unauthenticated`, `invalid_token`, `missing_authorization` or `token_revoked` (expired,
        malformed, missing or revoked credentials).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error403:
      description: "Forbidden, `permission_revoked` (with a `requiredAbility` hint), `scope_exceeded`, `forbidden`, `feature_not_available`,\
        \ `role_ceiling_exceeded`, `token_revoked` or `document_assign_forbidden`. The principal's live ability \u2229 token\
        \ scopes does not permit the action."
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error404:
      description: Not found, `not_found` (also returned for exists-but-unauthorized, so existence never leaks) or `document_target_not_found`.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error409:
      description: Conflict, `account_path_ambiguous`, `idempotency_conflict`, `already_assigned`, `status_unreachable_for_source`,
        `po_invalid_status` or `webhook_url_invalid_ssrf`.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error413:
      description: Payload / result too large, `budget_too_large` or `bulk_too_large`.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error422:
      description: Unprocessable entity, `field_read_only` (mass-assignment of a server-owned field), `source_not_postable`,
        `webhook_url_blocked` or `role_ceiling_exceeded`.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error429:
      description: Rate limited, `rate_limited`. The response carries a `Retry-After` header. Rate-limit internals are not
        leaked.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Error504:
      description: Gateway timeout, `budget_compute_timeout`. The budget engine did not return a computed result within the
        deadline; retry (the result may be cached on the next call).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  schemas:
    Id:
      type: string
      description: "A stable, prefixed canonical identifier (e.g. `ws_\u2026`, `prj_\u2026`, `acc_\u2026`, `txn_\u2026`, `bud_\u2026\
        `, `po_\u2026`, `doc_\u2026`). The internal domain `address` is never exposed."
      examples:
      - txn_8f2a1c9e
    PublicResourceRef:
      type: object
      description: A typed reference to another public resource. The referenced row is included only when the caller can read
        it.
      required:
      - type
      - id
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
          - document
          - payment_request
          - payment
          - transaction
          - approval
        id:
          $ref: '#/components/schemas/Id'
    ErrorCode:
      type: string
      description: The closed set of stable, typed error codes returned at the `/v1` boundary. (The internal catalog is numeric;
        these public string codes live only inside the public-API envelope.)
      enum:
      - unauthenticated
      - invalid_token
      - missing_authorization
      - token_revoked
      - permission_revoked
      - scope_exceeded
      - forbidden
      - feature_not_available
      - upgrade_required
      - capacity_exceeded
      - approval_required
      - resource_locked
      - temporarily_unavailable
      - role_ceiling_exceeded
      - api_access_disabled
      - not_found
      - document_target_not_found
      - validation
      - cursor_invalid
      - expand_invalid
      - invalid_date_range
      - webhook_https_required
      - document_invalid_target_kind
      - range_too_large
      - account_path_ambiguous
      - account_code_ambiguous
      - budget_compute_stale
      - idempotency_conflict
      - already_assigned
      - status_unreachable_for_source
      - po_invalid_status
      - webhook_url_invalid_ssrf
      - legacy_fringe_unsupported
      - field_read_only
      - scenario_too_large
      - source_not_postable
      - webhook_url_blocked
      - document_assign_forbidden
      - budget_too_large
      - bulk_too_large
      - rate_limited
      - signing_key_not_configured
      - internal_error
      - budget_compute_timeout
    Error:
      type: object
      description: 'The uniform error envelope. Only errors carry `success: false`; success responses are the bare resource
        or a `{ data, nextCursor? }` collection.'
      required:
      - success
      - code
      - message
      - requestId
      additionalProperties: false
      properties:
        success:
          type: boolean
          const: false
          description: Always `false`. Present only on errors.
        code:
          $ref: '#/components/schemas/ErrorCode'
        message:
          type: string
          description: A human-readable, self-diagnosable description of the failure.
        requestId:
          type: string
          description: The id of the request that failed, quote it in support tickets and correlate with usage logs.
          examples:
          - req_3f9b2a7c
        fieldErrors:
          type: object
          description: 'Present on validation / mass-assignment failures: a map of input field name to a list of error messages
            for that field.'
          additionalProperties:
            type: array
            items:
              type: string
        requiredAbility:
          type: string
          description: 'Present on `permission_revoked` (403): the permission ability the caller is missing, expressed as
            `action:subject` (e.g. `update:Transaction`), so the failure is self-diagnosable.'
        retryAfter:
          type: integer
          description: 'Present on `rate_limited` (429): seconds to wait before retrying (mirrors the `Retry-After` header).'
        accessDecision:
          $ref: '#/components/schemas/ProductAccessDecision'
          description: Present when product access denied the operation. Route remediation by the typed reason, not by HTTP
            status alone.
    ProductAccessDecision:
      type: object
      description: Redacted product-access denial evidence. Internal workspace, principal, resource, idempotency, catalog,
        and subscription evidence is never exposed.
      required:
      - operationId
      - allowed
      - state
      - primary
      - remediations
      - resolvingPlanId
      additionalProperties: false
      properties:
        operationId:
          type: string
          minLength: 1
        allowed:
          type: boolean
        state:
          type: string
          enum:
          - allowed
          - warning
          - denied
          - indeterminate
        primary:
          type:
          - object
          - 'null'
          additionalProperties: false
          required:
          - kind
          - status
          - reason
          properties:
            kind:
              type: string
              enum:
              - principal
              - authority
              - policy
              - rollout
              - provider
              - entitlement
              - quota
            status:
              type: string
              enum:
              - warning
              - denied
              - indeterminate
            reason:
              type: string
              enum:
              - principal_unauthenticated
              - principal_expired
              - principal_suspended
              - resource_unreachable
              - permission_denied
              - resource_locked
              - approval_required
              - safety_restricted
              - rollout_disabled
              - delivery_unavailable
              - provider_unavailable
              - provider_ineligible
              - legal_restricted
              - plan_excluded
              - contract_restricted
              - subscription_restricted
              - quota_warning
              - quota_exhausted
              - quota_unknown
              - quota_reservation_required
              - dependency_unknown
            quotaId:
              type: string
              minLength: 1
            usage:
              type: integer
              minimum: 0
            limit:
              type: integer
              minimum: 0
        remediations:
          type: array
          items:
            type: string
            enum:
            - self_serve_upgrade
            - contact_billing_owner
            - contact_sales
            - buy_capacity
            - cleanup
            - authenticate
            - request_access
            - resolve_policy
            - wait_for_rollout
            - resolve_provider
            - recover_payment
            - reactivate_subscription
            - contact_support
            - renew_link
        currentPlanId:
          type: string
          enum:
          - free
          - pro
          - team
          - enterprise
        resolvingPlanId:
          type:
          - string
          - 'null'
          enum:
          - free
          - pro
          - team
          - enterprise
          - null
        correlationId:
          type: string
          minLength: 1
    Money:
      type: object
      description: A monetary amount as an integer count of the currency's minor units (e.g. cents) plus an explicit ISO-4217
        currency. Never a float.
      required:
      - amount
      - currency
      additionalProperties: false
      properties:
        amount:
          type: integer
          format: int64
          description: Amount in the currency's smallest unit (e.g. cents). May be negative.
          examples:
          - 152900
        currency:
          type: string
          description: ISO-4217 currency code.
          minLength: 3
          maxLength: 3
          examples:
          - USD
    NextCursor:
      type:
      - string
      - 'null'
      description: The canonical cursor field carried flat on every collection envelope. Opaque; pass it back as `cursor`
        to fetch the next page. `null` (or absent) on the last page. Encodes the mint-time filter + sort; replaying it against
        a changed filter/sort returns `400 cursor_invalid`.
      examples:
      - eyJpZCI6InR4bl84ZjJhMWM5ZSJ9
      - null
    PhaseType:
      type: string
      description: "The public budget-phase column type. This is the vocabulary. The internal value set is mapped at the API\
        \ boundary (`estimate\u2192regular`, `rollup\u2192variance`, `actual\u2192actual`, `committed\u2192committed`) and\
        \ never leaks into a response."
      enum:
      - estimate
      - actual
      - rollup
      - committed
      examples:
      - estimate
    BudgetLineKind:
      type: string
      description: The the row kind (the `type` column on a budget line). Accounts are NOT a separate table, an `account`
        row is a budget line with `kind=account`.
      enum:
      - account
      - line
      - subtotal
      - fringe
      - markup
      - credit
      - banner
    ComputedLineKind:
      type: string
      description: The narrowed kind that appears on a node in the computed whole-tree response. The computed response collapses
        the stored row kind to these four.
      enum:
      - account
      - line
      - credit
      - fringe
    BudgetLineExpand:
      type: string
      description: "Allowed `expand` keys for budget-line reads (comma-separated, depth \u2264 2). `contact` is a relation\
        \ expand; `account` projects classifier fields already inline on the row; `phaseTotals` adds the computed value matrix;\
        \ `phaseData` adds raw editable estimate phase data keyed by phase id; `source` dereferences the `sourceId` to its\
        \ library origin (or an explicit tombstone, never null). An unknown or too-deep key returns `400 expand_invalid`.\
        \ Legacy parity note: the legacy `lines.notes` expand is intentionally NOT ported, the note is now a plain-text `line.notes`\
        \ field always present inline on the line, so there is nothing to expand (the rich-text `NoteData` / `/budget/note`\
        \ resource is gone, see `BudgetLine.notes`)."
      enum:
      - phaseTotals
      - phaseData
      - contact
      - account
      - source
    TagMode:
      type: string
      description: How a multi-value `tags` filter composes. `any` = OR (default), `all` = AND, `none` = exclude any tagged
        row.
      enum:
      - any
      - all
      - none
      default: any
    FringeBreakdownEntry:
      type: object
      description: One contributing fringe within a computed PhaseValues, itemized for transparency.
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
          description: Fringe label as it appears in the Library / on screen.
        amount:
          type: integer
          format: int64
          description: This fringe's contribution to the cell, in workspace-base minor units.
    PhaseValues:
      type: object
      description: The computed value matrix for one phase of one row (or the rolled-up subtotal on an account row). All amounts
        are integers in the workspace **base** currency's minor units; each contributing line is converted to base via its
        exchange rate before summing, so a computed value is never a mix of currencies. `combined` is the source of truth
        (`combined === amount`); clients must never re-sum.
      required:
      - base
      - overtime
      - fringe
      - fringeBreakdown
      - amount
      - combined
      - currency
      additionalProperties: false
      properties:
        base:
          type: integer
          format: int64
          description: Base amount (pre-overtime, pre-fringe), minor units of the base currency.
        overtime:
          type: integer
          format: int64
          description: Overtime amount, minor units of the base currency.
        fringe:
          type: integer
          format: int64
          description: Total fringe amount, minor units of the base currency.
        fringeBreakdown:
          type: array
          description: Itemized contributing fringes that sum to `fringe`.
          items:
            $ref: '#/components/schemas/FringeBreakdownEntry'
        amount:
          type: integer
          format: int64
          description: The computed amount for the node. `combined` mirrors this; it is the source of truth.
        combined:
          type: integer
          format: int64
          description: base + overtime + fringe, post-credit; the authoritative total for the cell. Always equal to `amount`.
            Never re-summed client-side, never a stored fallback.
        currency:
          type: string
          description: The workspace base ISO-4217 currency the computed value is normalized to.
          minLength: 3
          maxLength: 3
          examples:
          - USD
      examples:
      - base: 1000000
        overtime: 0
        fringe: 220000
        fringeBreakdown:
        - id: frg_pension
          name: Pension
          amount: 220000
        amount: 1220000
        combined: 1220000
        currency: USD
    BudgetPhase:
      type: object
      description: A budget phase, the column dimension of the budget matrix.
      required:
      - id
      - type
      - name
      - sort
      - isHidden
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        type:
          $ref: '#/components/schemas/PhaseType'
        name:
          type: string
          description: Phase name as shown on screen.
        alias:
          type:
          - string
          - 'null'
          description: Optional short alias for the phase column header.
        color:
          type:
          - string
          - 'null'
          description: Display color token for the phase column (never a hex literal in the UI).
        sort:
          type: integer
          description: Stable display order among phases.
        isHidden:
          type: boolean
          description: Whether the phase column is hidden in the UI.
      examples:
      - id: phs_estimate
        type: estimate
        name: Estimate
        alias: EST
        color: blue
        sort: 0
        isHidden: false
    BudgetLine:
      type: object
      description: A stored budget row. `code` is the (non-unique, possibly empty) account number; `path` is the coded account
        path used for friendly lookups. The internal `address` is never returned. Expand-only relations (`contact`, `account`,
        `phaseTotals`, `phaseData`, `source`) appear only when requested via `expand`.
      required:
      - id
      - kind
      - name
      - parentId
      - sort
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        code:
          type:
          - string
          - 'null'
          description: Account number (the friendly `accountId` classifier). Non-unique, may be empty.
          examples:
          - '1100'
        path:
          type:
          - string
          - 'null'
          description: Coded account path for friendly lookups (e.g. `1100/1110`). Never the address.
          examples:
          - 1100/1110
        kind:
          $ref: '#/components/schemas/BudgetLineKind'
        name:
          type: string
          description: Line description (the on-screen line name).
        parentId:
          $ref: '#/components/schemas/Id'
        depth:
          type: integer
          description: Depth in the account tree (root account = 0). Present on tree reads.
        sort:
          type: integer
          description: Stable display order among siblings.
        contactId:
          $ref: '#/components/schemas/Id'
        lineColor:
          type:
          - string
          - 'null'
          description: Display color token for the row.
        emoji:
          type:
          - string
          - 'null'
        notes:
          type:
          - string
          - 'null'
          description: 'Free-text note on the line, a single plain-text string. Legacy parity note: the legacy budget-line
            note was a rich `NoteData` object (TipTap `json`/`html`/`text`) managed through dedicated `/budget/note` endpoints;
            that shape and those endpoints are intentionally NOT ported. Notes are plain text on `line.notes`, read and written
            inline on the line itself, there is no separate note resource and no rich-text payload.'
        tagIds:
          type: array
          description: Tag ids attached to this line.
          items:
            $ref: '#/components/schemas/Id'
        subtotalSumAllAbove:
          type: boolean
          description: For `subtotal` rows, sum all rows above rather than the immediate group.
        subtotalIsBold:
          type: boolean
        markupAccountFilter:
          type:
          - string
          - 'null'
          description: For `markup` rows, the account filter the markup applies to.
        sourceId:
          description: Identifies the **workspace** library item this row was copied from (e.g. a credit source). Null when
            the row has no workspace-library origin. Dereference with `expand=source`.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        sourceIncentiveVersionId:
          description: 'Identifies the **Saturation-derived** credit''s incentive *version* (not the program) this row was
            copied from. Source tracking uses two fields by origin: `sourceId` for workspace copies, `sourceIncentiveVersionId`
            (+ `sourceType`) for Saturation-derived credits. Null when the row has no incentive origin.'
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        sourceType:
          type:
          - string
          - 'null'
          description: The origin class that disambiguates `sourceId` vs `sourceIncentiveVersionId` for a row (e.g. an incentive-derived
            credit). Null when the row has no library origin.
        contact:
          description: Present only with `expand=contact`. The full contact (permission-projected).
          $ref: '#/components/schemas/Contact'
        source:
          description: 'Present only with `expand=source`. The live library item `sourceId` points to, or an explicit tombstone
            `{ id, deleted: true }` when the source was removed, never null.'
        phaseTotals:
          type: object
          description: Present only with `expand=phaseTotals`. The per-phase computed value matrix for this line, keyed by
            phase id.
          additionalProperties:
            $ref: '#/components/schemas/PhaseValues'
        phaseData:
          type: object
          description: Present only with `expand=phaseData`. Raw editable estimate phase data keyed by phase id.
          additionalProperties:
            $ref: '#/components/schemas/BudgetDocumentLinePhaseData'
      examples:
      - id: lin_7c1a2f
        code: '1100'
        path: '1100'
        kind: account
        name: Camera
        parentId: lin_root
        depth: 0
        sort: 0
        contactId: null
        tagIds:
        - tag_vfx
        sourceId: null
    BudgetLineCreate:
      type: object
      description: Create a budget line. Allow-list only, server mints `id`, the internal address reference, and all timestamps;
        naming any server-owned field returns `422 field_read_only`.
      required:
      - name
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 255
          description: Line description.
        kind:
          $ref: '#/components/schemas/BudgetLineKind'
        code:
          type: string
          description: Account number to set on the row (optional; non-unique).
        parentId:
          $ref: '#/components/schemas/Id'
        contactId:
          $ref: '#/components/schemas/Id'
        lineColor:
          type: string
        emoji:
          type: string
        notes:
          type: string
        tagIds:
          type: array
          items:
            $ref: '#/components/schemas/Id'
        subtotalSumAllAbove:
          type: boolean
        subtotalIsBold:
          type: boolean
        markupAccountFilter:
          type: string
        phaseData:
          type: object
          description: Optional initial editable phase data keyed by phase id.
          additionalProperties:
            $ref: '#/components/schemas/BudgetLinePhaseDataWrite'
      examples:
      - name: Camera Package
        kind: line
        code: '1110'
        parentId: lin_7c1a2f
        contactId: con_panavision
        tagIds:
        - tag_vfx
        phaseData:
          phs_estimate:
            rate: 25000
            quantity: 4
            multiplier: 1
    BudgetLineUpdate:
      type: object
      description: Patch a budget line. Allow-list mirrors the provider's writable set. Naming a server-owned or flow-derived
        field (`address`, `budgetId`, `path`, `depth`, `createdAt`, `updatedAt`, `soft-delete timestamp`) returns `422 field_read_only`
        listing the offenders, never a silent strip.
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          maxLength: 255
        kind:
          $ref: '#/components/schemas/BudgetLineKind'
        code:
          type: string
        parentId:
          $ref: '#/components/schemas/Id'
        contactId:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        lineColor:
          type:
          - string
          - 'null'
        emoji:
          type:
          - string
          - 'null'
        notes:
          type:
          - string
          - 'null'
        tagIds:
          type: array
          items:
            $ref: '#/components/schemas/Id'
        subtotalSumAllAbove:
          type: boolean
        subtotalIsBold:
          type: boolean
        markupAccountFilter:
          type:
          - string
          - 'null'
        phaseData:
          type: object
          description: Upsert editable phase data keyed by phase id. Omitted fields are left unchanged.
          additionalProperties:
            $ref: '#/components/schemas/BudgetLinePhaseDataWrite'
      examples:
      - name: Camera Package (A-cam)
        notes: Confirmed with vendor 5/12
    BudgetDocumentPhaseDataValue:
      description: Raw editable phase-data value. Numbers stay numbers; formulas and variable references stay exact strings;
        missing value is null.
      type:
      - number
      - string
      - 'null'
    BudgetDocumentOvertime:
      type:
      - object
      - 'null'
      additionalProperties: false
      properties:
        mode:
          type:
          - string
          - 'null'
        flatAmount:
          type:
          - number
          - 'null'
        hours:
          type:
          - number
          - 'null'
        baseHours:
          type:
          - number
          - 'null'
        multipliers:
          description: Raw overtime multiplier detail.
        detail:
          description: Canonical overtime V2 detail, when present.
    BudgetDocumentLinePhaseData:
      type: object
      description: Editable estimate phase data for one line and phase. Notes and agent summaries are intentionally excluded.
      required:
      - rate
      - quantity
      - multiplier
      - qtyAutoDerived
      - unit
      - customUnitId
      - unitLabel
      - startDate
      - endDate
      - fringeIds
      - fringeTagIds
      - currencyId
      - overtime
      additionalProperties: false
      properties:
        rate:
          $ref: '#/components/schemas/BudgetDocumentPhaseDataValue'
        quantity:
          $ref: '#/components/schemas/BudgetDocumentPhaseDataValue'
        multiplier:
          $ref: '#/components/schemas/BudgetDocumentPhaseDataValue'
        qtyAutoDerived:
          type: boolean
        unit:
          type:
          - string
          - 'null'
        customUnitId:
          oneOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        unitLabel:
          type:
          - string
          - 'null'
        startDate:
          type:
          - string
          - 'null'
        endDate:
          type:
          - string
          - 'null'
        fringeIds:
          type: array
          items:
            $ref: '#/components/schemas/Id'
        fringeTagIds:
          type: array
          items:
            $ref: '#/components/schemas/Id'
        currencyId:
          oneOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        overtime:
          $ref: '#/components/schemas/BudgetDocumentOvertime'
    BudgetLinePhaseDataWrite:
      type: object
      description: Public write body for one editable line/phase data cell. Omitted fields are left unchanged on upsert.
      additionalProperties: false
      properties:
        rate:
          $ref: '#/components/schemas/BudgetDocumentPhaseDataValue'
        quantity:
          $ref: '#/components/schemas/BudgetDocumentPhaseDataValue'
        multiplier:
          $ref: '#/components/schemas/BudgetDocumentPhaseDataValue'
        qtyAutoDerived:
          type:
          - boolean
          - 'null'
        unit:
          type:
          - string
          - 'null'
        customUnitId:
          oneOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        startDate:
          type:
          - string
          - 'null'
        endDate:
          type:
          - string
          - 'null'
        fringeIds:
          oneOf:
          - type: array
            items:
              $ref: '#/components/schemas/Id'
          - type: 'null'
        fringeTagIds:
          oneOf:
          - type: array
            items:
              $ref: '#/components/schemas/Id'
          - type: 'null'
        currencyId:
          oneOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        overtime:
          $ref: '#/components/schemas/BudgetDocumentOvertime'
    BudgetLinePhaseDataUpsert:
      allOf:
      - $ref: '#/components/schemas/BudgetLinePhaseDataWrite'
    BudgetLinePhaseDataUpsertResponse:
      type: object
      required:
      - lineId
      - phaseId
      - phaseData
      additionalProperties: false
      properties:
        lineId:
          $ref: '#/components/schemas/Id'
        phaseId:
          $ref: '#/components/schemas/Id'
        phaseData:
          $ref: '#/components/schemas/BudgetDocumentLinePhaseData'
    BudgetLineBulkCreate:
      type: object
      required:
      - lines
      additionalProperties: false
      properties:
        lines:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/BudgetLineCreate'
    BudgetLineBulkCreateResponse:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/BudgetLine'
    BudgetLinePhaseDataBulkUpsert:
      type: object
      required:
      - items
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 500
          items:
            type: object
            required:
            - lineId
            - phaseId
            - phaseData
            additionalProperties: false
            properties:
              lineId:
                $ref: '#/components/schemas/Id'
              phaseId:
                $ref: '#/components/schemas/Id'
              phaseData:
                $ref: '#/components/schemas/BudgetLinePhaseDataWrite'
    BudgetLinePhaseDataBulkUpsertResponse:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/BudgetLinePhaseDataUpsertResponse'
    BudgetAccount:
      type: object
      description: A derived account view, a budget line where `kind=account`. Accounts are not a separate table; this is
        a projection for browsing the chart of accounts.
      required:
      - id
      - parentId
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        code:
          type:
          - string
          - 'null'
          description: Account number (the `accountId` classifier). Non-unique, may be empty.
        name:
          type: string
          description: Account name.
        parentId:
          $ref: '#/components/schemas/Id'
        path:
          type:
          - string
          - 'null'
          description: Coded account path.
        depth:
          type: integer
      examples:
      - id: lin_7c1a2f
        code: '1100'
        name: Camera
        parentId: lin_root
        path: '1100'
        depth: 0
    BudgetCell:
      type: object
      description: 'A positional cell value, read via explicit `account` + `column` params. This is a computed read like `/budget/totals`:
        `value` is the authoritative value as of `computedAt` (may be slightly stale, not real-time), and the response carries
        `Cache-Control` + opaque `ETag` headers for conditional GET. Echoes the resolved coordinates plus the value.'
      required:
      - account
      - column
      - computedAt
      - value
      additionalProperties: false
      properties:
        account:
          type: string
          description: The resolved account path/code the cell sits on (echoes the request param).
          examples:
          - 1100/1110
        column:
          type: string
          description: The value column read (echoes the request param).
          examples:
          - estimate
        lineId:
          $ref: '#/components/schemas/Id'
        computedAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp at which this cell value was computed. The value reflects state within the cache
            TTL window, not a real-time guarantee. Mirrors the `computedAt` contract of `/budget`, `/budget/totals`, `/budget/rollup`,
            `/budget/variance`.
        value:
          allOf:
          - $ref: '#/components/schemas/PhaseValues'
          description: The computed value matrix for the cell.
      examples:
      - account: 1100/1110
        column: estimate
        lineId: lin_7c1a2f
        computedAt: '2026-05-28T18:04:11Z'
        value:
          base: 1000000
          overtime: 0
          fringe: 0
          fringeBreakdown: []
          amount: 1000000
          combined: 1000000
          currency: USD
    BudgetDocumentPhaseValues:
      type: object
      description: The computed value matrix for one phase in the budget document. All amounts are integers in workspace-base
        minor units; `amount` is the total. The legacy `combined` alias is intentionally omitted from this document shape.
      required:
      - base
      - overtime
      - fringe
      - fringeBreakdown
      - amount
      - currency
      additionalProperties: false
      properties:
        base:
          type: integer
          format: int64
        overtime:
          type: integer
          format: int64
        fringe:
          type: integer
          format: int64
        fringeBreakdown:
          type: array
          items:
            $ref: '#/components/schemas/FringeBreakdownEntry'
        amount:
          type: integer
          format: int64
        currency:
          type: string
          minLength: 3
          maxLength: 3
    BudgetDocumentSelection:
      type: object
      description: Echo of the selector dimensions applied to the budget document.
      additionalProperties: false
      properties:
        path:
          type: string
        accountCode:
          type: string
        phase:
          type: string
    BudgetDocumentLine:
      type: object
      description: One flat line in the budget document. `values` are computed; `phaseData` contains raw editable estimate
        fields keyed by phase id.
      required:
      - id
      - name
      - parentId
      - depth
      - kind
      - values
      - phaseData
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        code:
          type:
          - string
          - 'null'
        name:
          type: string
        parentId:
          oneOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        path:
          type:
          - string
          - 'null'
        depth:
          type: integer
        kind:
          $ref: '#/components/schemas/ComputedLineKind'
        values:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/BudgetDocumentPhaseValues'
        phaseData:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/BudgetDocumentLinePhaseData'
    BudgetDocument:
      type: object
      description: The full public budget document, combining visible phases, computed totals, flat lines, and editable phase
        data in one response.
      required:
      - id
      - computedAt
      - phaseDataReadAt
      - selection
      - phases
      - totals
      - lines
      additionalProperties: false
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The budget id (`bud_\u2026`)."
        computedAt:
          type: string
          format: date-time
        phaseDataReadAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp after the raw phase-data rows were read.
        selection:
          $ref: '#/components/schemas/BudgetDocumentSelection'
        phases:
          type: array
          items:
            $ref: '#/components/schemas/BudgetPhase'
        totals:
          type: object
          description: Root or selected-subtree totals keyed by visible phase id.
          additionalProperties:
            $ref: '#/components/schemas/BudgetDocumentPhaseValues'
        lines:
          type: array
          description: Flat array of selected rows; rebuild the tree from `parentId`.
          items:
            $ref: '#/components/schemas/BudgetDocumentLine'
    BudgetTotals:
      type: object
      description: Computed grand totals (or, with supported filters, the computed slice total) as of `computedAt`. Supported
        filters are `phase`, `accountId`, and `path`. The filtered rollup is computed server-side; never summed client-side.
      required:
      - computedAt
      - totals
      additionalProperties: false
      properties:
        computedAt:
          type: string
          format: date-time
        totals:
          type: object
          description: Per-phase totals for the requested slice, keyed by phase id.
          additionalProperties:
            $ref: '#/components/schemas/PhaseValues'
        filter:
          type: object
          description: Echo of the applied filter slice (present when any filter was supplied), so the computed number is
            self-describing.
          additionalProperties: false
          properties:
            phase:
              type: string
            accountId:
              type: string
            path:
              type: string
      examples:
      - computedAt: '2026-05-28T18:04:11Z'
        totals:
          phs_estimate:
            base: 1000000
            overtime: 0
            fringe: 220000
            fringeBreakdown: []
            amount: 1220000
            combined: 1220000
            currency: USD
    BudgetRollup:
      type: object
      description: Computed rollup for a single phase, as of `computedAt`.
      required:
      - computedAt
      - phase
      - values
      additionalProperties: false
      properties:
        computedAt:
          type: string
          format: date-time
        phase:
          allOf:
          - $ref: '#/components/schemas/BudgetPhase'
          description: The phase the rollup is for.
        values:
          allOf:
          - $ref: '#/components/schemas/PhaseValues'
          description: The computed rollup value for the phase.
      examples:
      - computedAt: '2026-05-28T18:04:11Z'
        phase:
          id: phs_estimate
          type: estimate
          name: Estimate
          sort: 0
          isHidden: false
        values:
          base: 1000000
          overtime: 0
          fringe: 220000
          fringeBreakdown: []
          amount: 1220000
          combined: 1220000
          currency: USD
    BudgetVariance:
      type: object
      description: Computed variance between two phases (`to` minus `from`), as of `computedAt`. `delta` is the difference
        per value field in workspace-base minor units.
      required:
      - computedAt
      - from
      - to
      - delta
      additionalProperties: false
      properties:
        computedAt:
          type: string
          format: date-time
        from:
          allOf:
          - $ref: '#/components/schemas/PhaseValues'
          description: Values of the `from` phase.
        to:
          allOf:
          - $ref: '#/components/schemas/PhaseValues'
          description: Values of the `to` phase.
        delta:
          allOf:
          - $ref: '#/components/schemas/PhaseValues'
          description: "Per-field difference (`to` \u2212 `from`), computed."
      examples:
      - computedAt: '2026-05-28T18:04:11Z'
        from:
          base: 1000000
          overtime: 0
          fringe: 220000
          fringeBreakdown: []
          amount: 1220000
          combined: 1220000
          currency: USD
        to:
          base: 1100000
          overtime: 0
          fringe: 242000
          fringeBreakdown: []
          amount: 1342000
          combined: 1342000
          currency: USD
        delta:
          base: 100000
          overtime: 0
          fringe: 22000
          fringeBreakdown: []
          amount: 122000
          combined: 122000
          currency: USD
    DocumentTargetKind:
      type: string
      description: The kind of entity a document can be assigned to. v1 is single-target-per-kind (one reference per kind
        on the document). `contact` is workspace-scoped (contacts have no project), the rest are project-scoped.
      enum:
      - transaction
      - purchaseOrder
      - contact
      - project
      examples:
      - transaction
    DocumentStatus:
      type: string
      description: Document processing status. A new document is `PENDING` or `PROCESSING` until extraction finishes. `READY`
        means its content can be queried. `FAILED` means extraction did not finish. This field is read-only and set by Saturation.
      enum:
      - pending
      - processing
      - ready
      - failed
      examples:
      - READY
    DocumentCoarseType:
      type: string
      description: The coarse content category inferred during extraction.
      enum:
      - financial
      - tax
      - legal
      - insurance
      - production
      - other
      examples:
      - financial
    DocumentExpand:
      type: string
      description: "A single expand key valid on document responses. Pass as a comma list via `expand` (depth \u2264 2); an\
        \ unknown key returns `400 expand_invalid`. Expanded relations are permission-projected."
      enum:
      - transaction
      - contact
      - purchaseOrder
      - folder
    DocumentTargetRef:
      type: object
      description: A typed reference to the entity a document is (or should be) assigned to. The raw internal domain `address`
        is never used on the public surface, targets are always `{ kind, id }`.
      required:
      - kind
      - id
      additionalProperties: false
      properties:
        kind:
          $ref: '#/components/schemas/DocumentTargetKind'
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The target entity's canonical id, matching `kind` (`txn_\u2026`, `lin_\u2026`, `po_\u2026`, `con_\u2026\
            `, `prj_\u2026`)."
      examples:
      - kind: transaction
        id: txn_8f2a1c9e
    Document:
      type: object
      description: A document. Dropped once, then assigned to typed targets. Server-owned fields (`workspaceId`, `uploaderId`,
        `status`, `contentHash`, timestamps) are read-only; the per-kind assignment reference are surfaced as the typed `links`
        array, not as fields. The internal workspaceId / `soft-delete timestamp` are never exposed.
      required:
      - id
      - name
      - mimeType
      - size
      - status
      - workspaceId
      - links
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Canonical document id (`doc_\u2026`)."
        name:
          type: string
          description: The document's display name (file name). Writable via `PATCH`.
        mimeType:
          type: string
          description: The stored file's MIME type.
          examples:
          - application/pdf
        size:
          type: integer
          format: int64
          description: The stored file's size in bytes.
          examples:
          - 184320
        status:
          $ref: '#/components/schemas/DocumentStatus'
        workspaceId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The workspace this document belongs to (`ws_\u2026`). Server-owned, read-only."
        pageCount:
          type:
          - integer
          - 'null'
          description: Number of pages, once extraction has determined it. Null until `READY`.
        classification:
          type:
          - string
          - 'null'
          description: Fine-grained classification inferred during extraction (e.g. `invoice`, `receipt`).
        coarseType:
          description: Coarse content category. Null until extraction has classified the document.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/DocumentCoarseType'
          - type: 'null'
        hasSensitiveInfo:
          type:
          - boolean
          - 'null'
          description: Whether extraction flagged the document as containing sensitive information.
        description:
          type:
          - string
          - 'null'
          description: Optional free-text description. Writable via `PATCH`.
        source:
          type:
          - string
          - 'null'
          description: How the document entered the workspace (e.g. `api`, `agent`, `upload`). Read-only.
        folderId:
          description: "The folder this document lives in (`fld_\u2026`), or null at the workspace root. Writable via `PATCH`."
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        projectId:
          description: "The project this document is scoped to (`prj_\u2026`). Derived from a project-scoped assign target;\
            \ null for workspace-only (e.g. contact-attached) documents."
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        contentHash:
          type:
          - string
          - 'null'
          description: Content hash of the stored file (dedupe / caching). Read-only.
        links:
          type: array
          description: 'Every entity this document is currently assigned to, derived from the document''s non-null per-kind
            reference (single-reference v1: at most one per kind).'
          items:
            $ref: '#/components/schemas/DocumentTargetRef'
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: ISO-8601 last-modified timestamp.
      examples:
      - id: doc_4b1f0a7c
        name: vendor-invoice-0425.pdf
        mimeType: application/pdf
        size: 184320
        status: READY
        pageCount: 2
        classification: invoice
        coarseType: financial
        folderId: fld_9c2d
        projectId: prj_71aa
        links:
        - kind: transaction
          id: txn_8f2a1c9e
        createdAt: '2026-05-28T14:03:11Z'
        updatedAt: '2026-05-28T14:03:40Z'
    DocumentExtractedField:
      type: object
      description: One structured field extracted from a document, paired with the platform's confidence in the value so a
        consumer can threshold low-confidence reads.
      required:
      - value
      additionalProperties: false
      properties:
        value:
          type:
          - string
          - 'null'
          description: The extracted value, serialized as a string (money values are decimal strings; dates are ISO-8601).
            Null when the field was detected as a key but no value could be read.
        confidence:
          type:
          - number
          - 'null'
          description: Extraction confidence in `[0,1]`, or null when the platform does not score this field.
          minimum: 0
          maximum: 1
      examples:
      - value: B&H Photo
        confidence: 0.95
    DocumentExtractedLineItem:
      type: object
      description: One line item extracted from a document (e.g. an invoice row). Money is integer minor units + currency,
        like everywhere else.
      additionalProperties: false
      properties:
        description:
          type:
          - string
          - 'null'
        amount:
          description: The line amount as money, when extracted.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        confidence:
          type:
          - number
          - 'null'
          description: Extraction confidence in `[0,1]` for this line.
          minimum: 0
          maximum: 1
    DocumentExtraction:
      type: object
      description: The structured fields extracted from a document, the replacement for the legacy per-attachment parsed-document
        payload. Available only once the document's `status` is `READY`. Read-only; the platform owns extraction. `fields`
        is a flat, open map of named extracted values (the named keys vary by `classification`, e.g. an invoice carries `invoiceNumber`/`vendorName`/`total`),
        each a `DocumentExtractedField` with a confidence score.
      required:
      - documentId
      - status
      - fields
      additionalProperties: false
      properties:
        documentId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The document this extraction belongs to (`doc_\u2026`)."
        status:
          $ref: '#/components/schemas/DocumentStatus'
        classification:
          type:
          - string
          - 'null'
          description: Fine-grained classification that determines which `fields` keys are present (e.g. `invoice`, `receipt`).
        coarseType:
          description: Coarse content category (mirrors `Document.coarseType`).
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/DocumentCoarseType'
          - type: 'null'
        extractedAt:
          type:
          - string
          - 'null'
          format: date-time
          description: ISO-8601 timestamp extraction completed, or null if not yet available.
        fields:
          type: object
          description: Flat map of extracted field name to its value + confidence. The set of keys depends on `classification`;
            unknown keys are allowed (open map) so new classifications never break the contract.
          additionalProperties:
            $ref: '#/components/schemas/DocumentExtractedField'
        lineItems:
          type: array
          description: Line items extracted from the document, when applicable (e.g. invoice rows). Absent for documents with
            no itemized content.
          items:
            $ref: '#/components/schemas/DocumentExtractedLineItem'
      examples:
      - documentId: doc_4b1f0a7c
        status: READY
        classification: invoice
        coarseType: financial
        extractedAt: '2026-05-28T14:03:40Z'
        fields:
          invoiceNumber:
            value: '4471'
            confidence: 0.98
          total:
            value: '1529.00'
            confidence: 0.91
        lineItems:
        - description: Camera body rental - 3 days
          amount:
            amount: 75000
            currency: USD
          confidence: 0.86
    DocumentCollection:
      type: object
      description: A paginated page of documents.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Document'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when a permission/expand under-fetch bounded the page short of `limit`.
    DocumentList:
      description: Alias of `DocumentCollection` (the canonical paginated document page). Retained as a stable name for reverse-read
        responses on host resources (e.g. the contact-documents read) that reference `DocumentList`.
      allOf:
      - $ref: '#/components/schemas/DocumentCollection'
    DocumentLinkRequest:
      type: object
      description: Link a document to a target of the kind named in the path.
      required:
      - targetId
      additionalProperties: false
      properties:
        targetId:
          $ref: '#/components/schemas/Id'
        replace:
          type: boolean
          default: false
          description: Replace a different target already linked for this kind.
    DocumentCreateMetadata:
      type: object
      description: Client-supplied metadata for a drop. Sent as the `metadata` part of a `multipart/form-data` upload alongside
        the `file` part. Server-owned fields (`status`, `workspaceId`, `uploaderId`, `contentHash`, timestamps) are NOT accepted,
        supplying one returns `422 field_read_only`.
      additionalProperties: false
      properties:
        name:
          type: string
          description: Display name. Defaults to the uploaded file's name when omitted.
        description:
          type: string
          description: Optional free-text description.
        folderId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Folder to drop into (`fld_\u2026`). Omit for the workspace root."
        links:
          description: Optional target(s) to assign atomically with the drop. Accepts a single `{ kind, id }` or an array.
            In v1 every target must be a DISTINCT kind (one reference per kind); multiple same-kind targets return `400 validation`.
            If any target fails validation / permission / tenant checks, the whole request fails 4xx and NO document (and
            no orphan blob) is created.
          oneOf:
          - $ref: '#/components/schemas/DocumentTargetRef'
          - type: array
            items:
              $ref: '#/components/schemas/DocumentTargetRef'
            minItems: 1
      examples:
      - name: vendor-invoice-0425.pdf
        folderId: fld_9c2d
        links:
          kind: transaction
          id: txn_8f2a1c9e
    DocumentCreateRequest:
      type: object
      description: The `multipart/form-data` drop body. `file` is the raw bytes; `metadata` is a JSON part serialized as `DocumentCreateMetadata`.
      required:
      - file
      additionalProperties: false
      properties:
        file:
          type: string
          format: binary
          description: The document bytes to upload.
        metadata:
          $ref: '#/components/schemas/DocumentCreateMetadata'
    DocumentUpdateRequest:
      type: object
      description: Rename, move, or re-describe a document. Only client-writable fields are accepted. Naming a server-owned
        field returns `422 field_read_only`. Links are changed through `/documents/{documentId}/links/{kind}`.
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          description: New display name.
        description:
          type:
          - string
          - 'null'
          description: New description (null to clear).
        folderId:
          description: "Move to this folder (`fld_\u2026`), or null to move to the workspace root."
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
      examples:
      - name: vendor-invoice-april.pdf
        folderId: fld_9c2d
    PackVisibility:
      type: string
      description: Pack visibility. `PUBLIC` packs are Saturation-published (guild packs, no owning workspace); `PRIVATE`
        packs are owned by one workspace; `SHARED` is reserved. A `PRIVATE` pack owned by another workspace is invisible (reads
        return `404 not_found`).
      enum:
      - public
      - private
      - shared
    TagColor:
      type:
      - string
      - 'null'
      description: Tag colour as a Tailwind palette key (not a hex literal), or `null` for the neutral default. One of the
        19 allowed keys.
      enum:
      - slate
      - gray
      - zinc
      - red
      - orange
      - amber
      - yellow
      - lime
      - green
      - emerald
      - teal
      - cyan
      - sky
      - blue
      - indigo
      - violet
      - purple
      - pink
      - rose
      - null
    SourceType:
      type: string
      description: Origin of a project copy. `workspace` = copied from a workspace template (tracked by `sourceId`); `saturation`
        = derived from a Saturation incentive version (tracked by `sourceIncentiveVersionId`). Incentive copies are surfaced
        as "incentives".
      enum:
      - workspace
      - saturation
    SourceTombstone:
      type: object
      description: 'Returned by `expand=source` when the source library item/version was deleted. Source tracking is never
        `null`: a removed source resolves to this explicit tombstone so a reader can distinguish "source deleted" from "no
        source". Reflects that the source was deleted rather than that no source exists.'
      required:
      - id
      - deleted
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        deleted:
          type: boolean
          const: true
    LibraryExpand:
      type: string
      description: Allowed `expand` keys on project copies.
      enum:
      - source
    ProjectIncentiveExpand:
      type: string
      description: Allowed `expand` keys on a project incentive.
      enum:
      - source
      - components
    RatePackExpand:
      type: string
      description: Allowed `expand` key on a workspace-source rate pack. `items` inlines the pack's rate items (permission-projected).
      enum:
      - items
    IncentivePackExpand:
      type: string
      description: Allowed `expand` key on a workspace-source incentive pack. `programs` inlines the pack's programs.
      enum:
      - programs
    ProjectRatePackExpand:
      type: string
      description: Allowed `expand` key on a project-installed rate pack. `installedPack` inlines the source pack ref.
      enum:
      - installedPack
    RatePackVersionRef:
      type: object
      description: The pack's current (latest) version. Version history stays internal.
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        effectiveDate:
          type:
          - string
          - 'null'
          format: date
        itemCount:
          type: integer
          description: Count of non-blank items in the latest version.
    RatePack:
      type: object
      description: A rate pack visible to the workspace (`PUBLIC` or workspace-owned). Carries enablement state and the current
        version. Effective-dating / version history is not a public surface.
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        slug:
          type: string
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        publisher:
          type:
          - string
          - 'null'
        category:
          type: string
        tags:
          type: array
          items:
            type: string
        visibility:
          $ref: '#/components/schemas/PackVisibility'
        isOwned:
          type: boolean
          description: True when this workspace owns the (PRIVATE) pack.
        deprecated:
          type: boolean
          description: 'A superseded pack. Distinct from `soft-delete timestamp`: an enabled/installed deprecated pack still
            resolves and is surfaced flagged `true`; newly enabling/installing one is rejected (`400 validation`).'
        enabled:
          type: boolean
          description: True when a live workspace-enable link exists.
        enabledAt:
          type:
          - string
          - 'null'
          format: date-time
        enabledBy:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        latestVersion:
          $ref: '#/components/schemas/RatePackVersionRef'
        items:
          type: array
          description: Present only when `expand=items`. permission-projected.
          items:
            $ref: '#/components/schemas/RatePackItem'
      required:
      - id
      - slug
      - name
      - category
      - visibility
      - isOwned
      - deprecated
      - enabled
    RatePackCreate:
      type: object
      description: Author a PRIVATE workspace-owned rate pack. Auto-enabled for the creator. Server-owned fields (`id`, `slug`,
        `visibility`, `isOwned`, `enabled*`, `deprecated`, `latestVersion`, `soft-delete timestamp`) are not writable; sending
        one returns `422 field_read_only`.
      additionalProperties: false
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        category:
          type: string
          default: custom
        tags:
          type: array
          items:
            type: string
    RatePackUpdate:
      type: object
      description: Patch an owned rate pack. Only owned packs are patchable (else `404 not_found`).
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
        description:
          type:
          - string
          - 'null'
        category:
          type: string
        tags:
          type: array
          items:
            type: string
    RatePackItem:
      type: object
      description: A single rate line in a pack's current version.
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        title:
          type: string
        description:
          type:
          - string
          - 'null'
        rate:
          $ref: '#/components/schemas/Money'
        unit:
          type: string
        currency:
          type: string
          description: ISO-4217 currency code for the line's rate.
        quantity:
          type:
          - number
          - 'null'
        multiplier:
          type:
          - number
          - 'null'
        group:
          type:
          - string
          - 'null'
        agreement:
          type:
          - string
          - 'null'
        local:
          type:
          - string
          - 'null'
        effectiveDate:
          type:
          - string
          - 'null'
          format: date
        labels:
          type: array
          items:
            type: string
        displayTags:
          type: array
          items:
            type: string
        searchAliases:
          type: array
          items:
            type: string
        facets:
          type: object
          additionalProperties: true
        sourceRefs:
          type: array
          items:
            type: string
        qualityFlags:
          type: array
          items:
            type: string
        rateBasis:
          type:
          - string
          - 'null'
        version:
          $ref: '#/components/schemas/RatePackVersionRef'
      required:
      - id
      - title
      - rate
      - unit
      - currency
    RatePackItemCreate:
      type: object
      description: Add an item to an owned pack (auto-creates v1 if the pack has no version). `searchText` and `itemCount`
        are server-recomputed and not writable.
      additionalProperties: false
      required:
      - title
      - rate
      - unit
      properties:
        title:
          type: string
          minLength: 1
        rate:
          $ref: '#/components/schemas/Money'
        unit:
          type: string
        currency:
          type: string
          default: USD
        description:
          type: string
        quantity:
          type: number
        multiplier:
          type: number
        group:
          type: string
        agreement:
          type: string
        local:
          type: string
        effectiveDate:
          type: string
          format: date
        labels:
          type: array
          items:
            type: string
        displayTags:
          type: array
          items:
            type: string
        searchAliases:
          type: array
          items:
            type: string
        facets:
          type: object
          additionalProperties: true
        sourceRefs:
          type: array
          items:
            type: string
        qualityFlags:
          type: array
          items:
            type: string
        rateBasis:
          type: string
    RatePackItemUpdate:
      type: object
      description: Patch an item. `searchText`/`itemCount` are server-recomputed.
      additionalProperties: false
      minProperties: 1
      properties:
        title:
          type: string
          minLength: 1
        rate:
          $ref: '#/components/schemas/Money'
        unit:
          type: string
        currency:
          type: string
        description:
          type:
          - string
          - 'null'
        quantity:
          type:
          - number
          - 'null'
        multiplier:
          type:
          - number
          - 'null'
        group:
          type:
          - string
          - 'null'
        agreement:
          type:
          - string
          - 'null'
        local:
          type:
          - string
          - 'null'
        effectiveDate:
          type:
          - string
          - 'null'
          format: date
        labels:
          type: array
          items:
            type: string
        displayTags:
          type: array
          items:
            type: string
        searchAliases:
          type: array
          items:
            type: string
        facets:
          type: object
          additionalProperties: true
        sourceRefs:
          type: array
          items:
            type: string
        qualityFlags:
          type: array
          items:
            type: string
        rateBasis:
          type:
          - string
          - 'null'
    ProjectRatePack:
      type: object
      description: A rate pack installed into a project. `expand=installedPack` inlines the pack ref.
      additionalProperties: false
      required:
      - id
      - ratePackId
      - installedAt
      properties:
        id:
          $ref: '#/components/schemas/Id'
        ratePackId:
          $ref: '#/components/schemas/Id'
        installedAt:
          type: string
          format: date-time
        installedBy:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        deprecated:
          type: boolean
        installedPack:
          description: Present only when `expand=installedPack`.
          oneOf:
          - $ref: '#/components/schemas/RatePack'
          - $ref: '#/components/schemas/SourceTombstone'
    IncentivePack:
      type: object
      description: An incentive pack. Enable-only at the workspace scope (no project install link exists). Programs are surfaced
        via `/{packId}/programs`.
      additionalProperties: false
      required:
      - id
      - slug
      - name
      - jurisdiction
      - visibility
      - isOwned
      - deprecated
      - enabled
      properties:
        id:
          $ref: '#/components/schemas/Id'
        slug:
          type: string
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        category:
          type:
          - string
          - 'null'
        jurisdiction:
          type: string
          description: Jurisdiction the incentive applies to (e.g. country / state / province).
        visibility:
          $ref: '#/components/schemas/PackVisibility'
        isOwned:
          type: boolean
        deprecated:
          type: boolean
        enabled:
          type: boolean
        enabledAt:
          type:
          - string
          - 'null'
          format: date-time
        programs:
          type: array
          description: Present only when `expand=programs`.
          items:
            $ref: '#/components/schemas/IncentiveProgram'
    IncentiveProgram:
      type: object
      description: A program (rule) inside an incentive pack, the unit a project adds to become a project incentive. Only
        `published` programs/versions are addable.
      additionalProperties: false
      required:
      - id
      - name
      - incentiveType
      - status
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        jurisdiction:
          type:
          - string
          - 'null'
        incentiveType:
          type: string
          description: e.g. `tax-credit`, `rebate`, `grant`.
        incentiveStructure:
          type:
          - string
          - 'null'
          description: e.g. `transferable`, `refundable`, `non-refundable`.
        status:
          type: string
          enum:
          - draft
          - published
          - archived
        currentVersionId:
          description: The current published version's id, the value to pass as `versionId` on add.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
    ProjectIncentive:
      type: object
      description: 'A project incentive. The source is tracked by `sourceIncentiveVersionId` with `sourceType: saturation`
        (`sourceId` is null for these). `expand=source` resolves the version; `expand=components` inlines its component rows.'
      additionalProperties: false
      required:
      - id
      - name
      - sourceType
      - isApplied
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        jurisdiction:
          type:
          - string
          - 'null'
        incentiveType:
          type:
          - string
          - 'null'
        sourceType:
          $ref: '#/components/schemas/SourceType'
        sourceId:
          description: Null for saturation-derived incentives (use `sourceIncentiveVersionId`).
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        sourceIncentiveVersionId:
          description: The incentive version this project incentive was derived from.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        isApplied:
          type: boolean
          description: Whether the incentive is currently applied to the project's budget.
        accountNumber:
          type:
          - string
          - 'null'
          description: Account number used for topsheet tracking.
        rate:
          type:
          - number
          - 'null'
          description: Credit rate percentage (30 = 30%).
        appliedCreditCap:
          type:
          - integer
          - 'null'
          description: Overall ceiling on the total credit, in integer MINOR units like every money value on this contract
            (a $1,500,000 cap reads 150000000; whole-currency amounts only). Null = no cap.
        discountPercent:
          type:
          - number
          - 'null'
          description: Broker fee percentage deducted from the capped credit.
        tierOverrideKey:
          type:
          - string
          - 'null'
          description: Manual tier pin (null = auto-select the band from qualified spend).
        amount:
          description: The computed incentive amount, when applied.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        source:
          description: Present only when `expand=source`. The live incentive version/program, or a tombstone if the source
            was deleted, never `null`.
          oneOf:
          - $ref: '#/components/schemas/IncentiveProgram'
          - $ref: '#/components/schemas/SourceTombstone'
        components:
          type: array
          description: Present only when `expand=components`.
          items:
            $ref: '#/components/schemas/IncentiveComponent'
    IncentiveComponent:
      type: object
      description: A component row of a project incentive.
      additionalProperties: false
      required:
      - id
      - label
      properties:
        id:
          $ref: '#/components/schemas/Id'
        label:
          type: string
        amount:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        rate:
          type:
          - number
          - 'null'
    ProjectIncentiveAdd:
      type: object
      description: Add an incentive program (its current published version) into the project. Idempotent per project and incentive
        version. Rejected (`400 validation`) when the version/program/pack is not `published`, the pack is `deprecated`, or
        the pack is neither workspace-enabled nor workspace-owned.
      additionalProperties: false
      required:
      - programId
      properties:
        programId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: The incentive program to add.
        versionId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: Optional explicit published version. Defaults to the program's current published version when omitted.
    ProjectIncentiveUpdate:
      type: object
      description: 'Patch a project incentive at UI parity: apply / un-apply plus the parameter fields the product edits (rate,
        cap, discount, jurisdiction, type, account number, tier pin). Source tracking is read-only. `appliedCreditCap` is
        integer MINOR units like all contract money (a $1,500,000 cap is 150000000; whole-currency amounts only - multiples
        of 100); null clears it. `rate` and `discountPercent` are percentages (30 = 30%).'
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
        isApplied:
          type: boolean
        accountNumber:
          type: string
        incentiveType:
          type: string
          enum:
          - tax_credit
          - rebate
          - grant
        jurisdiction:
          type: string
        rate:
          type: number
          minimum: 0
          maximum: 100
        appliedCreditCap:
          type:
          - integer
          - 'null'
          minimum: 0
          multipleOf: 100
        discountPercent:
          type: number
          minimum: 0
          maximum: 100
        tierOverrideKey:
          type:
          - string
          - 'null'
    Fringe:
      type: object
      description: A workspace fringe template (the source; `projectId` is null).
      additionalProperties: false
      required:
      - id
      - name
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        rate:
          type:
          - number
          - 'null'
          description: Fringe rate as a fraction (e.g. 0.0765), or null.
        cap:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        sort:
          type:
          - number
          - 'null'
    FringeWrite:
      type: object
      description: Create/patch a workspace fringe template. Server-owned fields (`id`, `projectId`, `sourceId`, `soft-delete
        timestamp`) are not writable; sending one returns `422 field_read_only`.
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
        rate:
          type:
          - number
          - 'null'
        cap:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        sort:
          type: number
    ProjectFringe:
      type: object
      description: 'A project fringe copy. Carries `sourceId` (the template it was copied from) with `sourceType: workspace`.
        Editing it diverges the copy while keeping the link to its source.'
      additionalProperties: false
      required:
      - id
      - name
      - sourceType
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        rate:
          type:
          - number
          - 'null'
        cap:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        sort:
          type:
          - number
          - 'null'
        sourceType:
          $ref: '#/components/schemas/SourceType'
        sourceId:
          description: The workspace template this copy derives from.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        source:
          description: 'Present only when `expand=source`: the live template, or a tombstone if deleted, never `null`.'
          oneOf:
          - $ref: '#/components/schemas/Fringe'
          - $ref: '#/components/schemas/SourceTombstone'
    Global:
      type: object
      description: A workspace global template (source).
      additionalProperties: false
      required:
      - id
      - name
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        value:
          type:
          - number
          - 'null'
        unit:
          type:
          - string
          - 'null'
        sort:
          type:
          - number
          - 'null'
    GlobalWrite:
      type: object
      description: Create/patch a workspace global template. Server-owned fields are not writable.
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
        value:
          type:
          - number
          - 'null'
        unit:
          type:
          - string
          - 'null'
        sort:
          type: number
    ProjectGlobal:
      type: object
      description: A project global copy. Carries `sourceId` source tracking.
      additionalProperties: false
      required:
      - id
      - name
      - sourceType
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        value:
          type:
          - number
          - 'null'
        unit:
          type:
          - string
          - 'null'
        sort:
          type:
          - number
          - 'null'
        sourceType:
          $ref: '#/components/schemas/SourceType'
        sourceId:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        source:
          oneOf:
          - $ref: '#/components/schemas/Global'
          - $ref: '#/components/schemas/SourceTombstone'
    Currency:
      type: object
      description: A workspace currency template (source; an FX/exchange-rate entry).
      additionalProperties: false
      required:
      - id
      - code
      properties:
        id:
          $ref: '#/components/schemas/Id'
        code:
          type: string
          description: ISO-4217 currency code.
          minLength: 3
          maxLength: 3
        name:
          type:
          - string
          - 'null'
        rate:
          type:
          - number
          - 'null'
          description: Exchange rate relative to the project's base currency.
        sort:
          type:
          - number
          - 'null'
    CurrencyWrite:
      type: object
      description: Create/patch a workspace currency template. Server-owned fields are not writable.
      additionalProperties: false
      properties:
        code:
          type: string
          minLength: 3
          maxLength: 3
        name:
          type:
          - string
          - 'null'
        rate:
          type:
          - number
          - 'null'
          description: 'USD per ONE unit of this currency (rate_to_usd): EUR ~1.17, JPY ~0.0064. A rate quoted as units-per-USD
            ("23.1 CZK to the dollar") must be INVERTED before writing (23.1 CZK/USD -> 0.0433).'
        sort:
          type: number
    ProjectCurrency:
      type: object
      description: A project currency copy. Carries `sourceId` source tracking.
      additionalProperties: false
      required:
      - id
      - code
      - sourceType
      properties:
        id:
          $ref: '#/components/schemas/Id'
        code:
          type: string
          minLength: 3
          maxLength: 3
        name:
          type:
          - string
          - 'null'
        rate:
          type:
          - number
          - 'null'
        sort:
          type:
          - number
          - 'null'
        sourceType:
          $ref: '#/components/schemas/SourceType'
        sourceId:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        source:
          oneOf:
          - $ref: '#/components/schemas/Currency'
          - $ref: '#/components/schemas/SourceTombstone'
    FringeGroup:
      type: object
      description: A workspace fringe-tag template (groups fringes). Uses the same `projectId` / `sourceId` pattern as the
        other templates.
      additionalProperties: false
      required:
      - id
      - name
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        color:
          $ref: '#/components/schemas/TagColor'
        sort:
          type:
          - number
          - 'null'
    FringeGroupWrite:
      type: object
      description: Create/patch a workspace fringe-tag template. Server-owned fields are not writable.
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
        color:
          $ref: '#/components/schemas/TagColor'
        sort:
          type: number
    ProjectFringeGroup:
      type: object
      description: A project fringe-tag copy. Carries `sourceId` source tracking.
      additionalProperties: false
      required:
      - id
      - name
      - sourceType
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        color:
          $ref: '#/components/schemas/TagColor'
        sort:
          type:
          - number
          - 'null'
        sourceType:
          $ref: '#/components/schemas/SourceType'
        sourceId:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        source:
          oneOf:
          - $ref: '#/components/schemas/FringeGroup'
          - $ref: '#/components/schemas/SourceTombstone'
    Tag:
      type: object
      description: A workspace tag. It has no `projectId` or `sourceId`. A project gets tags via an association (see project
        tags), never a project copy.
      additionalProperties: false
      required:
      - id
      - name
      - createdAt
      - updatedAt
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        color:
          $ref: '#/components/schemas/TagColor'
        description:
          type:
          - string
          - 'null'
        eligibilityKey:
          type:
          - string
          - 'null'
          description: Optional stable key used to dedupe / match a tag's eligibility role.
        sort:
          type:
          - number
          - 'null'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TagCreate:
      type: object
      description: Create a workspace tag. Idempotent on `id` if one is supplied (else server-assigned). `createdAt`/`updatedAt`
        are server-owned.
      additionalProperties: false
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
        color:
          $ref: '#/components/schemas/TagColor'
        description:
          type: string
        eligibilityKey:
          type: string
        sort:
          type: number
    TagUpdate:
      type: object
      description: Patch a workspace tag.
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
        color:
          $ref: '#/components/schemas/TagColor'
        description:
          type:
          - string
          - 'null'
        eligibilityKey:
          type:
          - string
          - 'null'
        sort:
          type: number
    ProjectTagAttach:
      type: object
      description: 'Attach a workspace tag to a project (creates or reactivates the association). If the tag id does not exist,
        supply `tag` to create the workspace tag first. There is no `/detach` in v1: the association is auto-reconciled from
        line/transaction tag assignments, so a manual detach would be silently re-created.'
      additionalProperties: false
      properties:
        tag:
          $ref: '#/components/schemas/TagCreate'
    Unit:
      type: object
      description: A workspace Library unit. Built-ins are readable and immutable. Custom units use the same shape and are
        writable.
      additionalProperties: false
      required:
      - id
      - label
      - isCustom
      properties:
        id:
          type: string
          description: The built-in key or custom unit id.
        label:
          type: string
        description:
          type:
          - string
          - 'null'
        isCustom:
          type: boolean
        createdAt:
          type: string
          format: date-time
          description: Present on custom units only.
        updatedAt:
          type: string
          format: date-time
          description: Present on custom units only.
    UnitCreate:
      type: object
      description: Create a workspace custom unit. `id`/timestamps are server-owned.
      additionalProperties: false
      required:
      - label
      properties:
        label:
          type: string
          minLength: 1
        description:
          type: string
    UnitUpdate:
      type: object
      description: Patch a workspace custom unit.
      additionalProperties: false
      minProperties: 1
      properties:
        label:
          type: string
          minLength: 1
        description:
          type:
          - string
          - 'null'
    RatePackCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/RatePack'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    RatePackItemCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/RatePackItem'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    IncentivePackCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/IncentivePack'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    IncentiveProgramCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/IncentiveProgram'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    ProjectRatePackCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProjectRatePack'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    ProjectIncentiveCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProjectIncentive'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    FringeCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Fringe'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    ProjectFringeCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProjectFringe'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    GlobalCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Global'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    ProjectGlobalCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProjectGlobal'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    CurrencyCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Currency'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    ProjectCurrencyCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProjectCurrency'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    FringeGroupCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/FringeGroup'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    ProjectFringeGroupCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProjectFringeGroup'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    TagCollection:
      type: object
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Tag'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion limits returned fewer than `limit` items for this page.
    UnitCollection:
      type: object
      description: Built-in + custom units. Not paginated (the set is small).
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Unit'
    RatePackEnableLink:
      type: object
      description: The workspace-enable link (idempotent, deterministic id).
      additionalProperties: false
      required:
      - id
      - ratePackId
      - enabledAt
      properties:
        id:
          $ref: '#/components/schemas/Id'
        ratePackId:
          $ref: '#/components/schemas/Id'
        enabledAt:
          type: string
          format: date-time
    IncentivePackEnableLink:
      type: object
      description: The workspace-enable link for an incentive pack.
      additionalProperties: false
      required:
      - id
      - incentivePackId
      - enabledAt
      properties:
        id:
          $ref: '#/components/schemas/Id'
        incentivePackId:
          $ref: '#/components/schemas/Id'
        enabledAt:
          type: string
          format: date-time
    ProjectStatus:
      type: string
      description: Project status. `active` is a committed, working project; `staged` is a draft not yet committed (created
        by an import or onboarding flow); `archived` is closed but retained. Marked extensible so a future status value does
        not break clients.
      x-extensible-enum: true
      enum:
      - active
      - staged
      - archived
    Project:
      type: object
      description: "A project, the unit a budget, transactions, documents and the project-project Library hang off. Addressable\
        \ by canonical `id` (`prj_\u2026`) or by `slug` (the human handle, unique per workspace)."
      required:
      - id
      - slug
      - name
      - status
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        slug:
          type: string
          description: The project's human handle, unique per workspace.
          examples:
          - q3-feature-doc
        name:
          type: string
          description: Display name.
          examples:
          - Q3 Feature Doc
        emoji:
          type:
          - string
          - 'null'
          description: Optional emoji shown next to the project in the UI.
        image:
          type:
          - string
          - 'null'
          description: Optional cover image URL.
        status:
          $ref: '#/components/schemas/ProjectStatus'
        summary:
          type:
          - string
          - 'null'
          description: A short, generated 1-2 sentence description of the project.
        spaceId:
          description: The space (folder) this project belongs to, or `null` if ungrouped.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 creation timestamp (serialized from the internal epoch).
        updatedAt:
          type: string
          format: date-time
          description: ISO-8601 last-modified timestamp.
    ProjectCreate:
      type: object
      description: Create a project. Server-owned fields (`id`, `workspaceId`, `status`, timestamps) are never client-writable;
        sending one returns `422 field_read_only`.
      required:
      - name
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          examples:
          - Q3 Feature Doc
        slug:
          type: string
          description: Optional human handle (unique per workspace). Generated from `name` when omitted.
        emoji:
          type:
          - string
          - 'null'
        image:
          type:
          - string
          - 'null'
        summary:
          type:
          - string
          - 'null'
        spaceId:
          description: Optional space to file the project under.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
    ProjectUpdate:
      type: object
      description: Patch a project (partial; only the supplied fields change). `status` accepts only `active`/`archived` here;
        `staged` is set by import and onboarding flows and is not client-settable.
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
        slug:
          type: string
        emoji:
          type:
          - string
          - 'null'
        image:
          type:
          - string
          - 'null'
        summary:
          type:
          - string
          - 'null'
        spaceId:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        status:
          type: string
          enum:
          - active
          - archived
    ProjectList:
      type: object
      description: A page of projects.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Project'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or an expand returned fewer rows than `limit`.
    Space:
      type: object
      description: "A space, a folder that groups projects (optionally nested under a parent space). Addressable by canonical\
        \ `id` (`spc_\u2026`)."
      required:
      - id
      - slug
      - name
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        slug:
          type: string
          description: The space's human handle (unique per workspace).
          examples:
          - features
        name:
          type: string
          examples:
          - Features
        image:
          type:
          - string
          - 'null'
        parentId:
          description: Parent space for nesting, or `null` for a top-level space.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        archived:
          type: boolean
          description: Whether the space is archived (closed but retained).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SpaceCreate:
      type: object
      description: Create a space. `id`, `workspaceId` and timestamps are server-owned.
      required:
      - name
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          examples:
          - Features
        slug:
          type: string
          description: Optional human handle (unique per workspace); generated from `name` when omitted.
        image:
          type:
          - string
          - 'null'
        parentId:
          description: Optional parent space for nesting.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
    SpaceUpdate:
      type: object
      description: Patch a space (partial).
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
        slug:
          type: string
        image:
          type:
          - string
          - 'null'
        parentId:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        archived:
          type: boolean
    SpaceList:
      type: object
      description: A page of spaces.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Space'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or an expand returned fewer rows than `limit`.
    ContactType:
      type: string
      description: The kind of payee. `vendor` is a supplier organization, distinct from `contractor` (a 1099 person) and
        `employee` (a W-2 person); the crew/person filters treat contractor and employee as people and vendor as not.
      enum:
      - contractor
      - employee
      - company
      - vendor
    Contact:
      type: object
      description: A workspace-scoped contact (vendor, crew member or payee). Sensitive tax / bank numbers are never returned,
        only display-safe last-four hints and the `track1099` flag.
      required:
      - id
      - displayName
      - track1099
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        displayName:
          type: string
          description: The contact's display title (company name for companies, person name otherwise).
          examples:
          - Aerial Imaging LLC
        name:
          type:
          - string
          - 'null'
          description: Person / primary name.
        company:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
          description: Role / job title, e.g. "Director of Photography".
        type:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/ContactType'
          - type: 'null'
        email:
          type:
          - string
          - 'null'
        phone:
          type:
          - string
          - 'null'
        address:
          type:
          - string
          - 'null'
          description: Free-form address as entered.
        website:
          type:
          - string
          - 'null'
        instagram:
          type:
          - string
          - 'null'
        imdb:
          type:
          - string
          - 'null'
        defaultRate:
          description: Default day/labour rate for this contact, if set.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        track1099:
          type: boolean
          description: Whether this contact is tracked for 1099 reporting.
        taxIdLast4:
          type:
          - string
          - 'null'
          description: Last four digits of the tax id (display-safe hint only). The full tax id is never returned by the API.
        hasTaxId:
          type: boolean
          description: Whether a tax id is on file (derived; the value itself is never exposed).
        notes:
          type:
          - string
          - 'null'
          description: Markdown notes.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        documents:
          type: array
          description: Present only when `expand=documents`. Documents assigned to this contact, filtered by your permissions
            (may be partial; see the `truncated` flag).
          items:
            $ref: '#/components/schemas/Id'
        transactions:
          type: array
          description: Present only when `expand=transactions`. Transactions for this contact across the projects you can
            access, filtered by your permissions.
          items:
            $ref: '#/components/schemas/Id'
    ContactCreate:
      type: object
      description: Create a contact. Full tax and bank numbers cannot be set through this endpoint; they are extracted from
        documents. `id`, `workspaceId`, `displayName` (derived) and timestamps are server-owned.
      required:
      - name
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
        company:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
        type:
          $ref: '#/components/schemas/ContactType'
        email:
          type:
          - string
          - 'null'
        phone:
          type:
          - string
          - 'null'
        address:
          type:
          - string
          - 'null'
        website:
          type:
          - string
          - 'null'
        instagram:
          type:
          - string
          - 'null'
        imdb:
          type:
          - string
          - 'null'
        defaultRate:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        track1099:
          type: boolean
        notes:
          type:
          - string
          - 'null'
    ContactUpdate:
      type: object
      description: Patch a contact (partial).
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
        company:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
        type:
          $ref: '#/components/schemas/ContactType'
        email:
          type:
          - string
          - 'null'
        phone:
          type:
          - string
          - 'null'
        address:
          type:
          - string
          - 'null'
        website:
          type:
          - string
          - 'null'
        instagram:
          type:
          - string
          - 'null'
        imdb:
          type:
          - string
          - 'null'
        defaultRate:
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Money'
          - type: 'null'
        track1099:
          type: boolean
        notes:
          type:
          - string
          - 'null'
    ContactList:
      type: object
      description: A page of contacts.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Contact'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or an expand returned fewer rows than `limit`.
    ContactExpand:
      type: string
      description: The expand values allowed for contacts.
      enum:
      - documents
      - transactions
    CommentTargetKind:
      type: string
      description: The kind of entity a comment is anchored to.
      enum:
      - project
      - budgetLine
      - transaction
      - purchaseOrder
      - contact
      - document
    CommentTarget:
      type: object
      description: The entity the comment is anchored to, given as `{ kind, id }`. The same target shape is used for document
        assignment.
      required:
      - kind
      - id
      additionalProperties: false
      properties:
        kind:
          $ref: '#/components/schemas/CommentTargetKind'
        id:
          $ref: '#/components/schemas/Id'
    Comment:
      type: object
      description: A comment anchored to an entity within a project. Replies thread under a root comment via `threadId`.
      required:
      - id
      - projectId
      - authorId
      - content
      - resolved
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        projectId:
          $ref: '#/components/schemas/Id'
        threadId:
          description: The root comment id when this is a reply; `null` for a root comment.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
        target:
          description: The entity this comment is anchored to, if any.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/CommentTarget'
          - type: 'null'
        authorId:
          $ref: '#/components/schemas/Id'
        authorName:
          type:
          - string
          - 'null'
        content:
          type: string
          description: Markdown comment body.
        resolved:
          type: boolean
          description: Whether the comment / thread is marked resolved.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CommentCreate:
      type: object
      description: Create a comment. `projectId` is taken from the target's project; `authorId`, `authorName` and timestamps
        are server-owned (the author is the authenticated caller).
      required:
      - content
      - target
      additionalProperties: false
      properties:
        content:
          type: string
          minLength: 1
        target:
          $ref: '#/components/schemas/CommentTarget'
        threadId:
          description: Set to reply under an existing root comment.
          anyOf:
          - allOf:
            - $ref: '#/components/schemas/Id'
          - type: 'null'
    CommentUpdate:
      type: object
      description: Patch a comment, edit the body or toggle resolution. Only the author may edit `content`; re-anchoring (`target`)
        is not allowed.
      additionalProperties: false
      minProperties: 1
      properties:
        content:
          type: string
          minLength: 1
        resolved:
          type: boolean
    CommentList:
      type: object
      description: A page of comments.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Comment'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or an expand returned fewer rows than `limit`.
    ViewFormat:
      type: string
      description: The render shape a view resolves into.
      enum:
      - table
      - board
      - calendar
      - timeline
      - gallery
    ViewVisibility:
      type: string
      description: Who can see the view.
      enum:
      - personal
      - workspace
    View:
      type: object
      description: A saved view, a named, persisted filter + shape bundle over a subject (e.g. transactions or budget lines).
        A view is the deep, persisted equivalent of inline filters; `GET /views/{viewId}/data` resolves it and inline filters
        layer on top.
      required:
      - id
      - projectId
      - name
      - subjectType
      - format
      - visibility
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        projectId:
          $ref: '#/components/schemas/Id'
        name:
          type: string
          examples:
          - VFX over budget
        description:
          type:
          - string
          - 'null'
        subjectType:
          type: string
          description: The resource the view filters over (e.g. `transactions`, `budgetLines`).
          examples:
          - transactions
        format:
          $ref: '#/components/schemas/ViewFormat'
        visibility:
          $ref: '#/components/schemas/ViewVisibility'
        filter:
          type: object
          description: The persisted deep filter (operators, AND/OR groups, negation) the view replays. Opaque to the client;
            resolved server-side by `/views/{viewId}/data`.
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ViewList:
      type: object
      description: A page of saved views for the project.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/View'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or an expand returned fewer rows than `limit`.
    ViewData:
      type: object
      description: The resolved rows of a view, with the view's saved filter applied and any inline `expand` / flat filters
        layered on top. The row shape matches the view's `subjectType` resource; pagination follows the standard response
        format.
      required:
      - data
      - subjectType
      additionalProperties: false
      properties:
        subjectType:
          type: string
          description: The resource type of the rows (echoed so the client can type the payload).
          examples:
          - transactions
        data:
          type: array
          description: The resolved rows (shape determined by `subjectType`).
          items:
            type: object
            additionalProperties: true
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or an expand returned fewer rows than `limit`.
    HealthStatus:
      type: object
      description: 'Liveness / readiness of the public API edge. Unauthenticated. `ok` reflects whether the dedicated database
        pool is reachable; a degraded edge returns `ok: false` with `503`, never a misleading `200`.'
      required:
      - status
      - time
      additionalProperties: false
      properties:
        status:
          type: string
          enum:
          - ok
          - degraded
          description: '`ok` when the edge can reach its dedicated database pool; `degraded` when a dependency check failed
            (served with HTTP `503`).'
        time:
          type: string
          format: date-time
          description: Server time the check ran (ISO-8601).
        version:
          type: string
          description: The deployed `/v1` contract version (e.g. `1.0.0`).
        sha:
          type: string
          description: Git commit SHA of the deployed build, stamped by the deployment pipeline. Absent when no release SHA
            was stamped (e.g. local dev). The release workflow polls this to verify the public edge has cut over to the promoted
            commit.
      example:
        status: ok
        time: '2026-05-28T17:04:09.000Z'
        version: 1.0.0
        sha: 9c2b04878d1f4c1f0e2a7b6d5c4e3f2a1b0c9d8e
    PrincipalType:
      type: string
      description: What kind of principal a token acts as. A `user` token inherits a person's live workspace ability; a `service`
        token acts as a service identity that carries its own live-resolved role and survives staff churn.
      enum:
      - user
      - service
    WorkspaceReach:
      type: object
      description: The workspace a token can act on, paired with the principal's current role there. Roles resolve live per
        request, never a frozen claim. Internal teamId / teamRole are never exposed.
      required:
      - workspaceId
      - workspaceRole
      additionalProperties: false
      properties:
        workspaceId:
          $ref: '#/components/schemas/Id'
        workspaceRole:
          $ref: '#/components/schemas/WorkspaceRole'
    WorkspaceRole:
      type: string
      description: The principal's role in a workspace, mirroring the product's role vocabulary. Ordering for any ceiling
        check is `owner > admin > accountant > edit > comment > view`.
      enum:
      - owner
      - admin
      - accountant
      - edit
      - comment
      - view
    Me:
      type: object
      description: The token's own identity and workspace, the first call any integration makes and the Zapier / n8n auth-probe.
        No path params; works before any workspace is known. Tokens list only their bound workspace. Never leaks teamId /
        teamRole.
      required:
      - id
      - type
      - workspaces
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        type:
          $ref: '#/components/schemas/PrincipalType'
        email:
          type: string
          format: email
          description: The acting user's email. Present for `user` principals; omitted for `service` principals (which have
            no email).
        name:
          type: string
          description: Display name of the acting principal, the person's name for a `user` token, the service identity's
            label for a `service` token.
        workspaces:
          type: array
          description: The token's bound workspace, permission-filtered to the principal's live reach, with the principal's
            current `workspaceRole`.
          items:
            $ref: '#/components/schemas/WorkspaceReach'
      example:
        id: usr_4f1a8c2e
        type: user
        email: producer@example.com
        name: Dana Producer
        workspaces:
        - workspaceId: ws_2b9d7a1f
          workspaceRole: admin
    WorkspacePlan:
      type: string
      description: The workspace's current plan tier (mirrors the product's plan names).
      enum:
      - free
      - creator
      - studio
      - enterprise
    Workspace:
      type: object
      description: "A workspace the token can act on, with the principal's current role. The internal workspaceId is renamed\
        \ to `workspaceId` at the boundary and never leaks; likewise workspaceRole \u2192 `workspaceRole`."
      required:
      - id
      - name
      - workspaceRole
      - createdAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
          description: The workspace's display name.
        slug:
          type: string
          description: URL-friendly workspace handle, where one exists.
        workspaceRole:
          $ref: '#/components/schemas/WorkspaceRole'
        plan:
          $ref: '#/components/schemas/WorkspacePlan'
        apiAccess:
          type: boolean
          description: Whether the public API is enabled for this workspace's plan (`checkFeatureAccess`). When `false`, `/v1`
            resource calls return `403 feature_not_available`.
        createdAt:
          type: string
          format: date-time
          description: When the workspace was created (ISO-8601; epoch serialized at the boundary).
      example:
        id: ws_2b9d7a1f
        name: Paradigm Pictures
        slug: paradigm
        workspaceRole: admin
        plan: studio
        apiAccess: true
        createdAt: '2025-11-02T09:14:00.000Z'
    WorkspaceCollection:
      type: object
      description: A paginated page of workspaces (cap-100; default 50).
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Workspace'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when a permission/expand under-fetch bounded the page short of `limit`.
    PaymentRequestStatus:
      type: string
      description: 'Where a request stands now. `unknown` means the server could not classify the stored status and will not
        guess: it is not a lifecycle state and no request transitions into it. Treat it as "ask the workspace", never as a
        decision. Clients switching exhaustively must handle it.'
      enum:
      - submitted
      - pending
      - approved
      - paid
      - rejected
      - canceled
      - unknown
    PaymentStatus:
      type: string
      description: Where a payment stands now.
      enum:
      - requested
      - needs_approval
      - scheduled
      - sending
      - settled
      - returned
      - failed
      - rejected
      - canceled
    PaymentRequestExpand:
      type: string
      description: A linked record to include with a payment request.
      enum:
      - contact
      - purchaseOrder
      - document
      - payment
      - transactions
      - budgetLine
    PaymentExpand:
      type: string
      description: A linked record to include with a payment.
      enum:
      - request
      - purchaseOrder
      - transactions
      - contact
      - budgetLine
      - document
    PaymentContactLink:
      type: object
      required:
      - id
      - name
      - displayTitle
      - company
      - email
      - type
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type:
          - string
          - 'null'
        displayTitle:
          type:
          - string
          - 'null'
        company:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
        type:
          type:
          - string
          - 'null'
    PaymentPurchaseOrderLink:
      type: object
      required:
      - id
      - projectId
      - number
      - title
      - status
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        projectId:
          type:
          - string
          - 'null'
        number:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
        status:
          type: string
    PaymentDocumentLink:
      type: object
      required:
      - id
      - projectId
      - name
      - mimeType
      - status
      - size
      - classification
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        projectId:
          type:
          - string
          - 'null'
        name:
          type: string
        mimeType:
          type:
          - string
          - 'null'
        status:
          type: string
        size:
          type: integer
        classification:
          type:
          - string
          - 'null'
    PaymentBudgetLineLink:
      type: object
      required:
      - id
      - accountId
      - description
      - type
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        accountId:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        type:
          type:
          - string
          - 'null'
    PaymentTransactionLink:
      type: object
      required:
      - id
      - projectId
      - amount
      - description
      - status
      - timestamp
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        projectId:
          type:
          - string
          - 'null'
        amount:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        description:
          type:
          - string
          - 'null'
        status:
          type: string
        timestamp:
          type:
          - string
          - 'null'
          format: date-time
    PaymentRequestBase:
      type: object
      description: A request to pay a person or company. It is separate from the payment that follows.
      required:
      - id
      - workspaceId
      - projectId
      - purchaseOrderId
      - contactId
      - budgetLineId
      - amount
      - description
      - memo
      - dueDate
      - method
      - status
      - submittedAt
      - approvedAt
      - rejectedAt
      - completedAt
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        workspaceId:
          $ref: '#/components/schemas/Id'
        projectId:
          type:
          - string
          - 'null'
        purchaseOrderId:
          type:
          - string
          - 'null'
        contactId:
          type:
          - string
          - 'null'
        budgetLineId:
          type:
          - string
          - 'null'
        amount:
          $ref: '#/components/schemas/Money'
        description:
          type:
          - string
          - 'null'
        memo:
          type:
          - string
          - 'null'
        dueDate:
          type:
          - string
          - 'null'
          format: date-time
        method:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/PaymentRequestStatus'
        submittedAt:
          type: string
          format: date-time
        approvedAt:
          type:
          - string
          - 'null'
          format: date-time
        rejectedAt:
          type:
          - string
          - 'null'
          format: date-time
        completedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    PaymentBase:
      type: object
      description: One payment from request through settlement.
      required:
      - id
      - workspaceId
      - projectId
      - paymentRequestId
      - purchaseOrderId
      - contactId
      - budgetLineId
      - status
      - amount
      - destinationAmount
      - rail
      - description
      - memo
      - notes
      - scheduledFor
      - expectedArrivalAt
      - destination
      - cancellationRequested
      - needsSupport
      - failureReason
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        workspaceId:
          $ref: '#/components/schemas/Id'
        projectId:
          type:
          - string
          - 'null'
        paymentRequestId:
          type:
          - string
          - 'null'
        purchaseOrderId:
          type:
          - string
          - 'null'
        contactId:
          type:
          - string
          - 'null'
        budgetLineId:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/PaymentStatus'
        amount:
          $ref: '#/components/schemas/Money'
        destinationAmount:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        rail:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        memo:
          type:
          - string
          - 'null'
        notes:
          type:
          - string
          - 'null'
        scheduledFor:
          type:
          - string
          - 'null'
          format: date-time
        expectedArrivalAt:
          type:
          - string
          - 'null'
          format: date-time
        destination:
          oneOf:
          - type: object
            required:
            - bankName
            - accountType
            - accountLast4
            - network
            additionalProperties: false
            properties:
              bankName:
                type:
                - string
                - 'null'
              accountType:
                type:
                - string
                - 'null'
              accountLast4:
                type:
                - string
                - 'null'
              network:
                type:
                - string
                - 'null'
          - type: 'null'
        cancellationRequested:
          type: boolean
        needsSupport:
          type: boolean
        failureReason:
          type:
          - string
          - 'null'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    PaymentRequest:
      allOf:
      - $ref: '#/components/schemas/PaymentRequestBase'
      - type: object
        properties:
          contact:
            oneOf:
            - $ref: '#/components/schemas/PaymentContactLink'
            - type: 'null'
          purchaseOrder:
            oneOf:
            - $ref: '#/components/schemas/PaymentPurchaseOrderLink'
            - type: 'null'
          document:
            oneOf:
            - $ref: '#/components/schemas/PaymentDocumentLink'
            - type: 'null'
          payment:
            oneOf:
            - $ref: '#/components/schemas/PaymentBase'
            - type: 'null'
          transactions:
            type: array
            items:
              $ref: '#/components/schemas/PaymentTransactionLink'
          budgetLine:
            oneOf:
            - $ref: '#/components/schemas/PaymentBudgetLineLink'
            - type: 'null'
    Payment:
      allOf:
      - $ref: '#/components/schemas/PaymentBase'
      - type: object
        properties:
          request:
            oneOf:
            - $ref: '#/components/schemas/PaymentRequestBase'
            - type: 'null'
          purchaseOrder:
            oneOf:
            - $ref: '#/components/schemas/PaymentPurchaseOrderLink'
            - type: 'null'
          transactions:
            type: array
            items:
              $ref: '#/components/schemas/PaymentTransactionLink'
          contact:
            oneOf:
            - $ref: '#/components/schemas/PaymentContactLink'
            - type: 'null'
          budgetLine:
            oneOf:
            - $ref: '#/components/schemas/PaymentBudgetLineLink'
            - type: 'null'
          document:
            oneOf:
            - $ref: '#/components/schemas/PaymentDocumentLink'
            - type: 'null'
    PaymentTimelineEvent:
      type: object
      required:
      - id
      - type
      - label
      - occurredAt
      - actor
      - related
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        type:
          type: string
        label:
          type: string
        occurredAt:
          type: string
          format: date-time
        actor:
          oneOf:
          - type: object
            required:
            - id
            - name
            - avatarUrl
            additionalProperties: false
            properties:
              id:
                $ref: '#/components/schemas/Id'
              name:
                type: string
              avatarUrl:
                type:
                - string
                - 'null'
          - type: 'null'
        related:
          type: array
          items:
            type: object
            required:
            - type
            - id
            additionalProperties: false
            properties:
              type:
                type: string
                enum:
                - document
                - payment_request
                - payment
                - transaction
                - purchase_order
                - approval
              id:
                $ref: '#/components/schemas/Id'
    PaymentTimelinePage:
      type: object
      required:
      - data
      - nextCursor
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PaymentTimelineEvent'
        nextCursor:
          type:
          - string
          - 'null'
    PaymentRequestList:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PaymentRequest'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
    PaymentList:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Payment'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
    PurchaseOrderStatus:
      type: string
      description: The current purchase-order status. Clients read this field and use the named actions to change it.
      enum:
      - draft
      - pending
      - approved
      - rejected
      - actualizing
      - paid
      - void
      examples:
      - pending
    PurchaseOrderActivityType:
      type: string
      description: A live PO condition using the same names shown in the product Activity column.
      enum:
      - invoice_requested
      - invoice_received
      - invoice_rejected
      - payment_requested
      - payment_approval_pending
      - payment_approved
      - payment_processing
      - payment_sent
      - payment_failed
      - payment_returned
      - actualized_needs_review
      - split_needs_review
      - transaction_match_needs_review
    PurchaseOrderActivitySeverity:
      type: string
      enum:
      - neutral
      - info
      - warning
      - danger
      - success
    PurchaseOrderExpand:
      type: string
      description: "A single allowed `expand` key for a purchase order (the parameter takes a comma list of these, dotted\
        \ nesting depth \u2264 2). An unknown key returns `400 expand_invalid`."
      enum:
      - items
      - contact
      - transactions
      - documents
      - paymentRequests
      - payments
      - budgetLine
      - activity
      - summary
    PurchaseOrderSort:
      type: string
      description: Allowed sort fields for the purchase-order collection (appends `,id`).
      enum:
      - number
      - title
      - date
      - serviceStartAt
      - serviceEndAt
      - status
      - sort
      - createdAt
      - updatedAt
    PurchaseOrder:
      type: object
      description: A purchase order. `projectId` is `null` when it is not assigned to a project. `status` is read-only and
        changes through named actions.
      required:
      - id
      - workspaceId
      - status
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        workspaceId:
          $ref: '#/components/schemas/Id'
        projectId:
          description: "The project (`prj_\u2026`) this PO is assigned to, or `null` when it is an unassigned workspace-level\
            \ purchase order."
          oneOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        number:
          type:
          - string
          - 'null'
          description: Human-facing PO number (e.g. `PO-0042`). Free text; filterable by exact match.
        title:
          type:
          - string
          - 'null'
          description: Short PO title. This is the field that `q` search matches against.
        status:
          $ref: '#/components/schemas/PurchaseOrderStatus'
        contactId:
          type:
          - string
          - 'null'
          description: "The vendor contact (`con_\u2026`). Setting it marks the contact as a vendor for this PO."
        budgetLineId:
          type:
          - string
          - 'null'
          description: "The budget line (`bud_\u2026`) this PO is charged against."
        date:
          type:
          - string
          - 'null'
          format: date
          description: PO date (ISO-8601 `YYYY-MM-DD`).
        serviceStartAt:
          type:
          - string
          - 'null'
          format: date
          description: First service date for this PO (ISO-8601 `YYYY-MM-DD`).
        serviceEndAt:
          type:
          - string
          - 'null'
          format: date
          description: Last service date for this PO (ISO-8601 `YYYY-MM-DD`).
        notes:
          type:
          - string
          - 'null'
          description: Free-text notes.
        sort:
          type:
          - number
          - 'null'
          description: Client-controlled display ordering key within the project.
        createdById:
          type:
          - string
          - 'null'
          description: "The principal that created the PO (`usr_\u2026`). Server-owned, read-only."
        total:
          allOf:
          - $ref: '#/components/schemas/Money'
          description: Sum of item amounts in the PO currency. Read-only.
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: ISO-8601 last-update timestamp.
        items:
          type: array
          description: Present only when `expand=items`. The PO line items.
          items:
            $ref: '#/components/schemas/PurchaseOrderItem'
        contact:
          description: Present only when `expand=contact`. Includes the vendor when you can read it.
          oneOf:
          - $ref: '#/components/schemas/Contact'
          - type: 'null'
        transactions:
          type: array
          description: Present only when `expand=transactions`. Includes linked transactions you can read.
          items:
            $ref: '#/components/schemas/Transaction'
        documents:
          type: array
          description: Present only when `expand=documents`. Includes assigned documents you can read.
          items:
            $ref: '#/components/schemas/Document'
        paymentRequests:
          type: array
          description: Present only when `expand=paymentRequests`. Includes linked payment requests you can read.
          items:
            $ref: '#/components/schemas/PaymentRequestBase'
        payments:
          type: array
          description: Present only when `expand=payments`. Includes linked sent payments you can read.
          items:
            $ref: '#/components/schemas/PaymentBase'
        budgetLine:
          description: Present only when `expand=budgetLine`. The charged budget line.
          oneOf:
          - $ref: '#/components/schemas/BudgetLine'
          - type: 'null'
        activity:
          description: Present only when `expand=activity`. Current live conditions using the same vocabulary as the product
            Activity column.
          oneOf:
          - $ref: '#/components/schemas/PurchaseOrderActivitySummary'
          - type: 'null'
        summary:
          description: Financial summary. Included on a single-PO read and available on lists with `expand=summary`.
          oneOf:
          - $ref: '#/components/schemas/PurchaseOrderSummary'
          - type: 'null'
      examples:
      - id: po_8f2a1c9e
        workspaceId: ws_3b1
        projectId: prj_demo
        number: PO-0042
        title: Camera package rental
        status: pending
        contactId: con_77a
        budgetLineId: bud_5510
        date: '2026-05-20'
        serviceStartAt: '2026-05-21'
        serviceEndAt: '2026-05-24'
        notes: Net-30
        sort: 1000
        createdById: usr_aa1
        total:
          amount: 152900
          currency: USD
        createdAt: '2026-05-18T14:02:11.000Z'
        updatedAt: '2026-05-20T09:31:00.000Z'
    PurchaseOrderItem:
      type: object
      description: A single purchase-order line item.
      required:
      - id
      - purchaseOrderId
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        purchaseOrderId:
          $ref: '#/components/schemas/Id'
        lineNumber:
          type:
          - integer
          - 'null'
          description: 1-based line number within the PO.
        description:
          type:
          - string
          - 'null'
          description: Line description.
        budgetLineId:
          type:
          - string
          - 'null'
          description: "Budget line (`bud_\u2026`) this item is charged against."
        qty:
          type:
          - number
          - 'null'
          description: Quantity.
        unit:
          type:
          - string
          - 'null'
          description: Unit of measure (e.g. `day`, `week`, `each`).
        rate:
          type:
          - integer
          - 'null'
          format: int64
          description: Per-unit rate in minor units of the project currency.
        amount:
          type:
          - integer
          - 'null'
          format: int64
          description: "Extended line amount in minor units (`qty \xD7 rate`, server-validated)."
        sort:
          type:
          - number
          - 'null'
          description: Display ordering key within the PO.
      examples:
      - id: poi_1a
        purchaseOrderId: po_8f2a1c9e
        lineNumber: 1
        description: Alexa Mini LF body
        budgetLineId: bud_5510
        qty: 5
        unit: day
        rate: 25000
        amount: 125000
        sort: 1000
    PurchaseOrderActivityItem:
      type: object
      description: One live condition shown in the purchase-order Activity column.
      required:
      - type
      - label
      - severity
      - count
      - sourceIds
      additionalProperties: false
      properties:
        type:
          $ref: '#/components/schemas/PurchaseOrderActivityType'
        label:
          type: string
        severity:
          $ref: '#/components/schemas/PurchaseOrderActivitySeverity'
        count:
          type: integer
          minimum: 0
        sourceIds:
          type: array
          items:
            type: string
    PurchaseOrderActivitySummary:
      type: object
      description: A short Activity summary for list and detail responses. This is not history.
      required:
      - primaryActivity
      - items
      additionalProperties: false
      properties:
        primaryActivity:
          oneOf:
          - $ref: '#/components/schemas/PurchaseOrderActivityItem'
          - type: 'null'
        items:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderActivityItem'
    PurchaseOrderActivityEntry:
      type: object
      required:
      - id
      - occurredAt
      - amount
      - method
      - accountLast4
      - reason
      - fileName
      - description
      - source
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        occurredAt:
          type:
          - string
          - 'null'
          format: date-time
        amount:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        method:
          type:
          - string
          - 'null'
        accountLast4:
          type:
          - string
          - 'null'
        reason:
          type:
          - string
          - 'null'
        fileName:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        source:
          $ref: '#/components/schemas/PublicResourceRef'
    PurchaseOrderActivityDetail:
      type: object
      required:
      - type
      - label
      - severity
      - count
      - sourceIds
      - entries
      additionalProperties: false
      properties:
        type:
          $ref: '#/components/schemas/PurchaseOrderActivityType'
        label:
          type: string
        severity:
          $ref: '#/components/schemas/PurchaseOrderActivitySeverity'
        count:
          type: integer
          minimum: 0
        sourceIds:
          type: array
          items:
            type: string
        entries:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderActivityEntry'
    PurchaseOrderActivity:
      type: object
      description: Current conditions and the records behind them.
      required:
      - purchaseOrderId
      - primaryActivity
      - items
      additionalProperties: false
      properties:
        purchaseOrderId:
          $ref: '#/components/schemas/Id'
        primaryActivity:
          oneOf:
          - $ref: '#/components/schemas/PurchaseOrderActivityDetail'
          - type: 'null'
        items:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderActivityDetail'
    PurchaseOrderSummary:
      type: object
      description: Current purchase-order amounts in the PO currency.
      required:
      - purchaseOrderId
      - currency
      - amounts
      additionalProperties: false
      properties:
        purchaseOrderId:
          $ref: '#/components/schemas/Id'
        currency:
          type: string
          minLength: 3
          maxLength: 3
        amounts:
          type: object
          required:
          - ordered
          - requested
          - invoiced
          - sent
          - actualized
          - remaining
          - variance
          additionalProperties: false
          properties:
            ordered:
              $ref: '#/components/schemas/Money'
            requested:
              $ref: '#/components/schemas/Money'
            invoiced:
              $ref: '#/components/schemas/Money'
            sent:
              $ref: '#/components/schemas/Money'
            actualized:
              $ref: '#/components/schemas/Money'
            remaining:
              $ref: '#/components/schemas/Money'
            variance:
              $ref: '#/components/schemas/Money'
    PurchaseOrderSuggestedMatch:
      type: object
      description: A record that may belong to this purchase order but is not linked.
      required:
      - recordType
      - recordId
      - name
      - amount
      - confidence
      - reasons
      additionalProperties: false
      properties:
        recordType:
          type: string
          const: document
        recordId:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        amount:
          oneOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        confidence:
          type: string
          const: suggested
        reasons:
          type: array
          prefixItems:
          - type: string
            const: purchase_order_number
          minItems: 1
          maxItems: 1
    PurchaseOrderSuggestedMatchList:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderSuggestedMatch'
    PurchaseOrderTimelineEventType:
      type: string
      description: Event types used in purchase-order history.
      enum:
      - created
      - item_added
      - submitted_for_approval
      - approved
      - rejected
      - approval_request_canceled
      - voided
      - transaction_linked
      - transaction_unlinked
      - marked_paid
      - invoice_requested
      - invoice_uploaded
      - attachment_uploaded
      - document_linked
      - decision_recorded
      - decision_undone
      - status_changed
      - approval_decision
      - details_updated
    PurchaseOrderTimelineActor:
      type: object
      required:
      - id
      - name
      - avatarUrl
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
        avatarUrl:
          type:
          - string
          - 'null'
    PurchaseOrderTimelineEvent:
      type: object
      required:
      - id
      - type
      - label
      - occurredAt
      - actor
      - related
      - superseded
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Id'
        type:
          $ref: '#/components/schemas/PurchaseOrderTimelineEventType'
        label:
          type: string
        occurredAt:
          type: string
          format: date-time
        actor:
          oneOf:
          - $ref: '#/components/schemas/PurchaseOrderTimelineActor'
          - type: 'null'
        related:
          type: array
          items:
            $ref: '#/components/schemas/PublicResourceRef'
        superseded:
          type: boolean
    PurchaseOrderTimelinePage:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderTimelineEvent'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
    PurchaseOrderCreate:
      type: object
      description: Create a purchase order. The server sets `status='draft'` and `createdById`; naming any server-owned field
        (`status`, `createdById`, `workspaceId`, or any internal bookkeeping field) returns `422 field_read_only` and changes
        nothing. Pass `projectId` to assign the PO to a project, or omit it to create a workspace-level (unassigned) purchase
        order.
      additionalProperties: false
      properties:
        projectId:
          type: string
          description: "Optional project (`prj_\u2026` id or slug) to assign the PO to. Omit to create a workspace-level (unassigned)\
            \ purchase order. A project that does not exist (or is not readable) returns `404 not_found`."
        number:
          type: string
          description: Human-facing PO number.
        title:
          type: string
          description: Short PO title.
        contactId:
          type: string
          description: "Vendor contact (`con_\u2026`); marks the contact as a vendor for this PO."
        budgetLineId:
          type: string
          description: "Budget line (`bud_\u2026`) to charge against."
        date:
          type: string
          format: date
          description: PO date (ISO-8601 `YYYY-MM-DD`).
        serviceStartAt:
          type: string
          format: date
          description: First service date for this PO (ISO-8601 `YYYY-MM-DD`).
        serviceEndAt:
          type: string
          format: date
          description: Last service date for this PO (ISO-8601 `YYYY-MM-DD`).
        notes:
          type: string
        sort:
          type: number
        items:
          type: array
          description: Optional line items to create with the PO.
          items:
            $ref: '#/components/schemas/PurchaseOrderItemWrite'
      examples:
      - number: PO-0042
        title: Camera package rental
        contactId: con_77a
        budgetLineId: bud_5510
        date: '2026-05-20'
        serviceStartAt: '2026-05-21'
        serviceEndAt: '2026-05-24'
        notes: Net-30
        items:
        - description: Alexa Mini LF body
          qty: 5
          unit: day
          rate: 25000
    PurchaseOrderUpdate:
      type: object
      description: "Partial update of a purchase order. Allowed only while `status` is `draft` or `rejected`; otherwise `409\
        \ po_invalid_status`. `status` is server-owned: naming `status`, `workspaceId`, `createdById`, or any internal bookkeeping\
        \ field returns `422 field_read_only` and changes nothing. `projectId` is settable: a `prj_\u2026` id or slug assigns\
        \ the PO to that project, and `null` unassigns it back to the workspace level."
      additionalProperties: false
      properties:
        projectId:
          type:
          - string
          - 'null'
          description: "Assign or unassign the PO. A `prj_\u2026` id or slug assigns it to that project; `null` unassigns\
            \ it back to the workspace level. A named project that does not exist (or is not readable) returns `404 not_found`."
        number:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
        contactId:
          type:
          - string
          - 'null'
        budgetLineId:
          type:
          - string
          - 'null'
        date:
          type:
          - string
          - 'null'
          format: date
        serviceStartAt:
          type:
          - string
          - 'null'
          format: date
        serviceEndAt:
          type:
          - string
          - 'null'
          format: date
        notes:
          type:
          - string
          - 'null'
        sort:
          type:
          - number
          - 'null'
      examples:
      - title: Camera package rental (updated)
        notes: Net-45
    PurchaseOrderLink:
      type: object
      description: Links or unlinks a transaction. Both records must belong to the same workspace. Project or coding differences
        remain visible for review.
      required:
      - transactionId
      additionalProperties: false
      properties:
        transactionId:
          $ref: '#/components/schemas/Id'
      examples:
      - transactionId: txn_42
    PurchaseOrderItemWrite:
      type: object
      description: "Create or update a purchase-order line item. Item writes are allowed only while the parent PO `status\
        \ \u2208 {draft, rejected}`; otherwise `409 po_invalid_status`. `id` and `purchaseOrderId` are server-owned."
      additionalProperties: false
      properties:
        lineNumber:
          type:
          - integer
          - 'null'
        description:
          type:
          - string
          - 'null'
        budgetLineId:
          type:
          - string
          - 'null'
        qty:
          type:
          - number
          - 'null'
        unit:
          type:
          - string
          - 'null'
        rate:
          type:
          - integer
          - 'null'
          format: int64
        amount:
          type:
          - integer
          - 'null'
          format: int64
          description: "Optional. When omitted, the server calculates `qty \xD7 rate`. An inconsistent value returns `validation`."
        sort:
          type:
          - number
          - 'null'
      examples:
      - description: Alexa Mini LF body
        budgetLineId: bud_5510
        qty: 5
        unit: day
        rate: 25000
    PurchaseOrderList:
      type: object
      description: A paginated page of purchase orders.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrder'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion returned fewer than `limit` items for this page.
    PurchaseOrderItemList:
      type: object
      description: A page of purchase-order line items.
      required:
      - data
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderItem'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when permission filtering or expansion returned fewer than `limit` items for this page.
    SearchKind:
      type: string
      description: 'The entity kinds the public Search can match in v1. `rate-pack-item` and `project` are deliberately NOT
        searchable in v1 and would each require additional work; treat as deferred. Note: `contacts` are workspace-level only
        (no project association), so a project-scoped token never matches them, see `SearchResponse.searchedTypes`.'
      enum:
      - transactions
      - documents
      - contacts
      - budget-lines
      - purchase-orders
      examples:
      - transactions
    SearchHit:
      type: object
      description: One ranked, mixed-type search result. Carries just enough to render a Search row and to re-fetch the full
        object by `{kind, id}`; `score` lets a client threshold weak matches. Every hit has passed the in-query permission
        / project-visibility filter, so the result set never contains a row the token cannot read.
      required:
      - kind
      - id
      - label
      - score
      additionalProperties: false
      properties:
        kind:
          $ref: '#/components/schemas/SearchKind'
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Canonical id of the matched entity (`txn_\u2026`, `doc_\u2026`, `con_\u2026`, `bud_\u2026`, `po_\u2026\
            `). Combine with `kind` to fetch the full object from its resource route."
        label:
          type: string
          description: The human title of the matched entity, exactly as it reads in the UI (e.g. a transaction description,
            a document name, a contact display title, a budget-line account label, a purchase-order title).
          examples:
          - Panavision Camera Package
        snippet:
          type: string
          description: The matched text fragment that produced the hit (the field the text similarity fired on). Absent when
            the matched text is identical to `label`.
          examples:
          - Invoice 4471, Panavision rental
        score:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: 'Similarity of the match in `[0, 1]` (higher is closer). Results are returned in a stable order: highest
            score first, with equal scores tiebroken on `(kind, id)`.'
          examples:
          - 0.62
    SearchResponse:
      type: object
      description: 'A page of mixed-type Search results. `data` is returned in a stable order: highest score first, with equal
        scores tiebroken on `(kind, id)`. `searchedTypes` echoes which kinds were actually queried so partial coverage is
        visible and never a silent empty.'
      required:
      - data
      - searchedTypes
      additionalProperties: false
      properties:
        data:
          type: array
          description: The ranked hits for this page, capped at the request `limit`.
          items:
            $ref: '#/components/schemas/SearchHit'
        nextCursor:
          allOf:
          - $ref: '#/components/schemas/NextCursor'
          description: The canonical cursor for the next page. Encodes the mint-time `q` + `types` + sort; replaying it with
            a changed `q`/`types` returns `400 cursor_invalid`. Absent/null when there are no more results.
          examples:
          - eyJzIjowLjYyLCJrIjoidHJhbnNhY3Rpb25zIiwiaWQiOiJ0eG5fOGYyYSJ9
        count:
          type: integer
          description: Total matching hits; present only when `withCount=true`.
        truncated:
          type: boolean
          description: '`true` when the page returned fewer than `limit` results because the permission / project-visibility
            filter removed matches you cannot read. Keep paging with `nextCursor`.'
        searchedTypes:
          type: array
          description: "The kinds that were actually searched for this request, computed as `requested \u2229 supported \u2229\
            \ authorized`. A requested kind is dropped here (not silently returned empty) when it is not searchable in v1,\
            \ or when the token's permission scope fully denies it, e.g. a project-scoped token drops `contacts` (workspace-level\
            \ only, never project-visible). Compare against your requested `types` to detect partial coverage."
          items:
            $ref: '#/components/schemas/SearchKind'
          examples:
          - - transactions
            - documents
            - budget-lines
    TransactionSource:
      type: string
      description: Where the record came from, a closed enum of the four rails. `journal` is the manual entry that replaces
        the legacy "actual"; the other three are sourced rails whose core fields the rail owns and the public API treats as
        read-only.
      enum:
      - journal
      - plaid
      - saturation_pay
      - saturation_credit
      examples:
      - journal
    TransactionType:
      type: string
      description: What kind of money movement. The underlying column is open, so a new rail never forces a re-migration;
        the API publishes it as an **extensible enum**, the canonical PascalCase values below are documented, autocompletable
        and client-validatable, but an unknown value is still accepted on write and echoed on read (never coerced or rejected).
        Every writer (Plaid, Sat Pay/Credit banking, Stripe Treasury/Issuing, manual journals) emits this one canonical vocabulary.
        A `?type=` filter matches a row's stored value verbatim.
      x-extensible-enum: true
      enum:
      - SaturationPCard
      - ACH
      - Cash
      - Check
      - CreditCard
      - Deposit
      - ETransfer
      - Invoice
      - Projection
      - TimeCard
      - Wire
      - Withdrawal
      examples:
      - CreditCard
    TransactionJournalType:
      type: string
      description: The subset of `type` values that may be set on a `journal` create/patch (the human/journal family). Rail-only
        types (`SaturationPCard`, `Deposit`, `Withdrawal`) arrive via their rail and cannot be journaled. Casing is preserved
        exactly (PascalCase). Unknown values are rejected on the journal-create body only (input validation), never on stored/returned
        values elsewhere.
      enum:
      - Invoice
      - Projection
      - Cash
      - Check
      - Wire
      - ACH
      - CreditCard
      - TimeCard
      - ETransfer
      examples:
      - Invoice
    TransactionStatus:
      type: string
      description: Where the record is in life, a CLOSED 7-value public enum. `posted` means money moved (unifies bank "settled"
        and expense "paid"). Refunds and disputes are surfaced via the derived `isReversal=true` flag, never as a status.
        Not every status is reachable for every `source`, see `statusReachability` on the collection and the `status_unreachable_for_source`
        (409) error.
      enum:
      - unpaid
      - pending
      - posted
      - void
      - needs_review
      - projected
      - rejected
      examples:
      - posted
    ExchangeRateSource:
      type: string
      description: Origin of the `exchangeRate` on a sourced multi-currency row. Published as an extensible enum, the documented
        members are below, but an unknown value is still accepted and echoed.
      x-extensible-enum: true
      enum:
      - bank
      - api
      - user
      examples:
      - bank
    TransactionExpandKey:
      type: string
      description: "Allowed `expand` keys for transactions (typed allow-list, depth \u2264 2). An unknown or too-deep key\
        \ returns `400 expand_invalid` (all-or-nothing). Expanded arrays are permission-projected, so empty/partial means\
        \ \"not-readable-or-absent\"."
      enum:
      - contact
      - documents
      - items
      - account
      - purchaseOrder
      - items.account
      examples:
      - items.account
    TransactionSortField:
      type: string
      description: Sortable fields for the transactions list. A non-unique field appends `,id` as the tiebreaker. Default
        sort is `timestamp` `desc`.
      enum:
      - timestamp
      - amount
      - status
      - type
      - description
      - createdAt
      - updatedAt
      default: timestamp
      examples:
      - timestamp
    TransactionItem:
      type: object
      description: An itemized line of a transaction. When a transaction `isItemized`, coding moves from `transaction.budgetLineId`
        onto these items. Items soft-delete (for unsplit recovery); a deleted item is never returned.
      additionalProperties: false
      required:
      - id
      - lineNumber
      - amount
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Stable item identifier (`txi_\u2026`)."
          examples:
          - txi_4a1b2c3d
        lineNumber:
          type: string
          description: Fractional-index line number, server-assigned so itemization order is consistent with auto-itemize.
          examples:
          - '1'
        description:
          type:
          - string
          - 'null'
          description: Free-text line description.
        budgetLineId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          description: "Budget line this item is coded to (`bud_\u2026`)."
        fringeId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          description: Fringe applied to this item, if any.
        qty:
          type:
          - number
          - 'null'
          description: Quantity (units of `unit`).
          examples:
          - 3
        unit:
          type:
          - string
          - 'null'
          description: Unit label (e.g. `Day`, `Hour`, `Flat`).
          examples:
          - Day
        rate:
          anyOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
          description: Per-unit rate as money (minor units + currency).
        amount:
          $ref: '#/components/schemas/Money'
        overtime:
          type:
          - boolean
          - 'null'
          description: TimeCard, line is overtime.
        taxable:
          type:
          - boolean
          - 'null'
          description: TimeCard, line is taxable.
        nonTaxable:
          type:
          - boolean
          - 'null'
          description: TimeCard, line is non-taxable.
        account:
          description: Present only when `expand=items.account`; the budget account the item's budget line resolves to. permission-projected.
          oneOf:
          - $ref: '#/components/schemas/TransactionAccountRef'
          - type: 'null'
    TransactionItemCreate:
      type: object
      description: Body for creating one transaction item. `lineNumber` is server-assigned.
      additionalProperties: false
      required:
      - amount
      properties:
        description:
          type: string
        budgetLineId:
          $ref: '#/components/schemas/Id'
        fringeId:
          $ref: '#/components/schemas/Id'
        qty:
          type: number
        unit:
          type: string
        rate:
          $ref: '#/components/schemas/Money'
        amount:
          $ref: '#/components/schemas/Money'
        overtime:
          type: boolean
        taxable:
          type: boolean
        nonTaxable:
          type: boolean
    TransactionItemPatch:
      type: object
      description: Partial update of an item. All fields optional; `id`, `lineNumber` and tenant keys are server-owned and
        rejected with `422 field_read_only` if named.
      additionalProperties: false
      minProperties: 1
      properties:
        description:
          type:
          - string
          - 'null'
        budgetLineId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        fringeId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        qty:
          type:
          - number
          - 'null'
        unit:
          type:
          - string
          - 'null'
        rate:
          anyOf:
          - $ref: '#/components/schemas/Money'
          - type: 'null'
        amount:
          $ref: '#/components/schemas/Money'
        overtime:
          type:
          - boolean
          - 'null'
        taxable:
          type:
          - boolean
          - 'null'
        nonTaxable:
          type:
          - boolean
          - 'null'
    TransactionAccountRef:
      type: object
      description: Lean budget-account reference inlined by `expand=account` / `items.account`.
      additionalProperties: false
      required:
      - id
      properties:
        id:
          $ref: '#/components/schemas/Id'
        name:
          type: string
          examples:
          - Camera
        code:
          type:
          - string
          - 'null'
          description: Friendly account code, if any.
          examples:
          - '1200'
    Transaction:
      type: object
      description: The unified financial record (no separate "actuals"). One shape covers manual journals and every sourced
        rail; the three orthogonal dimensions `source` / `type` / `status` are exposed as three independent fields. Money
        is always integer minor units + `currency`; timestamps are ISO-8601.
      additionalProperties: false
      required:
      - id
      - source
      - status
      - isReversal
      - amount
      - timestamp
      - isItemized
      - actualized
      - createdAt
      - updatedAt
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Stable transaction identifier (`txn_\u2026`)."
          examples:
          - txn_8f2a1c9e
        source:
          $ref: '#/components/schemas/TransactionSource'
        sourceId:
          type:
          - string
          - 'null'
          description: Opaque external source link reference for a sourced row (the rail's own id); `null` for journals. Not
            a joinable resource; the transaction record is itself the source of truth for reads.
        type:
          anyOf:
          - $ref: '#/components/schemas/TransactionType'
          - type: 'null'
        status:
          $ref: '#/components/schemas/TransactionStatus'
        isReversal:
          type: boolean
          description: 'DERIVED (no stored column): `true` when the row''s internal state is a refund or dispute. Lets a consumer
            ask for "money-back events" after refund/dispute left the `status` vocabulary.'
        amount:
          $ref: '#/components/schemas/Money'
        currency:
          type:
          - string
          - 'null'
          description: ISO-4217 currency (also carried inside `amount`). Surfaced top-level for convenience and filtering.
          minLength: 3
          maxLength: 3
          examples:
          - USD
        exchangeRate:
          type:
          - number
          - 'null'
          description: Sourced multi-currency rows, rate to the workspace base currency.
        exchangeRateSource:
          oneOf:
          - $ref: '#/components/schemas/ExchangeRateSource'
          - type: 'null'
        timestamp:
          type: string
          format: date-time
          description: When the money movement occurred (ISO-8601).
          examples:
          - '2026-05-20T14:03:00.000Z'
        description:
          type:
          - string
          - 'null'
        merchant:
          type:
          - string
          - 'null'
          description: Rail-supplied merchant name (read-only on sourced rows).
        logoUrl:
          type:
          - string
          - 'null'
          format: uri
          description: Rail-supplied merchant logo, if any.
        category:
          type:
          - string
          - 'null'
          description: Rail-supplied category, if any.
        sourceLast4:
          type:
          - string
          - 'null'
          description: Last 4 of the funding instrument on a sourced row (read-only).
        sourceName:
          type:
          - string
          - 'null'
          description: Human label of the source account/card (read-only on sourced rows).
        number:
          type:
          - string
          - 'null'
          description: Document/check/invoice number.
        ref:
          type:
          - string
          - 'null'
          description: Free-text external reference.
        payId:
          type:
          - string
          - 'null'
          description: Saturation Pay payment id, when applicable.
        notes:
          type:
          - string
          - 'null'
          description: User notes (always writable, any source).
        contactId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          description: Assigned contact (vendor/crew/payee).
        projectId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          nullable: true
          description: "Owning project (`prj_\u2026`), or `null` when the transaction is unassigned (workspace-level). Settable\
            \ via create/patch to assign, reassign, or unassign the row."
        budgetLineId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          description: Budget line coding when NOT itemized. When `isItemized=true`, coding lives on `items[]` and this is
            `null`.
        fringeId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        isItemized:
          type: boolean
          description: '`true` once the row has items; flips automatically on first item create and back on last item delete.'
        actualized:
          type: boolean
          description: Whether this transaction contributes to a budget's ACTUAL phase. Only `actualized=true` rows count
            toward actuals. Birth value follows the workspace auto-actualize setting at create time; writable thereafter via
            PATCH on any source.
        purchaseOrderId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          description: "Linked purchase order, if any (`po_\u2026`)."
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        contact:
          description: Present when `expand=contact`. Lean contact ref; null if not readable.
          type:
          - object
          - 'null'
        documents:
          description: Present when `expand=documents`. permission-projected document refs.
          type: array
          items:
            type: object
        items:
          type: array
          description: Present when `expand=items`. The itemized lines.
          items:
            $ref: '#/components/schemas/TransactionItem'
        account:
          description: Present when `expand=account`. Lean budget-account ref; null if absent.
          oneOf:
          - $ref: '#/components/schemas/TransactionAccountRef'
          - type: 'null'
        purchaseOrder:
          description: Present when `expand=purchaseOrder`. Lean PO ref; null if none/unreadable.
          type:
          - object
          - 'null'
    TransactionJournalCreate:
      type: object
      description: "Body for creating a JOURNAL transaction. `source` is forced to `journal`, naming any sourced source returns\
        \ `422 source_not_postable` (sourced rows arrive via their rail). Server-owned fields (`id`, `sourceId`, `isReversal`,\
        \ `createdAt`, `updatedAt`, tenant keys) are NOT writable. The chosen `status` is validated against the status \xD7\
        \ source reachability matrix \u2192 `409 status_unreachable_for_source`."
      additionalProperties: false
      required:
      - type
      - amount
      - timestamp
      properties:
        type:
          $ref: '#/components/schemas/TransactionJournalType'
        amount:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          description: ISO-4217 currency (alternative to embedding it in `amount`).
          minLength: 3
          maxLength: 3
        timestamp:
          type: string
          format: date-time
          description: RFC3339 datetime (UTC offsets valid), or date-only `YYYY-MM-DD` recorded as midnight UTC.
        status:
          allOf:
          - $ref: '#/components/schemas/TransactionStatus'
          description: Defaults to `posted`. Must be reachable for `journal`.
        description:
          type: string
        projectId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Optional project assignment (`prj_\u2026` id or slug). Omit to create a workspace-level (unassigned)\
            \ transaction; a named project that does not resolve returns `404 not_found`."
        contactId:
          $ref: '#/components/schemas/Id'
        budgetLineId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: Coding (omit when the row will be itemized).
        fringeId:
          $ref: '#/components/schemas/Id'
        number:
          type: string
        ref:
          type: string
        notes:
          type: string
        actualized:
          type: boolean
          description: Optional override of the actualization birth value. Omit and the workspace auto-actualize setting decides;
            set explicitly to force it.
    TransactionPatch:
      type: object
      description: "Partial update. Writability is driven by a `source \u2192 {readonly, writable}` allow-list. Assignment\
        \ fields (`projectId`, `contactId`, `budgetLineId`, `fringeId`, `notes`, `number`, `ref`, `actualized`) are writable\
        \ on ANY source. `projectId` is an assignment field - a `prj_\u2026` id or slug moves the transaction to that project,\
        \ and `null` unassigns it back to the workspace level. Core fields (`amount`, `currency`, `timestamp`, `type`, `status`,\
        \ `merchant`, `sourceLast4`, `sourceName`) are writable ONLY on `journal`; naming one on a sourced row returns `422\
        \ field_read_only`. Server-owned fields (`id`, `source`, `sourceId`, `isReversal`, `isItemized`, `createdAt`, `updatedAt`)\
        \ are never writable. A `status` change is checked against the reachability matrix."
      additionalProperties: false
      minProperties: 1
      properties:
        projectId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
          description: "Assign, reassign, or unassign the transaction. A `prj_\u2026` id or slug moves it to that project;\
            \ `null` unassigns it back to the workspace level. Writable on any source."
        contactId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        budgetLineId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        fringeId:
          anyOf:
          - $ref: '#/components/schemas/Id'
          - type: 'null'
        notes:
          type:
          - string
          - 'null'
        number:
          type:
          - string
          - 'null'
        ref:
          type:
          - string
          - 'null'
        amount:
          $ref: '#/components/schemas/Money'
        currency:
          type: string
          minLength: 3
          maxLength: 3
        timestamp:
          type: string
          format: date-time
          description: RFC3339 datetime (UTC offsets valid), or date-only `YYYY-MM-DD` recorded as midnight UTC.
        type:
          $ref: '#/components/schemas/TransactionType'
        status:
          $ref: '#/components/schemas/TransactionStatus'
        description:
          type:
          - string
          - 'null'
        merchant:
          type:
          - string
          - 'null'
        sourceLast4:
          type:
          - string
          - 'null'
        sourceName:
          type:
          - string
          - 'null'
        actualized:
          type: boolean
          description: Whether this transaction contributes to a budget's ACTUAL phase. Writable on any source.
    TransactionBulkCreate:
      type: object
      description: "Bulk JOURNAL import. Each entry is a `TransactionJournalCreate`; `source` is forced to `journal` on every\
        \ row. Over the per-request cap \u2192 `413 bulk_too_large`. Pair with an `Idempotency-Key` header for safe retries."
      additionalProperties: false
      required:
      - transactions
      properties:
        transactions:
          type: array
          minItems: 1
          maxItems: 500
          description: The journal transactions to create. Cap is 500 per request.
          items:
            $ref: '#/components/schemas/TransactionJournalCreate'
    TransactionCollection:
      type: object
      description: A paginated page of transactions plus disclosure metadata.
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching rows; present only when `withCount=true`.
        truncated:
          type: boolean
          description: True when a permission/expand under-fetch bounded the page short of `limit`.
        statusReachability:
          $ref: '#/components/schemas/StatusReachabilityMatrix'
    TransactionItemCollection:
      type: object
      description: The itemized lines of a transaction.
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TransactionItem'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching rows; present only when `withCount=true`.
        truncated:
          type: boolean
          description: True when a permission/expand under-fetch bounded the page short of `limit`.
    TransactionStats:
      type: object
      description: Aggregate count + summed amount for a filter set (permission/project-scoped, honoring the same filter vocabulary
        as the list). `amount` is money in minor units; sum a single `currency` filter for a meaningful total across mixed-currency
        ledgers.
      additionalProperties: false
      required:
      - total
      - amount
      properties:
        total:
          type: integer
          description: Number of matching transactions.
          examples:
          - 142
        amount:
          $ref: '#/components/schemas/Money'
    TransactionTypes:
      type: object
      description: The distinct `type` values the token can actually see (permission/project-filtered, never the raw workspace-wide
        set), for building dropdowns. Casing is preserved per family.
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          description: Distinct visible `type` values.
          items:
            $ref: '#/components/schemas/TransactionType'
          examples:
          - - CreditCard
            - Invoice
            - TimeCard
    StatusReachabilityMatrix:
      type: object
      description: The status x source reachability matrix the API owns and enforces. Maps each `source` to the subset of
        the 7 statuses it can legitimately reach. Setting an unreachable status on create/patch returns `409 status_unreachable_for_source`
        with the allowed set echoed in the error. Published so integrators can pre-validate.
      additionalProperties:
        type: array
        items:
          $ref: '#/components/schemas/TransactionStatus'
      examples:
      - journal:
        - posted
        - void
        - projected
        plaid:
        - pending
        - posted
        - void
        - needs_review
        saturation_pay:
        - unpaid
        - pending
        - posted
        - void
        - rejected
        saturation_credit:
        - pending
        - posted
        - void
        - needs_review
        - rejected
    WebhookEvent:
      type: string
      description: Events available for webhook subscriptions. Purchase-order events use the status the order entered. Only
        listed events are delivered.
      enum:
      - transaction.created
      - transaction.updated
      - budget.changed
      - purchaseOrder.created
      - purchaseOrder.pending
      - purchaseOrder.approved
      - purchaseOrder.rejected
      - purchaseOrder.actualizing
      - purchaseOrder.paid
      - purchaseOrder.void
      - document.created
      - document.assigned
      - document.unassigned
      - document.deleted
      - incentive.added
      - pack.installed
      - pack.uninstalled
      examples:
      - transaction.created
    WebhookPayloadStyle:
      type: string
      description: Deliver the record type and id. Fetch the record to read its current fields.
      enum:
      - thin
      default: thin
    WebhookDeliveryStatus:
      type: string
      description: Outcome of a single delivery attempt. `pending` = queued or mid-retry, not yet terminal. `success` = the
        endpoint returned a 2xx within the timeout. `failed` = a non-2xx, timeout, or transport error (retried with exponential
        backoff). `blocked` = the target failed the SSRF guard at delivery time (re-validated even if it passed registration).
        `dropped` = permission re-resolution at delivery denied the subscription's owning identity, so the event was withheld
        fail-closed.
      enum:
      - pending
      - success
      - failed
      - blocked
      - dropped
    Webhook:
      type: object
      description: An outbound webhook subscription. Workspace-scoped, optionally narrowed to one project; it delivers the
        events in `events[]` to `url` over signed, SSRF-guarded HTTPS. The `secret` is returned exactly once, in the create
        response, and never on any read.
      additionalProperties: false
      required:
      - id
      - url
      - events
      - payloadStyle
      - isActive
      - workspaceId
      - createdAt
      - updatedAt
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Subscription identifier (`whk_\u2026`)."
          examples:
          - whk_5c1a9e02
        url:
          type: string
          format: uri
          description: HTTPS destination for deliveries. Always validated by the SSRF guard on create and on any URL change;
            private / loopback / link-local / cloud-metadata / IPv6-mapped / decimal-octal-hex / internal-DNS targets are
            rejected.
          examples:
          - https://hooks.example.com/saturation
        events:
          type: array
          description: The subscribed event types. Closed enum ().
          minItems: 1
          items:
            $ref: '#/components/schemas/WebhookEvent'
        payloadStyle:
          $ref: '#/components/schemas/WebhookPayloadStyle'
        isActive:
          type: boolean
          description: Whether the subscription currently receives deliveries. Toggled via PATCH; a webhook continues to exist
            while paused.
        workspaceId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The workspace the subscription belongs to (`ws_\u2026`). Server-owned; not client-writable."
        projectId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Optional project scope (`prj_\u2026`). When set, the subscription only receives that project's events.\
            \ Server-owned after create; not changeable via PATCH."
        lastDeliveryAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp of the most recent delivery attempt for this subscription, for a quick health glance.
            Absent until the first delivery.
        lastDeliveryStatus:
          allOf:
          - $ref: '#/components/schemas/WebhookDeliveryStatus'
          description: Outcome of the most recent delivery attempt. Absent until the first delivery. Inspect the full history
            via `GET.../deliveries`.
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: ISO-8601 last-update timestamp.
    WebhookWithSecret:
      description: The create response. Identical to `Webhook` but additionally carries the signing `secret` this one time
        only, store it now; it can never be read again. Rotating the secret means recreating the subscription.
      allOf:
      - $ref: '#/components/schemas/Webhook'
      - type: object
        required:
        - secret
        properties:
          secret:
            type: string
            description: The HMAC signing secret. Returned exactly once, here. Used to verify `X-Saturation-Signature` on
              every delivery. Never echoed on GET / list / PATCH.
            examples:
            - whsec_example
    WebhookCreate:
      type: object
      description: Create-a-subscription request body (Zod allow-list). Server-owned fields (`id`, `workspaceId`, owning identity,
        `isActive`, `createdAt`, `updatedAt`) are not accepted; naming one returns `422 field_read_only`.
      additionalProperties: false
      required:
      - url
      - events
      properties:
        url:
          type: string
          format: uri
          description: HTTPS-only destination. Rejected with `400 webhook_https_required` if not HTTPS, and `422 webhook_url_blocked`
            / `409 webhook_url_invalid_ssrf` if it resolves to a blocked address.
          examples:
          - https://hooks.example.com/saturation
        events:
          type: array
          description: One or more event types to subscribe to.
          minItems: 1
          items:
            $ref: '#/components/schemas/WebhookEvent'
        secret:
          type: string
          description: Optional signing secret. If omitted, the server generates one and returns it once in the create response.
          minLength: 16
          maxLength: 256
        payloadStyle:
          $ref: '#/components/schemas/WebhookPayloadStyle'
        projectId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Optional project scope (`prj_\u2026`). Set only at create; the subscription then receives just that\
            \ project's events. Fixed thereafter (not PATCH-able)."
    WebhookUpdate:
      type: object
      description: PATCH a subscription (explicit Zod allow-list). Only these fields are writable. Tenant keys (`workspaceId`
        / `projectId`), the owning identity, and `secret` are server-owned, naming any of them returns `422 field_read_only`
        (fail-loud, never a silent strip). A `url` change re-runs the SSRF guard.
      additionalProperties: false
      minProperties: 1
      properties:
        url:
          type: string
          format: uri
          description: New HTTPS destination. Re-validated by the SSRF guard.
          examples:
          - https://hooks.example.com/saturation/v2
        events:
          type: array
          description: Replacement event set.
          minItems: 1
          items:
            $ref: '#/components/schemas/WebhookEvent'
        payloadStyle:
          $ref: '#/components/schemas/WebhookPayloadStyle'
        isActive:
          type: boolean
          description: Pause (`false`) or resume (`true`) deliveries.
    WebhookDelivery:
      type: object
      description: One delivery attempt of an event to a subscription, recorded for debugging. The `id` is stable across retries
        of the same delivery; a redelivery gets a new `id`.
      additionalProperties: false
      required:
      - id
      - event
      - status
      - entityKind
      - entityId
      - payloadStyle
      - attempts
      - occurredAt
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "Delivery identifier (`whd_\u2026`). Stable across retries; used as the dedupe key."
          examples:
          - whd_9a02c1e5
        event:
          $ref: '#/components/schemas/WebhookEvent'
        status:
          $ref: '#/components/schemas/WebhookDeliveryStatus'
        entityKind:
          type: string
          description: The kind of entity the event concerns (e.g. `transaction`, `budget`, `purchaseOrder`, `document`).
          examples:
          - transaction
        entityId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: The id of the entity the event concerns (what the consumer re-fetches).
          examples:
          - txn_8f2a1c9e
        payloadStyle:
          $ref: '#/components/schemas/WebhookPayloadStyle'
        requestUrl:
          type: string
          format: uri
          description: The validated, IP-pinned URL the delivery was sent to.
          examples:
          - https://hooks.example.com/saturation
        responseStatus:
          type: integer
          description: HTTP status the subscriber returned, when a connection was made. Absent for `blocked` / `dropped`.
          examples:
          - 200
        attempts:
          type: integer
          description: 1-based count of delivery attempts made so far (deliveries retry with exponential backoff).
          minimum: 1
          examples:
          - 1
        nextRetryAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp of the next scheduled retry. Present only while `pending` and more attempts remain;
            absent once the delivery is terminal (`success` / `failed` / `blocked` / `dropped`).
        error:
          type: string
          description: Failure detail for `failed` / `blocked` / `dropped` (e.g. SSRF rejection reason, permission drop, or
            transport error). Absent on `success`.
        occurredAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp of when the originating event occurred.
        deliveredAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp of the terminal attempt. Absent while `pending`.
    WebhookEventPayload:
      type: object
      description: The body sent to a subscriber. Fetch the record in `data` to read its current fields. Verify `X-Saturation-Signature`
        with the timestamp and raw body. Use `X-Saturation-Delivery-Id` to ignore duplicate deliveries.
      additionalProperties: false
      required:
      - id
      - event
      - workspaceId
      - occurredAt
      - data
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The stable delivery id (`whd_\u2026`); the dedupe key for at-least-once delivery."
        event:
          $ref: '#/components/schemas/WebhookEvent'
        workspaceId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: "The workspace the event belongs to (`ws_\u2026`)."
        projectId:
          allOf:
          - $ref: '#/components/schemas/Id'
          description: Present (and required) for project-scoped event kinds, so the consumer can build the fetch URL.
        occurredAt:
          type: string
          format: date-time
          description: ISO-8601 timestamp of the originating change.
        data:
          type: object
          description: The record to fetch.
          additionalProperties: false
          required:
          - kind
          - id
          properties:
            kind:
              type: string
              description: The entity kind (e.g. `transaction`, `budget`, `purchaseOrder`, `document`).
              examples:
              - transaction
            id:
              allOf:
              - $ref: '#/components/schemas/Id'
              description: The entity id to re-fetch.
              examples:
              - txn_8f2a1c9e
    WebhookCollection:
      type: object
      description: A paginated page of webhook subscriptions.
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Webhook'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when a permission/expand under-fetch bounded the page short of `limit`.
    WebhookDeliveryCollection:
      type: object
      description: A paginated page of delivery attempts for one subscription.
      additionalProperties: false
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WebhookDelivery'
        nextCursor:
          $ref: '#/components/schemas/NextCursor'
        count:
          type: integer
          description: Total matching items; present only when `withCount=true` was requested.
        truncated:
          type: boolean
          description: True when a permission/expand under-fetch bounded the page short of `limit`.
